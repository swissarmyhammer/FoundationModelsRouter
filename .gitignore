# Local agent / tooling state
.claude/
.sah/
.shell/
.code-context/
.ralph/

# macOS
.DS_Store

# Swift / Xcode
.build/
.swiftpm/
DerivedData/
*.xcodeproj/xcuserdata/
# Package.resolved is committed (not ignored): it pins the mlx-swift-lm branch
# dependency to a specific commit so builds are reproducible.
