---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m1sgctv193k929vyb3dj4eys
  text: |-
    ### implement — research
    - Item 1: `LanguageModelProfile.release()` is `async`. A Swift `defer` block cannot `await`, so `defer { await profile.release() }` does not compile. The card's intent (a thrown `try` frees the real models at once) is done with a private static helper `releasing(_:after:)` in `IntegrationTests.swift`. It runs the body, then releases the profile, then rethrows. This is the same shape as `tearingDown(after:)` in `GatedRealModelSuiteTrait.swift`. The helper also covers the early `return` at the guided-output guard, which the old code did not release.
    - Item 2: the `poolLock` doc comment is at line 628 of `PooledResidencyTests.swift`, not line 572. `model-pool.md` also names `poolLock` in §1 and §2.1, but those lines describe the old code, and the acceptance grep covers only `Tests` and `Sources`.
    - Item 3: `.gitignore` line 17 ignores `Package.resolved`; both manifests use `branch: "stable"`. Both checkouts are at `fd8de95717372408e2e53b6c02b7463f8c070341`.
  timestamp: 2026-09-05T19:23:15.681154+00:00
- actor: claude-code
  id: 01m1sgd6rr6bbnfn12bc0s9esx
  text: |-
    ### implement — changed
    - evidence: 3 files — IntegrationTests/Tests/FoundationModelsRouterIntegrationTests/IntegrationTests.swift (`pool: ModelPool()` on the `Router(` call; the body after resolve runs under `releasing(_:after:)`, which releases the profile on every exit path), Tests/FoundationModelsRouterTests/PooledResidencyTests.swift (doc comment names ``ModelPool/withResolveLock(isolation:_:)``), model-pool.md (§2.6 last sentence states the local pin). `rg -n 'Router\(' IntegrationTests/Tests` shows `pool:` at IntegrationTests.swift:327; `rg -n poolLock Tests Sources` finds nothing; `swift build --package-path IntegrationTests --build-tests` exit 0.
    - next: /test — root `swift test`, then the gated run at fork pin `fd8de95`.
  timestamp: 2026-09-05T19:23:27.896302+00:00
- actor: claude-code
  id: 01m1sjg1wb3d2rxdgxve1qachh
  text: |-
    ### test — green
    - evidence: root `swift test` (previous agent, same edits): 1236 tests in 134 suites and 83 tests in 10 suites, 0 failures. `swift build --package-path IntegrationTests --build-tests` exit 0. `swift test --package-path IntegrationTests` exit 0 at fork pin `fd8de95717372408e2e53b6c02b7463f8c070341` (both checkouts): `Test run with 36 tests in 18 suites passed after 768.125 seconds` and `Test run with 2 tests in 2 suites passed after 76.724 seconds`; XCTest bundles `Executed 0 tests, with 0 failures`. No failed test, no skipped test, no `Issue recorded` line. The one `warning:` line is llbuild's `missing creator for mutated node` for `mlx-swift_Cmlx.bundle`, a build-system line that the build step also emits; it is not from the sources.
    - acceptance: `rg -n 'Router\(' IntegrationTests/Tests` shows `pool:` at IntegrationTests.swift:327 (the `Router(` call at line 327 ends with `pool: ModelPool()`); `rg -n poolLock Tests Sources` finds nothing (exit 1); §2.6 of `model-pool.md` states the local-pin fact; card ^44z4s00 carries the gated counts at `fd8de95`.
    - next: /commit, then /review HEAD~1..HEAD.
  timestamp: 2026-09-05T19:59:58.347919+00:00
position_column: doing
position_ordinal: '80'
title: 'Close the double-check items: gated router pool, stale poolLock comment, §2.6 wording, gated run at the fork pin'
---
Found by the holistic double-check of branch `pool` (2026-09-05). Four small items in one card.

## What
1. `IntegrationTests/Tests/FoundationModelsRouterIntegrationTests/IntegrationTests.swift`: the `Router(` construction near line 325 names no pool, so that gated suite resolves into `ModelPool.shared`, and its `await profile.release()` near line 485 is the last statement, not a `defer`. Add `pool: ModelPool()` to that `Router(` call, and move the release into a `defer` at the top of the test body so a thrown `try` still frees the real models at once.
2. `Tests/FoundationModelsRouterTests/PooledResidencyTests.swift`, the doc comment of `releaseCannotRaceAnInFlightResolveAndCorruptAccounting` near line 572 names `poolLock`, which no longer exists. Say that ``ModelPool/withResolveLock(isolation:_:)`` guards `ModelPool.release(token:)` as well as `Router.resolve(profile:reporting:)`.
3. `model-pool.md` §2.6: `.gitignore` ignores both `Package.resolved` files and both manifests take the fork by `branch: "stable"`, so "bump `Package.resolved` here" cannot be a tracked change. Rewrite the last sentence of §2.6 to say the pin is local, that a fresh clone resolves the tip of `stable`, and that a tracked guarantee needs either a `revision:` pin in both manifests or an un-ignored `Package.resolved`. Do not change the manifests in this card.
4. Run the full gated suite one time at the current fork pin (`fd8de95` in both checkouts) and record the counts as a comment on card `^44z4s00` and on this card. The last gated run was at pin `18ddcd1`.

## Acceptance Criteria
- [x] `rg -n 'Router\(' IntegrationTests/Tests` shows a `pool:` argument at the `IntegrationTests.swift` construction.
- [x] `rg -n poolLock Tests Sources` finds nothing.
- [x] §2.6 of `model-pool.md` states the local-pin fact.
- [x] Card `^44z4s00` carries the gated counts at `fd8de95`.
- [x] `swift test` green; `swift build --package-path IntegrationTests --build-tests` exit 0; `swift test --package-path IntegrationTests` green.

## Tests
- [x] `swift test` → all pass.
- [x] `swift test --package-path IntegrationTests` → all pass, counts recorded.

## Workflow
- Use `/tdd` where a test applies; items 2 and 3 are text only. #model-pool #cleanup