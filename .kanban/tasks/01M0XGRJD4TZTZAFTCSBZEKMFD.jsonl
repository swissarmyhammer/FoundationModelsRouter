{"id":"01M0XGRJD471GRH6TJ4Y4WSDSB","timestamp":"2026-08-25T22:30:56.164557Z","op":"create","item_id":"01M0XGRJD4TZTZAFTCSBZEKMFD","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,31 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01M0XGQCF19BT6PM14919C9VV4\n+position_column: todo\n+position_ordinal: '8380'\n+title: 'Background engine: always return the handle, no race'\n+---\n+## What\n+Rework the engine in `Sources/FoundationModelsRouter/Hosting/DetachingTool.swift` so a background tool call never returns an in-band result:\n+\n+- [ ] In `Mode.background`, `call(arguments:)` starts the wrapped tool's body, tracks the run in the session's `SessionMailbox` at once (`track(tool:op:kind:completionToken:settling:canceler:)`), and returns the `PendingRunEnvelope` immediately. There is no wait window and no fast path.\n+- [ ] Delete the soft-deadline race: the window sleep, the window-elapse claim, and the claim arbitration between \"in-band result\" and \"detach\" (`RaceGate` use for the window). Keep the arbitration between natural settlement and `timeout` expiry — that race is real.\n+- [ ] Keep the timeout watcher unchanged: progress resets it, a pending elicitation suspends it, expiry cancels the work and settles the run as `OperationOutcome.timedOut` with one terminal event.\n+- [ ] Rewrite the `PendingRunEnvelope` `next` text: the tool now always answers with a handle; the text tells the model that the run continues, that the session will report the result when the run settles, and that `wait` with the `completionToken` collects it.\n+- [ ] In `Mode.runToCompletion`, behavior is unchanged: block to completion; a `timeout` expiry throws `DetachingToolError.timedOut(tool:timeoutSeconds:)` to the model as a named tool failure.\n+\n+## Acceptance Criteria\n+- [ ] A background tool call returns a `PendingRunEnvelope` on every call, also when the body completes in microseconds. A test proves it.\n+- [ ] `rg 'waitSeconds' Sources` returns no match.\n+- [ ] The exactly-one-terminal-event invariant holds across natural settle, cancel, timeout, and `close()` sweep. The existing tests for it stay green.\n+- [ ] `swift build` and the full suite are green.\n+\n+## Tests\n+- [ ] Rewrite the race cases in `Tests/FoundationModelsRouterTests/DetachingToolTests.swift` as immediate-handle cases.\n+- [ ] Simplify fixtures that used `waitSeconds: 0` (`detachAtOnceMount` in `DeclaredRunKindTests.swift`, `RegisteredJournalOpTests.swift`) — background mode now does this always.\n+- [ ] Run `swift test` — green.\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass. #long-running #api\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,31 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01M0XGQCF19BT6PM14919C9VV4\n-position_column: todo\n-position_ordinal: '8380'\n-title: 'Background engine: always return the handle, no race'\n----\n-## What\n-Rework the engine in `Sources/FoundationModelsRouter/Hosting/DetachingTool.swift` so a background tool call never returns an in-band result:\n-\n-- [ ] In `Mode.background`, `call(arguments:)` starts the wrapped tool's body, tracks the run in the session's `SessionMailbox` at once (`track(tool:op:kind:completionToken:settling:canceler:)`), and returns the `PendingRunEnvelope` immediately. There is no wait window and no fast path.\n-- [ ] Delete the soft-deadline race: the window sleep, the window-elapse claim, and the claim arbitration between \"in-band result\" and \"detach\" (`RaceGate` use for the window). Keep the arbitration between natural settlement and `timeout` expiry — that race is real.\n-- [ ] Keep the timeout watcher unchanged: progress resets it, a pending elicitation suspends it, expiry cancels the work and settles the run as `OperationOutcome.timedOut` with one terminal event.\n-- [ ] Rewrite the `PendingRunEnvelope` `next` text: the tool now always answers with a handle; the text tells the model that the run continues, that the session will report the result when the run settles, and that `wait` with the `completionToken` collects it.\n-- [ ] In `Mode.runToCompletion`, behavior is unchanged: block to completion; a `timeout` expiry throws `DetachingToolError.timedOut(tool:timeoutSeconds:)` to the model as a named tool failure.\n-\n-## Acceptance Criteria\n-- [ ] A background tool call returns a `PendingRunEnvelope` on every call, also when the body completes in microseconds. A test proves it.\n-- [ ] `rg 'waitSeconds' Sources` returns no match.\n-- [ ] The exactly-one-terminal-event invariant holds across natural settle, cancel, timeout, and `close()` sweep. The existing tests for it stay green.\n-- [ ] `swift build` and the full suite are green.\n-\n-## Tests\n-- [ ] Rewrite the race cases in `Tests/FoundationModelsRouterTests/DetachingToolTests.swift` as immediate-handle cases.\n-- [ ] Simplify fixtures that used `waitSeconds: 0` (`detachAtOnceMount` in `DeclaredRunKindTests.swift`, `RegisteredJournalOpTests.swift`) — background mode now does this always.\n-- [ ] Run `swift test` — green.\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass. #long-running #api\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M0XGY5FPBCEN8YY5Q3FXHW5F","timestamp":"2026-08-25T22:33:59.542354Z","op":"update","item_id":"01M0XGRJD4TZTZAFTCSBZEKMFD","forward_patch":"--- original\n+++ modified\n@@ -5,27 +5,28 @@\n - 01M0XGQCF19BT6PM14919C9VV4\n position_column: todo\n position_ordinal: '8380'\n-title: 'Background engine: always return the handle, no race'\n+title: Split the engine into two small wrappers; a background call always returns the handle\n ---\n ## What\n-Rework the engine in `Sources/FoundationModelsRouter/Hosting/DetachingTool.swift` so a background tool call never returns an in-band result:\n+`DetachingTool.swift` is ~1,600 lines because one type multiplexes two behaviors through a timing race. Replace it with two small, single-purpose types in `Sources/FoundationModelsRouter/Hosting/` (background vocabulary, no \"detach\" in new names):\n\n-- [ ] In `Mode.background`, `call(arguments:)` starts the wrapped tool's body, tracks the run in the session's `SessionMailbox` at once (`track(tool:op:kind:completionToken:settling:canceler:)`), and returns the `PendingRunEnvelope` immediately. There is no wait window and no fast path.\n-- [ ] Delete the soft-deadline race: the window sleep, the window-elapse claim, and the claim arbitration between \"in-band result\" and \"detach\" (`RaceGate` use for the window). Keep the arbitration between natural settlement and `timeout` expiry — that race is real.\n-- [ ] Keep the timeout watcher unchanged: progress resets it, a pending elicitation suspends it, expiry cancels the work and settles the run as `OperationOutcome.timedOut` with one terminal event.\n-- [ ] Rewrite the `PendingRunEnvelope` `next` text: the tool now always answers with a handle; the text tells the model that the run continues, that the session will report the result when the run settles, and that `wait` with the `completionToken` collects it.\n-- [ ] In `Mode.runToCompletion`, behavior is unchanged: block to completion; a `timeout` expiry throws `DetachingToolError.timedOut(tool:timeoutSeconds:)` to the model as a named tool failure.\n+- [ ] `RunToCompletionTool` (thin): runs the wrapped tool's body, returns its value. Optional `timeout` expiry throws one named error to the model. Owns correlation and event plumbing, nothing else.\n+- [ ] `BackgroundTool`: `call(arguments:)` starts the body, tracks the run in `SessionMailbox` at once (`track(tool:op:kind:completionToken:settling:canceler:)`), and returns the `PendingRunEnvelope` immediately — on every call, also when the body completes in microseconds. No wait window, no fast path, no in-band result. Keeps: the timeout watcher (progress resets it, elicitation suspends it, expiry cancels and settles as `timedOut`), the canceler with `RunKind` honesty, and exactly one terminal event per run.\n+- [ ] Delete the soft-deadline race and its claim arbitration (the `RaceGate` window use). Keep only the natural-settlement-versus-timeout arbitration — that race is real.\n+- [ ] Rewrite the `PendingRunEnvelope` `next` text: the run continues in the background; the session reports the result when the run settles; `wait` with the `completionToken` collects it earlier.\n+- [ ] Keep `PendingRunEnvelope`, `SessionMailbox`, `ToolContext`, and the journal contract unchanged in shape — only their prose and the callers change.\n\n ## Acceptance Criteria\n-- [ ] A background tool call returns a `PendingRunEnvelope` on every call, also when the body completes in microseconds. A test proves it.\n+- [ ] A background tool call returns a `PendingRunEnvelope` on every call. A test proves it, with a body that completes instantly.\n - [ ] `rg 'waitSeconds' Sources` returns no match.\n-- [ ] The exactly-one-terminal-event invariant holds across natural settle, cancel, timeout, and `close()` sweep. The existing tests for it stay green.\n+- [ ] The exactly-one-terminal-event invariant holds across natural settle, cancel, timeout, and `close()` sweep — existing tests stay green.\n+- [ ] The two new files are each an order of magnitude smaller than the old engine; the old `DetachingTool` type is gone.\n - [ ] `swift build` and the full suite are green.\n\n ## Tests\n-- [ ] Rewrite the race cases in `Tests/FoundationModelsRouterTests/DetachingToolTests.swift` as immediate-handle cases.\n+- [ ] Rewrite the race cases in `Tests/FoundationModelsRouterTests/DetachingToolTests.swift` as immediate-handle cases; split the file to match the two new types.\n - [ ] Simplify fixtures that used `waitSeconds: 0` (`detachAtOnceMount` in `DeclaredRunKindTests.swift`, `RegisteredJournalOpTests.swift`) — background mode now does this always.\n - [ ] Run `swift test` — green.\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass. #long-running #api\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -5,28 +5,27 @@\n - 01M0XGQCF19BT6PM14919C9VV4\n position_column: todo\n position_ordinal: '8380'\n-title: Split the engine into two small wrappers; a background call always returns the handle\n+title: 'Background engine: always return the handle, no race'\n ---\n ## What\n-`DetachingTool.swift` is ~1,600 lines because one type multiplexes two behaviors through a timing race. Replace it with two small, single-purpose types in `Sources/FoundationModelsRouter/Hosting/` (background vocabulary, no \"detach\" in new names):\n+Rework the engine in `Sources/FoundationModelsRouter/Hosting/DetachingTool.swift` so a background tool call never returns an in-band result:\n\n-- [ ] `RunToCompletionTool` (thin): runs the wrapped tool's body, returns its value. Optional `timeout` expiry throws one named error to the model. Owns correlation and event plumbing, nothing else.\n-- [ ] `BackgroundTool`: `call(arguments:)` starts the body, tracks the run in `SessionMailbox` at once (`track(tool:op:kind:completionToken:settling:canceler:)`), and returns the `PendingRunEnvelope` immediately — on every call, also when the body completes in microseconds. No wait window, no fast path, no in-band result. Keeps: the timeout watcher (progress resets it, elicitation suspends it, expiry cancels and settles as `timedOut`), the canceler with `RunKind` honesty, and exactly one terminal event per run.\n-- [ ] Delete the soft-deadline race and its claim arbitration (the `RaceGate` window use). Keep only the natural-settlement-versus-timeout arbitration — that race is real.\n-- [ ] Rewrite the `PendingRunEnvelope` `next` text: the run continues in the background; the session reports the result when the run settles; `wait` with the `completionToken` collects it earlier.\n-- [ ] Keep `PendingRunEnvelope`, `SessionMailbox`, `ToolContext`, and the journal contract unchanged in shape — only their prose and the callers change.\n+- [ ] In `Mode.background`, `call(arguments:)` starts the wrapped tool's body, tracks the run in the session's `SessionMailbox` at once (`track(tool:op:kind:completionToken:settling:canceler:)`), and returns the `PendingRunEnvelope` immediately. There is no wait window and no fast path.\n+- [ ] Delete the soft-deadline race: the window sleep, the window-elapse claim, and the claim arbitration between \"in-band result\" and \"detach\" (`RaceGate` use for the window). Keep the arbitration between natural settlement and `timeout` expiry — that race is real.\n+- [ ] Keep the timeout watcher unchanged: progress resets it, a pending elicitation suspends it, expiry cancels the work and settles the run as `OperationOutcome.timedOut` with one terminal event.\n+- [ ] Rewrite the `PendingRunEnvelope` `next` text: the tool now always answers with a handle; the text tells the model that the run continues, that the session will report the result when the run settles, and that `wait` with the `completionToken` collects it.\n+- [ ] In `Mode.runToCompletion`, behavior is unchanged: block to completion; a `timeout` expiry throws `DetachingToolError.timedOut(tool:timeoutSeconds:)` to the model as a named tool failure.\n\n ## Acceptance Criteria\n-- [ ] A background tool call returns a `PendingRunEnvelope` on every call. A test proves it, with a body that completes instantly.\n+- [ ] A background tool call returns a `PendingRunEnvelope` on every call, also when the body completes in microseconds. A test proves it.\n - [ ] `rg 'waitSeconds' Sources` returns no match.\n-- [ ] The exactly-one-terminal-event invariant holds across natural settle, cancel, timeout, and `close()` sweep — existing tests stay green.\n-- [ ] The two new files are each an order of magnitude smaller than the old engine; the old `DetachingTool` type is gone.\n+- [ ] The exactly-one-terminal-event invariant holds across natural settle, cancel, timeout, and `close()` sweep. The existing tests for it stay green.\n - [ ] `swift build` and the full suite are green.\n\n ## Tests\n-- [ ] Rewrite the race cases in `Tests/FoundationModelsRouterTests/DetachingToolTests.swift` as immediate-handle cases; split the file to match the two new types.\n+- [ ] Rewrite the race cases in `Tests/FoundationModelsRouterTests/DetachingToolTests.swift` as immediate-handle cases.\n - [ ] Simplify fixtures that used `waitSeconds: 0` (`detachAtOnceMount` in `DeclaredRunKindTests.swift`, `RegisteredJournalOpTests.swift`) — background mode now does this always.\n - [ ] Run `swift test` — green.\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass. #long-running #api\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M0XHB3A3NBX7GYNR3BG020ND","timestamp":"2026-08-25T22:41:03.299065Z","op":"update","item_id":"01M0XGRJD4TZTZAFTCSBZEKMFD","forward_patch":"--- original\n+++ modified\n@@ -3,6 +3,7 @@\n - claude-code\n depends_on:\n - 01M0XGQCF19BT6PM14919C9VV4\n+- 01M0XH9NMH6DYRNZDR06858XAS\n position_column: todo\n position_ordinal: '8380'\n title: Split the engine into two small wrappers; a background call always returns the handle\n@@ -10,18 +11,18 @@\n ## What\n `DetachingTool.swift` is ~1,600 lines because one type multiplexes two behaviors through a timing race. Replace it with two small, single-purpose types in `Sources/FoundationModelsRouter/Hosting/` (background vocabulary, no \"detach\" in new names):\n\n-- [ ] `RunToCompletionTool` (thin): runs the wrapped tool's body, returns its value. Optional `timeout` expiry throws one named error to the model. Owns correlation and event plumbing, nothing else.\n-- [ ] `BackgroundTool`: `call(arguments:)` starts the body, tracks the run in `SessionMailbox` at once (`track(tool:op:kind:completionToken:settling:canceler:)`), and returns the `PendingRunEnvelope` immediately — on every call, also when the body completes in microseconds. No wait window, no fast path, no in-band result. Keeps: the timeout watcher (progress resets it, elicitation suspends it, expiry cancels and settles as `timedOut`), the canceler with `RunKind` honesty, and exactly one terminal event per run.\n-- [ ] Delete the soft-deadline race and its claim arbitration (the `RaceGate` window use). Keep only the natural-settlement-versus-timeout arbitration — that race is real.\n+- [ ] `RunToCompletionTool` (thin): runs the wrapped tool's body, returns its value. `timeout` expiry throws one named error to the model. Owns correlation and event plumbing, nothing else.\n+- [ ] `BackgroundTool`: `call(arguments:)` starts the body, tracks the run in `SessionMailbox` at once (`track(tool:op:kind:completionToken:settling:canceler:)`), and returns the `PendingRunEnvelope` immediately — on every call, also when the body completes in microseconds. Keeps: the timeout watcher (progress resets it, elicitation suspends it, expiry cancels and settles as `timedOut`), the canceler with `RunKind` honesty, and exactly one terminal event per run. Uses the generation-permit rule from task ^6858xas.\n+- [ ] Delete the soft-deadline race and its claim arbitration (the `RaceGate` window use at the old `DetachingTool.swift:1001` area). Keep the natural-settlement-versus-timeout arbitration — that race is real.\n - [ ] Rewrite the `PendingRunEnvelope` `next` text: the run continues in the background; the session reports the result when the run settles; `wait` with the `completionToken` collects it earlier.\n-- [ ] Keep `PendingRunEnvelope`, `SessionMailbox`, `ToolContext`, and the journal contract unchanged in shape — only their prose and the callers change.\n+- [ ] Keep `PendingRunEnvelope`, `SessionMailbox`, `ToolContext`, and the journal contract unchanged in shape — only their callers change. Update the doc-only `waitSeconds` mentions in `Concurrency/RaceGate.swift:16`, `Hosting/ToolContext.swift:45`, `Hosting/SessionMailbox.swift:309`.\n\n ## Acceptance Criteria\n - [ ] A background tool call returns a `PendingRunEnvelope` on every call. A test proves it, with a body that completes instantly.\n-- [ ] `rg 'waitSeconds' Sources` returns no match.\n+- [ ] `rg 'waitSeconds' Sources` returns no match at all.\n - [ ] The exactly-one-terminal-event invariant holds across natural settle, cancel, timeout, and `close()` sweep — existing tests stay green.\n-- [ ] The two new files are each an order of magnitude smaller than the old engine; the old `DetachingTool` type is gone.\n-- [ ] `swift build` and the full suite are green.\n+- [ ] The two new files together are far smaller than the old engine; the old `DetachingTool` type is gone.\n+- [ ] `swift build --build-tests` and the full suite are green.\n\n ## Tests\n - [ ] Rewrite the race cases in `Tests/FoundationModelsRouterTests/DetachingToolTests.swift` as immediate-handle cases; split the file to match the two new types.\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,6 @@\n - claude-code\n depends_on:\n - 01M0XGQCF19BT6PM14919C9VV4\n-- 01M0XH9NMH6DYRNZDR06858XAS\n position_column: todo\n position_ordinal: '8380'\n title: Split the engine into two small wrappers; a background call always returns the handle\n@@ -11,18 +10,18 @@\n ## What\n `DetachingTool.swift` is ~1,600 lines because one type multiplexes two behaviors through a timing race. Replace it with two small, single-purpose types in `Sources/FoundationModelsRouter/Hosting/` (background vocabulary, no \"detach\" in new names):\n\n-- [ ] `RunToCompletionTool` (thin): runs the wrapped tool's body, returns its value. `timeout` expiry throws one named error to the model. Owns correlation and event plumbing, nothing else.\n-- [ ] `BackgroundTool`: `call(arguments:)` starts the body, tracks the run in `SessionMailbox` at once (`track(tool:op:kind:completionToken:settling:canceler:)`), and returns the `PendingRunEnvelope` immediately — on every call, also when the body completes in microseconds. Keeps: the timeout watcher (progress resets it, elicitation suspends it, expiry cancels and settles as `timedOut`), the canceler with `RunKind` honesty, and exactly one terminal event per run. Uses the generation-permit rule from task ^6858xas.\n-- [ ] Delete the soft-deadline race and its claim arbitration (the `RaceGate` window use at the old `DetachingTool.swift:1001` area). Keep the natural-settlement-versus-timeout arbitration — that race is real.\n+- [ ] `RunToCompletionTool` (thin): runs the wrapped tool's body, returns its value. Optional `timeout` expiry throws one named error to the model. Owns correlation and event plumbing, nothing else.\n+- [ ] `BackgroundTool`: `call(arguments:)` starts the body, tracks the run in `SessionMailbox` at once (`track(tool:op:kind:completionToken:settling:canceler:)`), and returns the `PendingRunEnvelope` immediately — on every call, also when the body completes in microseconds. No wait window, no fast path, no in-band result. Keeps: the timeout watcher (progress resets it, elicitation suspends it, expiry cancels and settles as `timedOut`), the canceler with `RunKind` honesty, and exactly one terminal event per run.\n+- [ ] Delete the soft-deadline race and its claim arbitration (the `RaceGate` window use). Keep only the natural-settlement-versus-timeout arbitration — that race is real.\n - [ ] Rewrite the `PendingRunEnvelope` `next` text: the run continues in the background; the session reports the result when the run settles; `wait` with the `completionToken` collects it earlier.\n-- [ ] Keep `PendingRunEnvelope`, `SessionMailbox`, `ToolContext`, and the journal contract unchanged in shape — only their callers change. Update the doc-only `waitSeconds` mentions in `Concurrency/RaceGate.swift:16`, `Hosting/ToolContext.swift:45`, `Hosting/SessionMailbox.swift:309`.\n+- [ ] Keep `PendingRunEnvelope`, `SessionMailbox`, `ToolContext`, and the journal contract unchanged in shape — only their prose and the callers change.\n\n ## Acceptance Criteria\n - [ ] A background tool call returns a `PendingRunEnvelope` on every call. A test proves it, with a body that completes instantly.\n-- [ ] `rg 'waitSeconds' Sources` returns no match at all.\n+- [ ] `rg 'waitSeconds' Sources` returns no match.\n - [ ] The exactly-one-terminal-event invariant holds across natural settle, cancel, timeout, and `close()` sweep — existing tests stay green.\n-- [ ] The two new files together are far smaller than the old engine; the old `DetachingTool` type is gone.\n-- [ ] `swift build --build-tests` and the full suite are green.\n+- [ ] The two new files are each an order of magnitude smaller than the old engine; the old `DetachingTool` type is gone.\n+- [ ] `swift build` and the full suite are green.\n\n ## Tests\n - [ ] Rewrite the race cases in `Tests/FoundationModelsRouterTests/DetachingToolTests.swift` as immediate-handle cases; split the file to match the two new types.\n","transaction_id":null}
