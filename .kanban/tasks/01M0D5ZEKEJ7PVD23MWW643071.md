---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m0d6pmytt48p2fnrk6v9f7np
  text: |-
    Research done. The work tree already holds staged renames of the two test directories and of the two smoke files. What stays open:

    - Struct names in the two smoke files are still `CompactionSmokeAutoTriggerTests` and `CompactionSmokeRecordedTranscriptTests`. They must go back to `AutoCompactionTriggerIntegrationTests` and `RecordedTranscriptCompactionIntegrationTests`, with the temp-directory prefixes and the cross-references in `CompactionSmokeIntegrationTests.swift`, `CompactionFold.swift`, the fixtures README and `Package.swift`.
    - `Package.swift`, `Scripts/swift-test.sh`, `.github/workflows/ci.yml`, `README.md`, `plan.md`, `compaction_plan.md`, two example READMEs, `Examples/CompactionDemo/main.swift` and eight doc comments still name the old targets or the `FoundationModelsRouterRealModel` prefix. The new shared token is the `IntegrationTests` suffix.
    - One collision found: the hermetic suite `ToolIntegrationTests` in `FoundationModelsRouterTests` carries the substring `IntegrationTests`. The selector `swift test --skip IntegrationTests` would silently skip its tests, and `--filter IntegrationTests` would pull them into the gated run. To keep the one-word selector exact, I rename that hermetic suite to `ToolSharedProfileTests` (its subject is the shared-profile constructor pattern). No other hermetic test identifier carries the substring; I checked all hermetic targets.
    - The smoke-tier one-word filter `CompactionSmoke` stops matching all three suites after the revert, so the README and doc comments state the three suite names in one alternation regex instead.
  timestamp: 2026-08-19T14:27:16.570619+00:00
- actor: claude-code
  id: 01m0d76v4kng1hxs8nkjfvvxq2
  text: |-
    Implementation landed. What changed, on top of the staged directory renames:

    - `Package.swift`: target `\(packageName)RealModelTests` -> `\(packageName)IntegrationTests`, target `\(packageName)RealModelEvals` -> `\(packageName)EvalIntegrationTests`, paths to match, and the prefix prose became suffix prose.
    - `Scripts/swift-test.sh` usage lines, `.github/workflows/ci.yml` (`--skip IntegrationTests` for the hermetic job, `--filter IntegrationTests` for the real-model job) and `README.md` now name the suffix selectors.
    - Suite renames reverted: `CompactionSmokeAutoTriggerTests` -> `AutoCompactionTriggerIntegrationTests` and `CompactionSmokeRecordedTranscriptTests` -> `RecordedTranscriptCompactionIntegrationTests`, with the temp-directory prefixes, the cross-references in `CompactionSmokeIntegrationTests.swift`, `CompactionFold.swift`, the fixtures README and `Package.swift`, and the `--filter CompactionSmoke` prose. The smoke tier's one-word filter is gone with the shared substring, so the README and `GatedSuiteSerialGate.swift` now state the three suite names in one alternation regex.
    - Every other doc comment and document that named an old target or the `FoundationModelsRouterRealModel` selector is updated: `GatedEvalSerialGate.swift`, `GatedSuiteSerialGate.swift`, `IntegrationTests.swift`, `RecordingHandleIntegrationTests.swift`, `CompactionEvalRealModelTests.swift`, the two eval Support files, `Summarization.swift`, `MultiTurnSessionTests.swift`, `CompactionSpikeTests.swift`, `MetalLibraryTestBootstrap.swift`, `Examples/CompactionDemo/main.swift`, both example READMEs, `plan.md`, `compaction_plan.md`.
    - Hermetic suite `ToolIntegrationTests` renamed to `ToolSharedProfileTests` (file, struct, temp-directory prefix). Its name carried the `IntegrationTests` substring, so `swift test --skip IntegrationTests` would have silently skipped its 8 hermetic tests and `--filter IntegrationTests` would have pulled them into the gated run. The rename makes the card's one-word selector exact. No other hermetic test identifier carries the substring.
    - Card ^cvsh3m9 (todo, READY) named the old targets in its instructions; its description now names the new ones. The done cards that record the rename history (^ryb01x7) stay as written — comments are history.

    Verification:

    - `rg 'FoundationModelsRouterRealModel'` over the tree (without `.git` and `.kanban`): zero matches.
    - `swift build --build-tests`: clean, zero compiler warnings.
    - `swift test list`: 1107 tests. The regex `IntegrationTests` matches all 40 tests of the two gated targets (37 + 3) and none of the 1067 hermetic tests (995 + 72). One word selects or excludes both targets exactly.
    - `Scripts/swift-test.sh --skip IntegrationTests`: 995 tests in 91 suites passed (1 pre-existing known issue in the main bundle, unchanged) plus 72 eval tests in 8 suites passed, exit 0. No gated suite ran.
    - One log line reads `warning: missing creator for mutated node ... mlx-swift_Cmlx.bundle`. That is swiftbuild noise about the dependency bundle after the directory renames, not a compiler warning; a repeat build prints no warning.
  timestamp: 2026-08-19T14:36:07.187969+00:00
- actor: claude-code
  id: 01m0d773020tr391y8x4j7qmvj
  text: |-
    ### implement — changed
    - evidence: 27 files renamed (the staged tree moves, plus Tests/FoundationModelsRouterTests/ToolSharedProfileTests.swift) and 24 files edited — Package.swift, Scripts/swift-test.sh, .github/workflows/ci.yml, README.md, plan.md, compaction_plan.md, Examples/CompactionDemo/{main.swift,README.md}, Examples/MultiModelGeneration/README.md, Sources/FoundationModelsRouter/Compaction/Summarization.swift, and 14 test files across the four test targets. `swift build --build-tests` clean, 0 warnings. `Scripts/swift-test.sh --skip IntegrationTests`: 995 + 72 tests passed, 0 failures, exit 0. `swift test list`: the `IntegrationTests` regex matches exactly the 40 tests of the two gated targets and none of the 1067 hermetic tests.
    - next: the card stays in `doing`, ready for `/review`. The orchestrator commits.
  timestamp: 2026-08-19T14:36:15.234529+00:00
- actor: claude-code
  id: 01m0d79sbva4qwmnn7e01v5nja
  text: |-
    ### test — green
    - evidence: Scripts/swift-test.sh --skip IntegrationTests — 995 tests in 91 suites passed with 1 known issue (BoundedWaitTests, pre-existing), plus 72 tests in 8 suites passed. 0 failed, 0 skipped.
    - evidence: swift build --build-tests -Xswiftc -warnings-as-errors — build complete, 0 warnings.
    - next: none. The suite is green and the build is warning-free.
  timestamp: 2026-08-19T14:37:43.675743+00:00
- actor: claude-code
  id: 01m0d8thhr0jmccmsv2qbmh22v
  text: |-
    ### review — clean
    - evidence: review sha HEAD~1..HEAD (commit 59de43f), backend local. Counts: 0 findings, 0 confirmed, 0 refuted. 60 files reviewed, 10 .kanban files excluded by .reviewignore. The tool notes about missing old file paths come from the rename; they are not findings.
    - next: none. The task moved to done.
  timestamp: 2026-08-19T15:04:21.304243+00:00
- actor: claude-code
  id: 01m0d8v3s8y8jfq534032cbce3
  text: |-
    ### finish iteration 1 — clean
    - implement: changed — 27 files renamed and 24 files edited; the two targets end in `IntegrationTests` again, and the hermetic `ToolIntegrationTests` suite became `ToolSharedProfileTests` to keep the selector exact
    - test: green — `Scripts/swift-test.sh --skip IntegrationTests`, 995 + 72 tests passed, 0 failures; `swift build --build-tests -Xswiftc -warnings-as-errors` clean
    - commit: 59de43f
    - review: clean — `review sha HEAD~1..HEAD`, 0 findings over 60 files; task moved to `done`
  timestamp: 2026-08-19T15:04:39.976353+00:00
position_column: done
position_ordinal: ffbf80
title: 'Undo the RealModel test-target rename: the two integration targets end in IntegrationTests again'
---
Commit `1db2b56` renamed the two real-model test targets without a good reason.
The names must go back to a form that ends in `IntegrationTests`.

## What to do

- [x] Rename target and directory `FoundationModelsRouterRealModelTests` back to `FoundationModelsRouterIntegrationTests`.
- [x] Rename target and directory `FoundationModelsRouterRealModelEvals` to `FoundationModelsRouterEvalIntegrationTests`.
- [x] Update `Package.swift`, `Scripts/swift-test.sh`, `.github/workflows/ci.yml`, `README.md`, and every doc comment or card that names an old target.
- [x] Revert the suite renames that `1db2b56` made only to make a shared `CompactionSmoke` substring.

Both target names then end in `IntegrationTests`, so one word selects or excludes both:

```
swift test --skip IntegrationTests
swift test --filter IntegrationTests
```

## What must stay

- The 16 deleted environment variables and their `.enabled(if:)` traits stay deleted.
- The target split stays: `FoundationModelsRouterEvalSupport` as a plain target, and the hermetic evals apart from the real-model evals.
- The `internal` to `package` widening stays.
- `Scripts/swift-test.sh` stays.

## Known facts

Swift Testing has tags, but SwiftPM cannot select by tag. `--filter` and `--skip`
are a regular expression over `<test-target>.<test-case>` only. The target is
therefore the unit of selection. Do not add tags. #test-debt #tests