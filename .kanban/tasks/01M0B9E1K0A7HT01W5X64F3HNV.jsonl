{"id":"01M0B9E1K0517S89JM164WMP85","timestamp":"2026-08-18T20:36:31.456591Z","op":"create","item_id":"01M0B9E1K0A7HT01W5X64F3HNV","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,65 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: 8b80\n+title: The pending-runs rendering can defeat the fold on its own — the retention bound never measures it\n+---\n+Found while `^azd033m` corrected the `summaryRetentionRatio` doc comment. The doc gap is corrected there. This card is the behaviour, which that card was told to leave alone.\n+\n+## The problem\n+\n+`Summarization.cut(_:toCharacters:)` bounds the summary TEXT. `Compactor`'s did-not-shrink guard measures the whole replacement ENTRY.\n+\n+`CompactionSegment.boundaryEntry(id:summaryText:content:)` puts a second `.text` segment on that entry when the session has parked runs. The segment holds `CompactionSegment.renderedPendingRuns(_:)`. `SegmentPayload.contentByteCount` counts a `.text` segment in full, so the guard sees those bytes. The cut never bounded them.\n+\n+Unlike the `User: `/`Assistant: ` labels, this cost does not scale with the span. The code charges it per parked run.\n+\n+## The measurement\n+\n+Measured with a byte-identical copy of `renderedPendingRuns(_:)` and a 26-character ULID completion token.\n+\n+| parked runs | op | progress | bytes |\n+|---|---|---|---|\n+| 1 | `run tool` | none | 217 |\n+| 3 | `run tool` | none | 385 |\n+| 5 | `run tool` | none | 553 |\n+| 6 | `run tool` | none | 637 |\n+| 10 | `run tool` | none | 973 |\n+| 10 | `generate completion` | 40 characters | 1413 |\n+\n+The heading is 134 bytes. One run line is 20 bytes of framing, plus the 26-character token, plus the op, plus 29 bytes when no progress was reported (or 22 bytes plus the detail when one was). Each further run adds that line and one line break.\n+\n+The cut's byte bound is `retained * Compactor.charsPerTokenEstimate`. The `minimumSummaryTokens` floor gives 512 bytes. The `maximumSummaryTokens` cap gives 2000 bytes.\n+\n+- Six parked runs come to 637 bytes. That is more than the whole 512-byte floor bound.\n+- Ten parked runs come to 973 bytes. That is 49% of the 2000-byte cap bound.\n+\n+## Why this is a defect and not only a doc gap\n+\n+`summaryRetentionRatio` is `0.8`, so the margin is about a fifth of the rendered content. The pending-runs rendering stays inside that margin only while its bytes are less than a fifth of the span bytes.\n+\n+- One parked run needs a span above about 1085 bytes.\n+- Ten parked runs need a span above about 4865 bytes.\n+\n+Below the `minimumSummaryTokens` floor the bound is a flat 512 bytes. Six parked runs exceed that bound on their own. No summary, however short, can then make the fold shrink the transcript.\n+\n+The result is bounded. `Compactor` discards the fold and returns the original transcript. That is the safe failure. But a session that parks background runs and then folds a modest span cannot compact at all, and that is the case the pending-runs feature exists for.\n+\n+## What to do\n+\n+1. Reproduce it first. Write a test that folds a small span with several parked runs and shows the fold discarded. Prove the pending-runs rendering is the cause: the same fold with no parked runs must be applied.\n+2. Then decide the fix. Do NOT change `summaryRetentionRatio` to hide the symptom. Options to weigh, each measured, not argued:\n+   - Subtract the pending-runs rendering from the bound the cut applies, so the cut bounds the whole replacement entry rather than the summary text alone.\n+   - Bound the pending-runs rendering itself, the way `ToolOutputCapping` bounds tool output.\n+   - Leave the boundary entry alone and state the requirement, if the measurement shows the case cannot arise.\n+3. Whatever is chosen, keep the four properties `^azd033m` protected: `Compactor` untouched, the empty-summary refusal before any cut, `outputTokenCeiling(forSummaryAllowance:)` and `reasoningTokenHeadroom` untouched, and no cut that can return an empty summary from a non-empty answer.\n+\n+## Constraints\n+\n+- Never run `swift format` or `swiftformat`.\n+- Do not set `FM_ROUTER_INTEGRATION_TESTS` or `FM_ROUTER_COMPACTION_EVAL_FULL_DATASET`. `FM_ROUTER_COMPACTION_SMOKE=1` is permitted.\n+\n+## Related\n+\n+- `^azd033m` — the card that measured this. Its doc comment now names both disagreements. #compaction #defect\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,65 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: 8b80\n-title: The pending-runs rendering can defeat the fold on its own — the retention bound never measures it\n----\n-Found while `^azd033m` corrected the `summaryRetentionRatio` doc comment. The doc gap is corrected there. This card is the behaviour, which that card was told to leave alone.\n-\n-## The problem\n-\n-`Summarization.cut(_:toCharacters:)` bounds the summary TEXT. `Compactor`'s did-not-shrink guard measures the whole replacement ENTRY.\n-\n-`CompactionSegment.boundaryEntry(id:summaryText:content:)` puts a second `.text` segment on that entry when the session has parked runs. The segment holds `CompactionSegment.renderedPendingRuns(_:)`. `SegmentPayload.contentByteCount` counts a `.text` segment in full, so the guard sees those bytes. The cut never bounded them.\n-\n-Unlike the `User: `/`Assistant: ` labels, this cost does not scale with the span. The code charges it per parked run.\n-\n-## The measurement\n-\n-Measured with a byte-identical copy of `renderedPendingRuns(_:)` and a 26-character ULID completion token.\n-\n-| parked runs | op | progress | bytes |\n-|---|---|---|---|\n-| 1 | `run tool` | none | 217 |\n-| 3 | `run tool` | none | 385 |\n-| 5 | `run tool` | none | 553 |\n-| 6 | `run tool` | none | 637 |\n-| 10 | `run tool` | none | 973 |\n-| 10 | `generate completion` | 40 characters | 1413 |\n-\n-The heading is 134 bytes. One run line is 20 bytes of framing, plus the 26-character token, plus the op, plus 29 bytes when no progress was reported (or 22 bytes plus the detail when one was). Each further run adds that line and one line break.\n-\n-The cut's byte bound is `retained * Compactor.charsPerTokenEstimate`. The `minimumSummaryTokens` floor gives 512 bytes. The `maximumSummaryTokens` cap gives 2000 bytes.\n-\n-- Six parked runs come to 637 bytes. That is more than the whole 512-byte floor bound.\n-- Ten parked runs come to 973 bytes. That is 49% of the 2000-byte cap bound.\n-\n-## Why this is a defect and not only a doc gap\n-\n-`summaryRetentionRatio` is `0.8`, so the margin is about a fifth of the rendered content. The pending-runs rendering stays inside that margin only while its bytes are less than a fifth of the span bytes.\n-\n-- One parked run needs a span above about 1085 bytes.\n-- Ten parked runs need a span above about 4865 bytes.\n-\n-Below the `minimumSummaryTokens` floor the bound is a flat 512 bytes. Six parked runs exceed that bound on their own. No summary, however short, can then make the fold shrink the transcript.\n-\n-The result is bounded. `Compactor` discards the fold and returns the original transcript. That is the safe failure. But a session that parks background runs and then folds a modest span cannot compact at all, and that is the case the pending-runs feature exists for.\n-\n-## What to do\n-\n-1. Reproduce it first. Write a test that folds a small span with several parked runs and shows the fold discarded. Prove the pending-runs rendering is the cause: the same fold with no parked runs must be applied.\n-2. Then decide the fix. Do NOT change `summaryRetentionRatio` to hide the symptom. Options to weigh, each measured, not argued:\n-   - Subtract the pending-runs rendering from the bound the cut applies, so the cut bounds the whole replacement entry rather than the summary text alone.\n-   - Bound the pending-runs rendering itself, the way `ToolOutputCapping` bounds tool output.\n-   - Leave the boundary entry alone and state the requirement, if the measurement shows the case cannot arise.\n-3. Whatever is chosen, keep the four properties `^azd033m` protected: `Compactor` untouched, the empty-summary refusal before any cut, `outputTokenCeiling(forSummaryAllowance:)` and `reasoningTokenHeadroom` untouched, and no cut that can return an empty summary from a non-empty answer.\n-\n-## Constraints\n-\n-- Never run `swift format` or `swiftformat`.\n-- Do not set `FM_ROUTER_INTEGRATION_TESTS` or `FM_ROUTER_COMPACTION_EVAL_FULL_DATASET`. `FM_ROUTER_COMPACTION_SMOKE=1` is permitted.\n-\n-## Related\n-\n-- `^azd033m` — the card that measured this. Its doc comment now names both disagreements. #compaction #defect\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M0BJRC9AMDWMHPXGP87V7VW4","timestamp":"2026-08-18T23:19:27.274125Z","op":"update","item_id":"01M0B9E1K0A7HT01W5X64F3HNV","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,16 @@\n ---\n assignees:\n - claude-code\n+comments:\n+- actor: claude-code\n+  id: 01m0bjrc992tn7z22z5j8nq1ep\n+  text: |-\n+    ## Audit at `dd55fcd2c` — LIVE\n+\n+    Re-checked, and the claim holds. `Sources/FoundationModelsRouter/Compaction/Summarization.swift:631` cuts `summary` alone. `Sources/FoundationModelsRouter/Compaction/CompactionSegment.swift:247-256` still appends the pending-runs text segment with no bound.\n+\n+    `Summarization.swift:268-277` documents the disagreement, which is what `^azd033m` was asked to do. But the behaviour did not change, so this card is still open work.\n+  timestamp: 2026-08-18T23:19:27.273803+00:00\n position_column: todo\n position_ordinal: 8b80\n title: The pending-runs rendering can defeat the fold on its own — the retention bound never measures it\n","reverse_patch":"--- original\n+++ modified\n@@ -1,16 +1,6 @@\n ---\n assignees:\n - claude-code\n-comments:\n-- actor: claude-code\n-  id: 01m0bjrc992tn7z22z5j8nq1ep\n-  text: |-\n-    ## Audit at `dd55fcd2c` — LIVE\n-\n-    Re-checked, and the claim holds. `Sources/FoundationModelsRouter/Compaction/Summarization.swift:631` cuts `summary` alone. `Sources/FoundationModelsRouter/Compaction/CompactionSegment.swift:247-256` still appends the pending-runs text segment with no bound.\n-\n-    `Summarization.swift:268-277` documents the disagreement, which is what `^azd033m` was asked to do. But the behaviour did not change, so this card is still open work.\n-  timestamp: 2026-08-18T23:19:27.273803+00:00\n position_column: todo\n position_ordinal: 8b80\n title: The pending-runs rendering can defeat the fold on its own — the retention bound never measures it\n","transaction_id":null}
{"id":"01M0DX319KX70XJDY38NEVZ2RE","timestamp":"2026-08-19T20:58:31.091318Z","op":"update","item_id":"01M0B9E1K0A7HT01W5X64F3HNV","forward_patch":"--- original\n+++ modified\n@@ -11,8 +11,8 @@\n\n     `Summarization.swift:268-277` documents the disagreement, which is what `^azd033m` was asked to do. But the behaviour did not change, so this card is still open work.\n   timestamp: 2026-08-18T23:19:27.273803+00:00\n-position_column: todo\n-position_ordinal: 8b80\n+position_column: doing\n+position_ordinal: '8480'\n title: The pending-runs rendering can defeat the fold on its own — the retention bound never measures it\n ---\n Found while `^azd033m` corrected the `summaryRetentionRatio` doc comment. The doc gap is corrected there. This card is the behaviour, which that card was told to leave alone.\n","reverse_patch":"--- original\n+++ modified\n@@ -11,8 +11,8 @@\n\n     `Summarization.swift:268-277` documents the disagreement, which is what `^azd033m` was asked to do. But the behaviour did not change, so this card is still open work.\n   timestamp: 2026-08-18T23:19:27.273803+00:00\n-position_column: doing\n-position_ordinal: '8480'\n+position_column: todo\n+position_ordinal: 8b80\n title: The pending-runs rendering can defeat the fold on its own — the retention bound never measures it\n ---\n Found while `^azd033m` corrected the `summaryRetentionRatio` doc comment. The doc gap is corrected there. This card is the behaviour, which that card was told to leave alone.\n","transaction_id":null}
{"id":"01M0DXWQ0R7CFCS3QV4YXA50ZC","timestamp":"2026-08-19T21:12:32.536224Z","op":"update","item_id":"01M0B9E1K0A7HT01W5X64F3HNV","forward_patch":"--- original\n+++ modified\n@@ -11,6 +11,26 @@\n\n     `Summarization.swift:268-277` documents the disagreement, which is what `^azd033m` was asked to do. But the behaviour did not change, so this card is still open work.\n   timestamp: 2026-08-18T23:19:27.273803+00:00\n+- actor: claude-code\n+  id: 01m0dxwq0qcqthv4mft26qmcj5\n+  text: |-\n+    ## Research — picked up\n+\n+    Code state confirmed at HEAD:\n+    - `Summarization.summarizeOnce` cuts each answer to `summaryRetentionRatio` of that CALL's content. `Summarization.apply` then hands the cut text to `CompactionSegment.boundaryEntry`, which adds a second `.text` segment with `renderedPendingRuns(_:)` when the session has parked runs. No bound measures that segment.\n+    - `TranscriptEntryPayload.contentByteCount` counts a `.text` segment in full and does NOT count the router's own `.structure` manifest (`RouterSegmentSchemaNames`). So the guard weighs exactly: summary bytes + rendering bytes.\n+    - `UTF8Budget.prefix(of:keepingAtMostBytes:)` returns \"\" for a limit at or below zero, and `Summarization.cut` then returns the summary unchanged. So a cut with a non-positive limit cannot empty the summary — the guard judges instead.\n+\n+    Fix decision (option 1 from the card): in `Summarization.apply`, after the summarizer answers, cut the FINAL summary once more to `retention(render(old span)) - renderedPendingRuns(pendingRuns).utf8.count`, only when `pendingRuns` is not empty. This makes the cut bound the whole replacement entry.\n+    - Option 2 (bound the rendering itself) was rejected: a truncated rendering drops completion tokens, and the tokens are the reason the rendering exists.\n+    - Option 3 does not hold: the hermetic test shows the case arises with plain arithmetic.\n+\n+    Scope check against later cards: `^51e9dyq` (mid-section truncation) and `^59fd9rt` (flash tier quality) are separate defects — this change touches neither the cut's boundary selection nor the tier order.\n+\n+    Blast radius: `apply` callers are `Compactor.compact` and tests. The only unit test that folds with parked runs is `RoutedSessionCompactTests.compactCarriesParkedRunAcrossBoundary` — one run (~250 rendered bytes), short canned summary, so the new cut is a no-op there. `pendingRuns` empty (the common path) is unchanged by construction.\n+\n+    Plan (TDD): RED test in `SummarizationStageTests` — a modest span (about 583 estimated tokens) with 10 parked runs must fold; today the entry is ~731 tokens against a 583-token span and the guard discards it. Plus one edge test: rendering alone at or over the bound → fold discarded safely, never applied with an emptied summary.\n+  timestamp: 2026-08-19T21:12:32.535846+00:00\n position_column: doing\n position_ordinal: '8480'\n title: The pending-runs rendering can defeat the fold on its own — the retention bound never measures it\n","reverse_patch":"--- original\n+++ modified\n@@ -11,26 +11,6 @@\n\n     `Summarization.swift:268-277` documents the disagreement, which is what `^azd033m` was asked to do. But the behaviour did not change, so this card is still open work.\n   timestamp: 2026-08-18T23:19:27.273803+00:00\n-- actor: claude-code\n-  id: 01m0dxwq0qcqthv4mft26qmcj5\n-  text: |-\n-    ## Research — picked up\n-\n-    Code state confirmed at HEAD:\n-    - `Summarization.summarizeOnce` cuts each answer to `summaryRetentionRatio` of that CALL's content. `Summarization.apply` then hands the cut text to `CompactionSegment.boundaryEntry`, which adds a second `.text` segment with `renderedPendingRuns(_:)` when the session has parked runs. No bound measures that segment.\n-    - `TranscriptEntryPayload.contentByteCount` counts a `.text` segment in full and does NOT count the router's own `.structure` manifest (`RouterSegmentSchemaNames`). So the guard weighs exactly: summary bytes + rendering bytes.\n-    - `UTF8Budget.prefix(of:keepingAtMostBytes:)` returns \"\" for a limit at or below zero, and `Summarization.cut` then returns the summary unchanged. So a cut with a non-positive limit cannot empty the summary — the guard judges instead.\n-\n-    Fix decision (option 1 from the card): in `Summarization.apply`, after the summarizer answers, cut the FINAL summary once more to `retention(render(old span)) - renderedPendingRuns(pendingRuns).utf8.count`, only when `pendingRuns` is not empty. This makes the cut bound the whole replacement entry.\n-    - Option 2 (bound the rendering itself) was rejected: a truncated rendering drops completion tokens, and the tokens are the reason the rendering exists.\n-    - Option 3 does not hold: the hermetic test shows the case arises with plain arithmetic.\n-\n-    Scope check against later cards: `^51e9dyq` (mid-section truncation) and `^59fd9rt` (flash tier quality) are separate defects — this change touches neither the cut's boundary selection nor the tier order.\n-\n-    Blast radius: `apply` callers are `Compactor.compact` and tests. The only unit test that folds with parked runs is `RoutedSessionCompactTests.compactCarriesParkedRunAcrossBoundary` — one run (~250 rendered bytes), short canned summary, so the new cut is a no-op there. `pendingRuns` empty (the common path) is unchanged by construction.\n-\n-    Plan (TDD): RED test in `SummarizationStageTests` — a modest span (about 583 estimated tokens) with 10 parked runs must fold; today the entry is ~731 tokens against a 583-token span and the guard discards it. Plus one edge test: rendering alone at or over the bound → fold discarded safely, never applied with an emptied summary.\n-  timestamp: 2026-08-19T21:12:32.535846+00:00\n position_column: doing\n position_ordinal: '8480'\n title: The pending-runs rendering can defeat the fold on its own — the retention bound never measures it\n","transaction_id":null}
{"id":"01M0DYGEK20YWGS3405HK4C314","timestamp":"2026-08-19T21:23:19.266585Z","op":"update","item_id":"01M0B9E1K0A7HT01W5X64F3HNV","forward_patch":"--- original\n+++ modified\n@@ -31,6 +31,29 @@\n\n     Plan (TDD): RED test in `SummarizationStageTests` — a modest span (about 583 estimated tokens) with 10 parked runs must fold; today the entry is ~731 tokens against a 583-token span and the guard discards it. Plus one edge test: rendering alone at or over the bound → fold discarded safely, never applied with an emptied summary.\n   timestamp: 2026-08-19T21:12:32.535846+00:00\n+- actor: claude-code\n+  id: 01m0dygek2aj84p8qza63ftb4m\n+  text: |-\n+    ## Implementation landed\n+\n+    What changed, per the card's steps:\n+\n+    1. Reproduced first (TDD RED). New hermetic test `aFoldWithParkedRunsOverAModestSpanIsStillApplied` in `Tests/FoundationModelsRouterTests/SummarizationStageTests.swift`: a modest span (about 583 estimated tokens) with ten parked runs (973 rendered bytes, the card's measured shape). Before the fix the run showed the fold DISCARDED: `stagesApplied: []`, `summary: nil`, `tokensAfter == tokensBefore == 1756`. The same test proves the cause: the control fold with no parked runs is applied, and three geometry pins (measured at run time, not hardcoded) show retention < span < retention + rendering.\n+\n+    2. Fix: option 1 from the card, measured. New private method `Summarization.boundaryBoundedSummary(_:folding:pendingRuns:)` — `apply` cuts the FINAL summary once more to `retention(render(old span)) - renderedPendingRuns(pendingRuns).utf8.count`, so the cut bounds the whole replacement entry. With `pendingRuns` empty (every bare-recipe caller) the method returns the summary untouched, so the common path is byte-identical. The test asserts the fixed invariant: stored summary bytes + rendering bytes <= retention bound, and the fold applies.\n+       - Option 2 (bound the rendering like ToolOutputCapping) rejected: truncation drops completion tokens, and the tokens are the reason the rendering exists.\n+       - Option 3 (state the requirement) does not hold: the RED run showed the case arises.\n+\n+    3. The four `^azd033m` properties hold: `Compactor.swift` untouched (diff shows 3 files, none of them Compactor); the empty-summary refusal still reads the raw answer in `summarizeOnce`, before every cut; `outputTokenCeiling(forSummaryAllowance:)` and `reasoningTokenHeadroom` untouched; and the second new test `aFoldWhosePendingRunsRenderingAloneReachesTheBoundIsDiscarded` pins that a rendering at or over the bound makes the guard discard the fold whole — the cut never returns an emptied summary (an emptied entry WOULD have fit under that fixture's span, so the test can tell the two apart).\n+\n+    Docs updated to match the behavior: `summaryRetentionRatio` (the second disagreement is now closed here), `cut(_:toCharacters:)` (a limit of zero or below takes the non-empty fallback), `apply`'s `pendingRuns` parameter, and `CompactionSegment.renderedPendingRuns` (now two callers). Test helpers: `makeModelAssistedFoldFixture` refactored onto a shared `makeSizedFoldFixture(phrase:repeatsPerText:)` so the two new fixtures do not copy its body.\n+\n+    Not run: real-model eval tiers. The change is a guard-clause no-op when `pendingRuns` is empty, and every eval recipe passes none, so the folds the evals measure are byte-identical. The hermetic eval wiring tests (75) all pass.\n+\n+    ### implement — changed\n+    - evidence: 3 files — Sources/FoundationModelsRouter/Compaction/Summarization.swift, Sources/FoundationModelsRouter/Compaction/CompactionSegment.swift, Tests/FoundationModelsRouterTests/SummarizationStageTests.swift. `Scripts/swift-test.sh --skip IntegrationTests` → 1020 tests in 96 suites passed (baseline 1018 + 2 new) and 75 tests in 8 suites passed; `swift build --build-tests -Xswiftc -warnings-as-errors` → Build complete.\n+    - next: review\n+  timestamp: 2026-08-19T21:23:19.266163+00:00\n position_column: doing\n position_ordinal: '8480'\n title: The pending-runs rendering can defeat the fold on its own — the retention bound never measures it\n","reverse_patch":"--- original\n+++ modified\n@@ -31,29 +31,6 @@\n\n     Plan (TDD): RED test in `SummarizationStageTests` — a modest span (about 583 estimated tokens) with 10 parked runs must fold; today the entry is ~731 tokens against a 583-token span and the guard discards it. Plus one edge test: rendering alone at or over the bound → fold discarded safely, never applied with an emptied summary.\n   timestamp: 2026-08-19T21:12:32.535846+00:00\n-- actor: claude-code\n-  id: 01m0dygek2aj84p8qza63ftb4m\n-  text: |-\n-    ## Implementation landed\n-\n-    What changed, per the card's steps:\n-\n-    1. Reproduced first (TDD RED). New hermetic test `aFoldWithParkedRunsOverAModestSpanIsStillApplied` in `Tests/FoundationModelsRouterTests/SummarizationStageTests.swift`: a modest span (about 583 estimated tokens) with ten parked runs (973 rendered bytes, the card's measured shape). Before the fix the run showed the fold DISCARDED: `stagesApplied: []`, `summary: nil`, `tokensAfter == tokensBefore == 1756`. The same test proves the cause: the control fold with no parked runs is applied, and three geometry pins (measured at run time, not hardcoded) show retention < span < retention + rendering.\n-\n-    2. Fix: option 1 from the card, measured. New private method `Summarization.boundaryBoundedSummary(_:folding:pendingRuns:)` — `apply` cuts the FINAL summary once more to `retention(render(old span)) - renderedPendingRuns(pendingRuns).utf8.count`, so the cut bounds the whole replacement entry. With `pendingRuns` empty (every bare-recipe caller) the method returns the summary untouched, so the common path is byte-identical. The test asserts the fixed invariant: stored summary bytes + rendering bytes <= retention bound, and the fold applies.\n-       - Option 2 (bound the rendering like ToolOutputCapping) rejected: truncation drops completion tokens, and the tokens are the reason the rendering exists.\n-       - Option 3 (state the requirement) does not hold: the RED run showed the case arises.\n-\n-    3. The four `^azd033m` properties hold: `Compactor.swift` untouched (diff shows 3 files, none of them Compactor); the empty-summary refusal still reads the raw answer in `summarizeOnce`, before every cut; `outputTokenCeiling(forSummaryAllowance:)` and `reasoningTokenHeadroom` untouched; and the second new test `aFoldWhosePendingRunsRenderingAloneReachesTheBoundIsDiscarded` pins that a rendering at or over the bound makes the guard discard the fold whole — the cut never returns an emptied summary (an emptied entry WOULD have fit under that fixture's span, so the test can tell the two apart).\n-\n-    Docs updated to match the behavior: `summaryRetentionRatio` (the second disagreement is now closed here), `cut(_:toCharacters:)` (a limit of zero or below takes the non-empty fallback), `apply`'s `pendingRuns` parameter, and `CompactionSegment.renderedPendingRuns` (now two callers). Test helpers: `makeModelAssistedFoldFixture` refactored onto a shared `makeSizedFoldFixture(phrase:repeatsPerText:)` so the two new fixtures do not copy its body.\n-\n-    Not run: real-model eval tiers. The change is a guard-clause no-op when `pendingRuns` is empty, and every eval recipe passes none, so the folds the evals measure are byte-identical. The hermetic eval wiring tests (75) all pass.\n-\n-    ### implement — changed\n-    - evidence: 3 files — Sources/FoundationModelsRouter/Compaction/Summarization.swift, Sources/FoundationModelsRouter/Compaction/CompactionSegment.swift, Tests/FoundationModelsRouterTests/SummarizationStageTests.swift. `Scripts/swift-test.sh --skip IntegrationTests` → 1020 tests in 96 suites passed (baseline 1018 + 2 new) and 75 tests in 8 suites passed; `swift build --build-tests -Xswiftc -warnings-as-errors` → Build complete.\n-    - next: review\n-  timestamp: 2026-08-19T21:23:19.266163+00:00\n position_column: doing\n position_ordinal: '8480'\n title: The pending-runs rendering can defeat the fold on its own — the retention bound never measures it\n","transaction_id":null}
{"id":"01M0DYK378B8SHMEZTGGJ7NHP4","timestamp":"2026-08-19T21:24:45.928192Z","op":"update","item_id":"01M0B9E1K0A7HT01W5X64F3HNV","forward_patch":"--- original\n+++ modified\n@@ -54,6 +54,18 @@\n     - evidence: 3 files — Sources/FoundationModelsRouter/Compaction/Summarization.swift, Sources/FoundationModelsRouter/Compaction/CompactionSegment.swift, Tests/FoundationModelsRouterTests/SummarizationStageTests.swift. `Scripts/swift-test.sh --skip IntegrationTests` → 1020 tests in 96 suites passed (baseline 1018 + 2 new) and 75 tests in 8 suites passed; `swift build --build-tests -Xswiftc -warnings-as-errors` → Build complete.\n     - next: review\n   timestamp: 2026-08-19T21:23:19.266163+00:00\n+- actor: claude-code\n+  id: 01m0dyk377ez3f4t0pad6ef4gm\n+  text: |-\n+    ### test — green\n+    - command: `Scripts/swift-test.sh --skip IntegrationTests`\n+    - result: Test run with 1020 tests in 96 suites passed after 5.388 seconds with 1 known issue (the known BoundedWaitTests issue).\n+    - result: Test run with 75 tests in 8 suites passed after 0.031 seconds.\n+    - command: `swift build --build-tests -Xswiftc -warnings-as-errors`\n+    - result: Build complete, exit code 0, no warnings.\n+    - No skipped tests. No real-model IntegrationTests suites were run. No FM_ROUTER_* env vars were used.\n+    - next: none, the suite is green.\n+  timestamp: 2026-08-19T21:24:45.927779+00:00\n position_column: doing\n position_ordinal: '8480'\n title: The pending-runs rendering can defeat the fold on its own — the retention bound never measures it\n","reverse_patch":"--- original\n+++ modified\n@@ -54,18 +54,6 @@\n     - evidence: 3 files — Sources/FoundationModelsRouter/Compaction/Summarization.swift, Sources/FoundationModelsRouter/Compaction/CompactionSegment.swift, Tests/FoundationModelsRouterTests/SummarizationStageTests.swift. `Scripts/swift-test.sh --skip IntegrationTests` → 1020 tests in 96 suites passed (baseline 1018 + 2 new) and 75 tests in 8 suites passed; `swift build --build-tests -Xswiftc -warnings-as-errors` → Build complete.\n     - next: review\n   timestamp: 2026-08-19T21:23:19.266163+00:00\n-- actor: claude-code\n-  id: 01m0dyk377ez3f4t0pad6ef4gm\n-  text: |-\n-    ### test — green\n-    - command: `Scripts/swift-test.sh --skip IntegrationTests`\n-    - result: Test run with 1020 tests in 96 suites passed after 5.388 seconds with 1 known issue (the known BoundedWaitTests issue).\n-    - result: Test run with 75 tests in 8 suites passed after 0.031 seconds.\n-    - command: `swift build --build-tests -Xswiftc -warnings-as-errors`\n-    - result: Build complete, exit code 0, no warnings.\n-    - No skipped tests. No real-model IntegrationTests suites were run. No FM_ROUTER_* env vars were used.\n-    - next: none, the suite is green.\n-  timestamp: 2026-08-19T21:24:45.927779+00:00\n position_column: doing\n position_ordinal: '8480'\n title: The pending-runs rendering can defeat the fold on its own — the retention bound never measures it\n","transaction_id":null}
