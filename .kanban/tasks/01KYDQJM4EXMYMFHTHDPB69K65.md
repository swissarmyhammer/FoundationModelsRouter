---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01kydwf8m87bsp1227ffzz4k6n
  text: |-
    Picked up; moved to `doing`. Read ^0r7mxew's comments + Outcome first, as instructed — the event-loss hazard is real and confirmed by reading the code: `runTurn` awaits `performAutoCompaction` before `runTurnAttempt`, and `finishTurnAndRequeueIfUnattached` is only reachable from inside `runTurnAttempt`.

    Design settled before touching code. Four source changes, all reuse rather than new mechanism:

    1. **Cancellable summarizer, not a bare check.** The acceptance criterion is "a cancellation landing *during* a fold's model-assisted stage stops that fold", so a pre-check alone is insufficient — the summarizer's model call itself has to be cancellable. `fold(prompt:budget:summarizer:)` will wrap a non-nil `summarizer` in a decorator that routes every `summarize(_:)` call through the existing `runCancellableModelCall(composedPrompt:_:)`. That gives, for free and by construction: the both-routes pre-check (`Task.isCancelled` + `cancelRequestedTurnId`), registration as `inFlightModelCall` so `cancelCurrentTurn()` actually cancels it, and `withTaskCancellationHandler` so the caller's own `Task` keeps propagating. A deterministic-only fold never calls `summarize`, so a cheap fold is byte-identically unaffected — which is exactly the card's "do not make a cheap fold slower or nondeterministic" constraint. Map-reduce means several summarizer calls per fold; each is separately registered, so a cancellation between chunks is caught by the next call's pre-check.
    2. **`performAutoCompaction` must rethrow `CancellationError` instead of degrading tiers.** Its three tiers (flash -> own model -> deterministic-only) currently `catch {}` and fall through, so a cancelled flash tier would silently fall through to the own-model tier and then to a deterministic fold that *applies*. `catch let error as CancellationError { throw error }` on both tiers.
    3. **`runTurn` catches the fold's throw, records the cut-short turn, and re-queues `pendingEvents` before rethrowing** — the hazard fix. Chosen over moving the proactive fold into `runTurnAttempt`, which would break the documented "the proactive gate is read exactly once per turn" property (the overflow retry re-enters `runTurnAttempt`, not `runTurn`).
    4. **Extract the failed-attempt recording tail** (`finishTurnAndRequeueIfUnattached` + the synthetic bodyless close) out of `runTurnAttempt`'s catch into a shared private helper, so (3) records a fold-cancelled turn *identically* to every other failed turn rather than inventing a second shape. Verified behavior-preserving: nothing else in that catch uses the returned `diffIncludedResponse`/`usage`.

    Chosen trace contract: a fold-cancelled turn records exactly one bodyless `.response` close and no `.prompt`, which is the same shape `cancellingAQueuedTurnNeverReachedTheModel` already pins for the other never-reached-the-model case. Leaving no trace would have forced an exception into `cancelCurrentTurn()`'s existing "every failed turn leaves a trace / exactly one close per turn" promise.

    Docs to update: `cancelCurrentTurn()` (the "what that does *not* do is interrupt the fold already running" paragraph), `TurnCancellationResult.requested`, `fold`'s and `performAutoCompaction`'s `- Throws:`, `runTurn`, and plan.md's turn-loop bullet ("what neither route interrupts is a fold already in progress").

    Test plan (TDD, tests first): the proactive fold needs metered fill to trigger, so `HookedSessionBackend` gains an opt-in per-turn measured-usage knob (default `nil`, so every existing test is untouched) plus a real `replacingTranscript(_:)`, and the fixture drives 6 warm-up turns with the last one metering — the same shape as `AutoCompactionTests.makeTriggeredSession`. Budget sized off the warm-up transcript's own recency-window floor so `Summarization` is forced. The park hook keys off a distinctive `CompactionPrompt` handed to `makeSession(compactionPrompt:)`, which is a *prefix* match on the summarizer's prompt, so `parkInsideCancellationAwareTool` gets a predicate-taking primary and keeps its `prompt:`-suffix overload for the 5 existing call sites.
  timestamp: 2026-07-26T00:16:19.592431+00:00
- actor: claude-code
  id: 01kydxpvvztmtn1x1844dgvrgc
  text: |-
    Implementation landed, green. `swift build --build-tests` exit 0, full `swift test` exit 0 at **607 tests / 69 suites** (577/58 + 18/7 + 12/4), three consecutive runs — up from the 601/69 baseline, so +6 tests (6 new test functions, one of them parameterized over both cancellation routes).

    **Files**

    - `Sources/FoundationModelsRouter/Session/RoutedSession.swift` — new file-scope `CancellableCompactionSummarizer`; `fold` wraps a non-nil summarizer in it; `performAutoCompaction` rethrows `CancellationError` per tier; `runTurn` brackets the proactive fold in do/catch and records the cut-short turn before rethrowing; `recordFailedTurn(...)` extracted from `runTurnAttempt`'s catch and now shared; `runCancellableModelCall` widened `private` -> `fileprivate` (documented at the declaration, one caller).
    - `Tests/FoundationModelsRouterTests/TurnCancellationTests.swift` — 6 new tests + fold fixtures.
    - `plan.md` — the turn-loop bullet's "what neither route interrupts is a fold already in progress" replaced by a full "A fold is cancellable too" paragraph.

    **Docs no longer claiming the old limitation:** `cancelCurrentTurn()` (the paragraph that said "What that does *not* do is interrupt the fold already running"), `TurnCancellationResult.requested` (narrowed from "a turn folding its own transcript has none" to "a turn part-way through a fold's deterministic stages has none"), `compact(prompt:budget:)` (now states a stop reaches it and it can throw `CancellationError`), `cancelRequestedTurnId`, `fold`'s and `performAutoCompaction`'s and `runTurn`'s `- Throws:`.

    **Red-green, watched in this order rather than asserted after the fact.** The tests were written first and the source changed in three stages, so each stage has its own watched failure:

    1. Tests only, no source change: the both-routes fold test failed (with the `.routerAPI` case never reaching the tool at all, and the `.callerTask` case showing **two** summarizer calls — proof the flash tier's cancellation was being swallowed and the next tier's model called), and the requeue/transcript tests failed at `awaitCancelledUnwind`'s bounded escape hatch in 0.14s. Nothing hung.
    2. With the wrapper + rethrows in, before the `runTurn` fix: **the requeue test failed with the outbox empty** — the hazard on the card, reproduced live, not theoretically. The transcript test failed with the recorder gaining *no* event at all (the turn left no trace).
    3. With the `runTurn` fix in: all green.

    Mutation re-checks on the final tree, each reverted after: removing the summarizer wrapping fails **5 tests in 0.21s with zero hangs**; removing **both** `CancellationError` rethrows in `performAutoCompaction` fails `cancelledProactiveFoldReportsNoCompaction`; removing the `recordFailedTurn(...)` call in `runTurn`'s catch fails the requeue test and both recording assertions.

    **Discoveries worth keeping**

    1. **A pre-check before the Summarization stage would not have satisfied the card.** The criterion is a stop landing *during* the model-assisted stage, so the summarizer's own call has to be cancellable — which is exactly what `runCancellableModelCall` already provides for a turn body. Routing `summarize(_:)` through it gets the both-routes pre-check, `inFlightModelCall` registration, and `withTaskCancellationHandler` in one move, with no new mechanism and nothing new to keep in lockstep.
    2. **Tier 3 (`summarizer: nil`) is structurally always a no-op once Summarization has been reached** — the deterministic stages are pure and already failed to land the transcript under target, so re-running them yields the same over-target result and the oversized-tail return. That is why the `CancellationError` rethrows are *not* observable as a half-applied fold; what they are observable as is a spurious `.compaction(stagesApplied: [])` event delivered to a `streamEvents` consumer for a turn that folded nothing. Hence `cancelledProactiveFoldReportsNoCompaction`, which pins them.
    3. **The two rethrows are individually redundant but jointly load-bearing**: with only the flash one removed, tier 2's pre-check throws and tier 2's rethrow escapes; with only tier 2's removed, the flash rethrow escapes first. Each is the *sole* guard on a distinct path, though — a session on the `flash` slot skips the flash tier entirely, and a non-flash session whose flash tier fails for a non-cancellation reason reaches tier 2 with the cancellation still outstanding. So a test can only go red when both are removed, which is how the mutation was run.
    4. **`RoutedSessionActor.backend` and `persistedEntryCount` are `private`**, so "the transcript is unchanged" cannot be asserted directly from a test. Asserted through the public surface instead: `contextFill` unchanged (a successful fold sets `usageState = .measured(result.tokensAfter, 0)` on the line right after the `backend` swap, so fill is a faithful companion signal for the swap) plus the recorder gaining exactly one bodyless `.response` and no fold entries. The first attempt at reading `actor.backend`/`actor.persistedEntryCount` was a compile error, which is what forced the better test.
    5. **The fold fixture needs metered usage, and metering must start late.** A proactive fold only triggers on measured `contextFill`, and `HookedSessionBackend.usageTokenCounts()` returned `nil` — so it gained an opt-in per-turn increment (default `nil`, leaving all 14 pre-existing tests byte-identical in behavior) that `makeFoldTriggeredSession` switches on only for the **last** of 6 warm-up turns. Metering from the first turn would cross the trigger with a near-empty transcript, and a fold with fewer than 5 turns has no old span, so `Summarization` returns `nil` and never calls a summarizer at all.
    6. **`HookedSessionBackend` now implements `replacingTranscript(_:)`.** The protocol default ignores the replacement and forwards to `makeFork()`, so a fixture that took it would leave a *successful* fold's `backend` holding the pre-fold entries while `persistedEntryCount` was set from the folded ones — the next turn's diff would then record entries no real session would. That mattered immediately for the deterministic-fold regression test, which folds for real.
    7. **`parkInsideCancellationAwareTool` needed a predicate, not a prompt.** A summarizer call is identified by its *prefix* (the compaction prompt's text, followed by the rendered span), while an ordinary turn is identified by its *suffix* (a drained outbox preamble precedes the caller's own prompt). The `parkingOn:` primary takes the predicate; the `prompt:` suffix spelling stays as a thin overload, so all 5 pre-existing call sites are untouched. The fold tests key off a distinctive `CompactionPrompt` passed to `makeSession(compactionPrompt:)`, so the hook fires for fold model calls and for nothing else.
    8. **`compact()`'s cancellation is now covered too.** The new doc claims a caller-driven `compact()` is cancelled on the same terms, and an untested doc claim is exactly what a previous review round on ^0r7mxew flagged, so `cancellingACallerDrivenCompactStopsIt` pins it — including gate balance, via `followUpTurnCompletes`.

    **Not changed, deliberately:** gate accounting (`beginTurn`/`endTurn`/`beginHumanWait`/`endHumanWait` are byte-identical), the `runCancellableModelCall` pre-check, the post-streaming-loop `try Task.checkCancellation()`, and the deterministic stages — no cancellation check was added inside `Compactor`/`ToolOutputElision`/`TurnTruncation`, so a cheap fold is exactly as fast and as deterministic as before. `deterministicOnlyFoldIsUnaffected` pins that: the fold applies `[ToolOutputElision, TurnTruncation]`, reports no summary, makes no summarizer call, and the turn's own streamed response arrives normally after it.

    Zero compiler warnings verified on a provably real recompile, not a cache hit: an unused-value canary was injected into both changed files, the warning was observed carrying each file's own path, then removed and the clean rebuild re-run.
  timestamp: 2026-07-26T00:37:57.247749+00:00
- actor: claude-code
  id: 01kydzdxw5s302xv59h6yxxt49
  text: |-
    Adversarial review came back REVISE with 6 findings. All 6 worked, and **two of them turned into real fixes** — one a behavioral bug in my own change, one a latent suite-hang. State now: `swift build --build-tests` exit 0 with zero first-party warnings, full `swift test` exit 0 at **609 tests / 69 suites** (579/58 + 18/7 + 12/4). Suite is 22 tests / 24 cases.

    **Finding 1 (Medium) — a real bug I introduced: the tier rethrow keyed off the error's *type*, so a summarizer raising `CancellationError` from its own internals killed a turn nobody cancelled.** `LanguageModelSessionBackend` is a public protocol; a conformer with an internal timeout or task group can surface `CancellationError` with nothing cancelled here. Before my change that degraded flash -> own-model -> deterministic and the turn survived; after it, the fold aborted and the turn died with a bogus `CancellationError` — while the same doc comment still promised, unqualified, that "a broken summarizer model can never block an automatic mid-turn fold outright". Fixed by keying on **an actually-outstanding cancellation** instead: a new `isTurnCancelled` computed property (`Task.isCancelled` OR `cancelRequestedTurnId == currentTurnId`) is now the single predicate every "is this turn still wanted?" decision asks — the pre-check in `runCancellableModelCall` and both tier catches. That also removes the risk the two routes drift apart between those three sites, which was the shape of an earlier defect on ^0r7mxew. Red-green: new test `summarizerCancellationErrorWithNoStopOutstandingStillDegrades` fails with `Caught error: CancellationError()` before the fix and passes after; both halves of the predicate are separately mutation-verified (neutering the recorded-request half fails 6 tests in 0.02s; neutering `Task.isCancelled` fails 5 tests / 11 issues in 0.02s), so the extraction did not weaken either pre-existing guarantee.

    **Finding 2 (Medium) — the reactive compact-and-retry-once fold's cancellability was claimed in two docs and exercised by no test.** The pre-existing `cancellationSurvivesIntoTheOverflowRetry` uses an inert budget whose reactive fold applies no stages and makes no summarizer call, so it could not reach the new code. New test `cancellingTheReactiveFoldStopsTheRetry`: session vended **unmetered** (so fill stays `0` and the proactive gate never fires — `makeFoldTriggeredSession` gained a `metersTriggeringFill` switch for exactly this), first attempt throws `contextSizeExceeded`, the reactive fold's summarizer parks, the turn is cancelled there. Asserts the caller gets `CancellationError` rather than the overflow it was recovering from, the model saw the turn's own prompt exactly **once** (the retry never ran), and **one** close is recorded, not two — `recordedBefore + 2` with `[.prompt, .response]` at the end and a bodyless close. The hook is composed on top of the summarizer park rather than replacing it, since this turn must overflow *and then* park inside the fold that overflow triggers.

    **Finding 5 (Low) — the "transcript untouched" claim rested on an unpinned ordering coincidence.** `contextFill` unchanged plus "recorder gained one bodyless close" only covers "`backend` untouched" *because* `fold` happens to append before swapping and to write `usageState` last. Closed it through the public event surface instead of widening `private` state: the test now drives one more turn (hook cleared) through a new bounded `followUpTurnEvents` helper and asserts that turn's own `.compaction` event measured `tokensBefore == Compactor.estimatedTokenCount(of: Transcript(entries: warmUpEntries()))` — the *untouched* warm-up transcript. Had the abandoned fold swapped `backend` for a folded one, that next fold would measure the smaller folded size. Confirmed discriminating: it fires under the neutered-predicate mutation. `followUpTurnEvents` is bounded by the same `spin(until:)` as `followUpTurnCompletes`, for the same reason.

    **Finding 3 (Low-Medium) — a comment claimed a detection the assertion cannot make.** "Exactly one summarizer call, not one per tier" cannot detect a lost tier rethrow, because `runCancellableModelCall`'s pre-check refuses the later tier's call *before* the hook is reached, so the count stays `1` either way. Comment rewritten to state what the assertion really pins (no further model call is entered while unwinding) and to point at `cancelledProactiveFoldReportsNoCompaction` as the test that guards the rule.

    **Finding 4 (Low) — the requeue was pinned on one route only.** `cancelledProactiveFoldRequeuesItsDrainedEvents` is now parameterized over `CancellationRoute.allCases`. On the caller-cancels route the re-queue itself runs inside an already-cancelled task, which is worth pinning: nothing about it may depend on the task still being live.

    **Finding 6 (Low) — the doc attributed turn-recording behavior to `compact()`, which has none.** `cancelCurrentTurn()`'s paragraph now splits what the caller observes: a *turn's* fold throws and is recorded like any other cut-short turn (lone bodyless close, attach-or-requeue), while a cancelled `compact()` throws `CancellationError` and leaves **no** trace at all — it drained no outbox and owns no turn recording, only the gates it hands back.

    **The most valuable thing the review produced was indirect: running its finding-1 mutation revealed that my new fold tests could HANG rather than fail.** Neutering the recorded-request predicate let a cancelled fold degrade to the next tier, whose summarizer call parked a *second* time on a semaphore nothing was left to signal — and `awaitCancelledUnwind`'s final `await turnTask.value` is unbounded once cancellation *has* been observed, so the suite hung for the full 300s timeout instead of failing. Fixed at the root, not per test: every fold test now parks on `firstSummarizerCall()`, a single-shot predicate that matches a fold's first summarizer call and lets later ones run straight through. The same mutation now fails **6 tests in 0.02s with zero hangs**. This is the same defect class two prior review rounds found in this file, in a new disguise — a park semaphore that a regression can arrive at twice is as dangerous as a bare `await respond(...)` after a cancel.

    **One reviewer suggestion deliberately not taken**, with reasoning: they noted the doc claim "each of a map-reduced fold's several calls included" is unexercised, since the warm-up transcript sits far under `Summarization`'s `maxChunkTokens` (2000) and every test drives the single-chunk path. Exercising it would need either a much larger fixture transcript (perturbing every fold test's sizing) or a production injection point for `maxChunkTokens` that exists only for a test — `Compactor.compact` constructs `Summarization()` with defaults, so there is none today. The claim is true by construction: `fold` wraps the summarizer once and *every* `summarize(_:)` call it makes — map, reduce, and re-reduce — goes through `runCancellableModelCall`, and the between-calls pre-check path is already exercised (that is exactly what refuses a later tier's call). Their own wording was "optionally". Left as is rather than bending the fixture or the production surface around it.
  timestamp: 2026-07-26T01:08:01.541410+00:00
- actor: claude-code
  id: 01kye033hmmss7mgjytn1ydwsj
  text: |-
    Adversarial re-check (bounded to one, per really-done): **PASS**, no new findings, all six earlier findings confirmed genuinely fixed and the hang fixed at the root rather than papered over. Its one advisory note is now applied: `followUpTurnEvents`' doc says outright why it awaits `task.value` on the give-up path while `followUpTurnCompletes` deliberately does not — this helper consumes an `AsyncThrowingStream`, whose `next()` is cancellation-aware and ends, whereas `followUpTurnCompletes` wraps a bare `respond(to:)` that can be parked in `AsyncSemaphore.wait()`, which ignores cancellation by design. Without that clause the two look gratuitously inconsistent and someone "fixing" them to match would reintroduce a suite hang.

    Two things the reviewer established that are worth keeping on the card:

    - **`cancelRequestedTurnId` now has exactly one read site in the whole file** (inside `isTurnCancelled`), so the two cancellation routes cannot diverge between decisions even in principle. That is the structural improvement, beyond the bug fix that motivated it.
    - **The discriminating assertion in `cancellingTheReactiveFoldStopsTheRetry` is the recorded-event count, not the `entered` count.** A completed reactive fold would still have its retry refused by the pre-check, so `entered.filter { $0.hasSuffix(overflowingFoldPrompt) }.count == 1` holds either way; `recorded.count == recordedBefore + 2` is what fails, because a completed fold appends diff partials. Also confirmed: the two-sided precondition `#expect((contextFill >= budget.trigger) == metersTriggeringFill)` is what stops that test quietly degrading into a proactive-fold test, and the summarizer prompt provably cannot end with `overflowingFoldPrompt` (with `keepRecentTurns: 4` against 6 warm-up turns, the dangling overflow `.prompt` sits inside the recency window the summarizer never sees), so the composed hook's suffix discriminator is safe with margin rather than by luck.

    Final state, left in `doing` for `/review`: `swift build --build-tests` exit 0 with no first-party diagnostics (only SwiftPM's pre-existing `missing creator for mutated node`), full `swift test` exit 0 at **609 tests / 69 suites** (579/58 + 18/7 + 12/4) on two further consecutive runs. Every acceptance-criteria and test checkbox on the card is `- [x]`. Files: `Sources/FoundationModelsRouter/Session/RoutedSession.swift`, `Tests/FoundationModelsRouterTests/TurnCancellationTests.swift`, `plan.md`.
  timestamp: 2026-07-26T01:19:35.476828+00:00
- actor: claude-code
  id: 01kye04kb15xbrhbht7btyq5by
  text: |-
    Orchestrator (/finish) iteration 1 — implement landed green, left in `doing`. 609 tests / 69 suites, exit 0 (baseline 601/69, so +8). Independent /test verification running before checkpoint commit + /review.

    An adversarial reviewer ran mid-implementation, raised six findings, and after they were fixed returned **PASS** with no new findings, independently re-verifying the build and suite rather than accepting the report.

    **The hazard this card was filed for was reproduced live, not just reasoned about.** With the summarizer made cancellable but *before* the `runTurn` fix, the requeue test failed **with the outbox empty** — confirming that a throw from the fold escapes through `generate`'s `defer { endTurn() }` and destroys the turn's already-drained `pendingEvents`. The fix: `runTurn` now brackets the proactive fold and calls a newly extracted `recordFailedTurn(...)` before rethrowing, so a fold-cancelled turn requeues its drained events and leaves the same lone bodyless close as any other cut-short turn. `recordFailedTurn(...)` is shared with `runTurnAttempt`'s catch so the two sites cannot record different shapes.

    **Two defects found in the implementer's own drafts — both worth not repeating:**

    1. **Do not key the tier rethrow on `CancellationError`'s type.** The first draft did, which meant a backend surfacing `CancellationError` from its *own* internals (e.g. an internal timeout) killed a turn nobody cancelled — while the doc still promised a broken summarizer can never block a fold. Now keyed on the new `isTurnCancelled` predicate. Good structural side effect: `cancelRequestedTurnId` now has exactly **one** read site in the file, so the caller-cancel and API-cancel routes cannot diverge even in principle.
    2. **New fold tests could hang rather than fail.** A degraded fold parked a *second* summarizer call on a semaphore nothing was left to signal, and `awaitCancelledUnwind`'s final await is unbounded once cancellation has been observed. Fixed at the root with a single-shot `firstSummarizerCall()` park predicate; the mutation that exposed it now fails 6 tests in 0.02s with zero hangs.

    **Reviewer's note to preserve:** `followUpTurnEvents` awaits `task.value` on its give-up path where `followUpTurnCompletes` deliberately does **not**. This asymmetry is correct — `followUpTurnEvents` consumes an `AsyncThrowingStream` whose `next()` is cancellation-aware and always terminates, whereas `followUpTurnCompletes` wraps a bare `respond()` that parks in `AsyncSemaphore.wait()` and must never be awaited. Do not "fix" the two to match; that reintroduces a suite hang.

    **Deliberate non-change, agreed by the reviewer:** multi-chunk map-reduce fold coverage is skipped. `fold` wraps the summarizer once and `Summarization` threads that single instance through `summarizeOnce` and every `reduce` round, so no path can reach the base summarizer — a test would only re-verify that `Summarization` passes its own parameter along, and buying it would require a test-only `maxChunkTokens` injection point in production code. Wrong trade.

    Also note a deterministic-only fold (`nil` summarizer) stays `nil` and gained no cancellation check, so it is byte-identically as fast as before.
  timestamp: 2026-07-26T01:20:24.417985+00:00
- actor: claude-code
  id: 01kye15tkmcbjygr48q8ezrx1d
  text: |-
    Test verification (tester agent): PASS. Forced full recompile of every first-party target (library, 3 test targets, both Examples/ executables) is warning-free apart from the pre-existing vendored `mlx-swift_Cmlx.bundle` planner warning. `swift test` = 609 tests / 69 suites, exit 0. Hang risk cleared empirically: 10 consecutive filtered runs (TurnCancellation|HumanWaitGate|ForkConcurrency|SessionChokepoint|MultiTurnSession|AsyncSemaphore|Compaction) and 11 consecutive full runs, all under timeout, zero hangs and zero flakes; the new tests contain no `Task.sleep` at all. Red-green proved: reverting only RoutedSession.swift makes 6 of the new fold tests fail with 13 issues in 9s *without hanging*, confirming the single-shot `firstSummarizerCall()` park predicate really removed the hang. Fixed one stray artifact of the change: RoutedSession.swift ended `}\n\n` where every sibling ends `}\n`.

    Adversarial double-check verdict was REVISE with three findings, all classified by the reviewer as coverage/diagnosability rather than defects — all five correctness probes (recordFailedTurn equivalence, double-recording, isTurnCancelled staleness, gate/lock leak, manual compact balance) cleared. Not actioned here, deliberately: they are test-authoring and logging changes on the implementer's side of the line, my mandate was verification plus failures/warnings, and another agent was committing to this repo concurrently (HEAD moved mid-session), so editing the test file risked a conflict. Handing them back for a decision:

    1. `cancellingACallerDrivenCompactStopsIt` only exercises `CancellationRoute.routerAPI`. Routing the summarizer through `runCancellableModelCall` structurally changed how a *caller-task* cancellation reaches a manual fold — it used to propagate structurally, and now reaches an unstructured `Task` only via `withTaskCancellationHandler` + the `isTurnCancelled` pre-flight. That is the most-changed public behavior on this diff and nothing pins it. Fix: parameterize over `CancellationRoute.allCases`, cancelling `compactTask` on the `.callerTask` route.
    2. `cancelledProactiveFoldLeavesTheTranscriptUntouched` and `cancelledProactiveFoldReportsNoCompaction` are unparameterized, so the "exactly one bodyless close" invariant is pinned only for `.routerAPI` — yet `.callerTask` is the riskier route for recording, since `recordFailedTurn` then runs inside an already-cancelled task. The same reasoning was applied to the outbox test but not these. Verified low-risk today (nothing under Recording/ checks cancellation, `append` is non-throwing async), so an unpinned invariant rather than a live bug.
    3. Both `if isTurnCancelled { throw CancellationError() }` branches in `performAutoCompaction` discard `error`, so a genuine summarizer fault coinciding with a stop is neither rethrown, degraded, nor logged. Suggest logging it on `sessionRecordingLogger` before the throw, as `recordTranscriptDelta`'s shrink guard already does.

    Also worth noting for the future: `Summarization.summarize`/`reduce` call `summarizeOnce` in a serial loop, which is what makes per-chunk `inFlightModelCall` registration safe. If that loop is ever parallelized, only the last-registered chunk would stay cancellable.
  timestamp: 2026-07-26T01:38:33.204276+00:00
- actor: claude-code
  id: 01kye3h4q9kadxht60zxk68enm
  text: |-
    Worked the tester's three hand-back findings plus the forward-looking safeguard. Left in `doing`. Full `swift test` exit 0 at **610 tests / 69 suites** (580/58 + 18/7 + 12/4), 19 consecutive full runs green today, no hangs, no flakes. `swift build --build-tests` exit 0 with no first-party diagnostics on a canary-proven real recompile (unused-value canaries injected into both changed library files and into **both** `Examples/` executables — `CompactionDemo/SampleTools.swift` and `MultiModelGeneration/main.swift` — each warning observed carrying its own path, then removed).

    **Files:** `Sources/FoundationModelsRouter/Session/RoutedSession.swift`, `Sources/FoundationModelsRouter/Compaction/Summarization.swift` (comments only), `Tests/FoundationModelsRouterTests/TurnCancellationTests.swift`.

    **Finding 1 — `cancellingACallerDrivenCompactStopsIt` parameterized over both routes.** Discriminating on exactly the mechanism the finding named: neutering `runCancellableModelCall`'s `onCancel { modelCall.cancel() }` fails the new `.callerTask` case (along with every other caller-task case) in 0.289s with zero hangs.

    **Finding 2 — `cancelledProactiveFoldLeavesTheTranscriptUntouched` and `cancelledProactiveFoldReportsNoCompaction` parameterized.** The first went green on both routes as-is. The second surfaced something real: **cancelling a task suspended in `AsyncThrowingStream.next()` *finishes* the stream rather than throwing from it**, so a cancelled stream *consumer* never sees `CancellationError` and `awaitCancelledUnwind` cannot be used on that route. `awaitCancelledUnwind` was therefore split — new `awaitCancellationReachingTheTool(_:observer:parked:) -> Bool` holds the bounded spin + force-unwind + diagnostic, and `awaitCancelledUnwind` is that plus the `#expect(throws:)` — so both spellings share one bound. Behavior-preserving for all 5 pre-existing callers (independently confirmed).

    **Finding 3 — chose LOGGING over surfacing, and why.** Rethrowing the tier's own fault was rejected outright: a stop the user asked for must surface as `CancellationError`, which is what `cancelCurrentTurn()`'s contract, `awaitCancelledUnwind`, and Swift's own cancellation convention all require — and a new test now *pins* that choice (mutating the flash tier to `throw error` fails it with `.summarizerFailed`). Since `CancellationError` carries no payload, the discarded fault is logged instead: a new file-private `sessionCompactionLogger` (its own `"Compaction"` category rather than `sessionRecordingLogger`'s `"Recording"` — a compaction outcome is not a recording one) plus `noteAbandonedFold(discarding:tier:)`, called from both tier catches. Shaped after `recordTranscriptDelta`'s shrink guard, this file's other "swallowed, so say so" case, and after the package's error-logging convention (`SessionSidecar`, `RepoMetadata`). A `FoldSummarizerTier` enum names the tier once instead of a literal per throw site. **The `guard !(error is CancellationError)` is a log-noise filter only** — it runs *after* `if isTurnCancelled`, returns `Void`, and `throw CancellationError()` follows unconditionally, so it cannot influence control flow. Not keying the decision on the error's type is the defect from an earlier round, and it stays not-keyed.

    **Safeguard — the serial-loop constraint is written down in three places** so it cannot be broken silently: `summarizeOnce`'s doc (the single funnel every summarizer call goes through), a one-line note above each of the two serial `summarizeOnce` loops, and the registration site in `CancellableCompactionSummarizer.summarize`. `Summarization` itself is untouched (comments only). Scoped precisely after review: concurrent chunks would be unreachable by **`cancelCurrentTurn()`** specifically — the caller-task route would still reach each call through its own `withTaskCancellationHandler`.

    **Adversarial review: REVISE (6 findings), all worked, then PASS on the bounded re-check.** Two were substantive:

    1. **My new test claimed a rule it did not pin.** Deleting *both* `if isTurnCancelled` blocks — i.e. mutating away the whole abandon-instead-of-degrade rule — left it green: the degraded tier's call is refused by `runCancellableModelCall`'s pre-flight *before* the hook is reached, so the summarizer count stays `1` either way (the exact caveat `cancellingAProactiveFoldStopsIt` already documents, which my test asserted the opposite of). Fixed by driving it through `streamEvents` and asserting `compactions.isEmpty` — the deterministic-only tier's `.compaction(stagesApplied: [])` is the *only* observable difference. That mutation now fails the test in 0.027s.
    2. **`privacy: .public` on `error.localizedDescription` would have opted transcript content out of redaction.** This error comes out of a summarizer call whose prompt is `Summarization.render(...)` of the folded span — user turns, tool outputs, reasoning — and `LanguageModelSessionBackend` is a public protocol whose conformers may echo the failing request into their error text. Now only the tier, the session id, and the error's *type* are `.public`; the description stays at `os.Logger`'s redacted default, matching the shrink guard's counts-and-ids-only policy.

    Also from that review: the `.callerTask` route of `cancelledProactiveFoldReportsNoCompaction` was **vacuous** for its headline assertion (a cancelled consumer receives nothing further, so `compactions.isEmpty` held regardless). Worth knowing why no assertion can fix that directly: tier 3 is structurally a no-op once `Summarization` has been reached, so `contextFill` and the recorder look identical abandoned-vs-degraded — the `.compaction` event is the sole discriminator, and that route cannot see it. So the route now carries a different, genuinely failing invariant instead: exactly one bodyless close, spun for because a cancelled consumer returns before the producer behind it finishes recording. Removing `recordFailedTurn(...)` from `runTurn`'s fold catch now fails **both** routes of that test in 0.097s. The `compactions.isEmpty` comment is scoped to say `.routerAPI` is what pins the degrade rule. Two doc overstatements were also corrected (`- Throws:` promised unconditional logging; `noteAbandonedFold`'s doc now names the heuristic's blind spot in both directions), and the pre-existing stale `sessionRecordingLogger` doc link five lines above the new code was repaired to the real `recordTranscriptDelta(grammar:since:usage:pendingEvents:onEvent:)` signature.

    **One finding declined, with justification (permitted by really-done):** the reviewer wanted the `is CancellationError` guard extracted as a testable pure function. Declined — there is no log-observation seam anywhere in `Tests/` (no `OSLogStore`, no injectable sink), so making it observable means widening `private` purely for a test, for a branch whose only effect is log volume; this file's comparable report (the shrink guard) is equally unobservable and equally untested. The re-check independently agreed and said not to spend the edit.

    **Untouched, deliberately:** the `followUpTurnEvents`/`followUpTurnCompletes` await asymmetry, the single-shot `firstSummarizerCall()` park predicate, `isTurnCancelled`'s one read site for `cancelRequestedTurnId`, `recordFailedTurn` sharing, gate accounting, `endHumanWait()`'s re-validation, and multi-chunk map-reduce coverage. No `swift format` run. Note `git` HEAD moved under this session (a concurrent agent), so `git diff` shows this card's earlier work as uncommitted too — the working tree is correct and green.
  timestamp: 2026-07-26T02:19:41.161310+00:00
- actor: claude-code
  id: 01kye6559g5anbfv3ab2njma2c
  text: |-
    Pulled back from `review` to `doing` and worked the single `## Review Findings (2026-07-25 21:29)` item; it is now `- [x]`. Left in `doing`.

    **Files:** `Sources/FoundationModelsRouter/Session/RoutedSession.swift` (only source file touched; the finding was confined to it).

    **The extraction.** The verbatim-duplicated guard in `performAutoCompaction(prompt:budget:)` is now one non-`async`, actor-isolated `private func abandonFoldIfCancelled(discarding error: Error, tier: FoldSummarizerTier) throws` — `guard isTurnCancelled else { return }` / `noteAbandonedFold(discarding: error, tier: tier)` / `throw CancellationError()` — called as `try abandonFoldIfCancelled(discarding: error, tier: .flash)` and `... tier: .ownModel)` from the two tier catches. Placed between `FoldSummarizerTier` and `noteAbandonedFold` so the file reads caller -> tier vocabulary -> decision -> report.

    **The card's critical constraint is honored structurally, not by comment:** the helper returns `Void` when nothing is cancelled, so each fall-through stays at its own call site and the two are not collapsed — the flash catch ends after the call and falls into the own-model `do`; the own-model catch follows the call with `return try await fold(..., summarizer: nil)`. A `throw` from inside a `catch` is not caught by that block's own `do`, so propagation is byte-equivalent to the inline `throw` it replaced.

    **Non-obvious reason the helper must stay non-`async`:** `isTurnCancelled`'s doc requires it be read *after* every `await` that matters and never cached across one. A synchronous same-actor call introduces no suspension point and no new `Task`, so `Task.isCancelled` reads the identical task at the identical point relative to the failing `await fold(...)`. An `async` helper would have been a semantic change dressed as a refactor.

    **Root-cause sweep, as asked.** Walked commit `fbbe4c3`'s complete non-comment addition set to `RoutedSession.swift` (94 added/removed code lines, comments and doc filtered out). The two tier guards were the last remaining duplicate: `recordFailedTurn(...)` and `isTurnCancelled` were already extractions in that same commit, and `recordFailedTurn`'s two call sites are the shared helper with genuinely different surroundings (one rethrows; the other continues into the overflow-retry path). A re-review of this file should find zero recurrences. Independently confirmed by the adversarial reviewer, who walked the same set.

    **Load-bearing properties verified unchanged, each by evidence rather than by reading:**

    - **Decision still keyed on `isTurnCancelled`, never on `CancellationError`'s type.** Mutation: `guard isTurnCancelled` -> `guard error is CancellationError` fails `summarizerCancellationErrorWithNoStopOutstandingStillDegrades` with `Caught error: CancellationError()` in 0.074s. Reverted. The only `error is CancellationError` test left in the file is still `noteAbandonedFold`'s log-noise filter, which runs *after* the decision and returns `Void`.
    - **`cancelRequestedTurnId` still has exactly one read site** — inside `isTurnCancelled` (`return cancelRequestedTurnId == turnId`). The other two occurrences are the writes in `cancelCurrentTurn()` and `endTurn()`. Untouched by this diff.
    - **The abandon-instead-of-degrade rule is still pinned.** Mutation: deleting both `try abandonFoldIfCancelled(...)` calls fails 4 assertions across 3 tests in 0.025s, no hangs (`cancellingTheReactiveFoldStopsTheRetry`'s recorded-event count, `cancelledProactiveFoldReportsNoCompaction`'s `.routerAPI` `compactions.isEmpty`, and `summarizerFaultCoincidingWithAStopIsAbandonedAsCancelled`). Reverted.
    - **The uncancelled degradation path still works:** `swift test --filter proactiveFoldFallsBackToOwnModelWhenFlashFails` passes on its own ("when the flash summarizer fails, auto-compaction falls back to the session's own model").
    - Log privacy, the `CancellationError`-not-the-tier's-fault contract, `runTurn`'s bracketing + `recordFailedTurn(...)`, the sharing of `recordFailedTurn`, the `firstSummarizerCall()` park predicate, the `awaitCancelledUnwind`/`awaitCancellationReachingTheTool` split, the `followUpTurnEvents`/`followUpTurnCompletes` await asymmetry, gate accounting and `endHumanWait()`'s re-validation: all untouched — the diff is 53 changed lines in one function plus one new helper and two doc-wording repairs, and `Tests/` was not edited at all.

    **Known limit, pre-existing and not introduced here:** neither call site is *individually* pinned. Deleting only the `.flash` call leaves the tests green, because the fall-through then hits `runCancellableModelCall`'s pre-flight, which throws `CancellationError`, which the surviving `.ownModel` guard abandons with an identical observable outcome. That is the guard topology this card already recorded (the two guards are individually redundant but jointly load-bearing), which is why both mutations had to remove both sites. The extraction makes it moot in the direction that matters — there is now one body, so the two tiers cannot drift.

    **Doc handling.** The long rationale comment that sat inline at the flash catch moved into the helper's doc rather than being duplicated a third time; `performAutoCompaction`'s `- Throws:` keeps the caller-facing contract and the helper cross-references it, so nothing was lost when the inline copy went. One piece of prose my own change made stale was repaired: `FoldSummarizerTier`'s "rather than from a string literal per throw site" -> "at each tier's `catch`", since after the extraction there is exactly one throw site.

    **Adversarial double-check: REVISE with 2 findings, both real, both applied; no behavioral defect found and every constraint above independently re-verified by it.**

    1. **The new `- Parameters: error` prose promised logging unconditionally** — but `noteAbandonedFold` opens `guard !(error is CancellationError) else { return }`, so the majority case is *not* logged. This is the exact defect class an earlier round on this card already fixed once on `performAutoCompaction`'s `- Throws:`; I reintroduced it in a new place. Now carries the qualifier ("unless it is itself a `CancellationError`, the usual case and the one with nothing to say").
    2. **"Shared by every tier" was over-broad** — `performAutoCompaction`'s own doc counts the deterministic `nil`-summarizer fold as a tier, and it has no `do`/`catch` and no guard at all, so the sentence contradicted its own summary line. Now "Shared by both of ...'s model-assisted tiers — the two `FoldSummarizerTier` cases, since the deterministic fold beneath them makes no model call to abandon".

    Its bounded re-check was not spent: both findings were doc wording only, and the applied text is the reviewer's own suggested wording, so a second pass had nothing new to read. Recording that here rather than leaving it implicit, per really-done.

    **Verification on the final tree.** `swift build --build-tests` exit 0 after `touch`ing every `.swift` file under `Sources Tests Examples` (a real recompile of all six targets, including the `MultiModelGeneration` and `CompactionDemo` executables — 7.84s, not a cache hit), with no first-party diagnostics; the only warning is SwiftPM's pre-existing vendored `missing creator for mutated node: mlx-swift_Cmlx.bundle`. Full `swift test` exit 0 at **610 tests / 69 suites** (580/58 + 18/7 + 12/4) on three consecutive runs — unchanged from the pre-refactor count, as a pure extraction must be. No `swift format` run.
  timestamp: 2026-07-26T03:05:34.256263+00:00
position_column: doing
position_ordinal: '80'
title: Make a compaction fold observe cancellation, so a client stop lands during a long fold too
---
## What

`cancelCurrentTurn()` (task ^0r7mxew) cancels the turn's **model call**. A turn that is *folding its own transcript* has no model call outstanding, so a stop landing in that window is remembered against the turn and honored by its next model call — the fold itself runs to completion first.

That window is not small. A proactive auto-compaction fold (`autoCompactionBudget` reached) runs the whole `Compactor` pipeline and, when the deterministic stages cannot land it, a full model-assisted `Summarization` generation on the flash slot. A user pressing stop during it currently waits out a summarization they asked to abandon. `compact(prompt:budget:)`'s caller-driven fold has the same shape, though there the caller owns the enclosing `Task` and can cancel it.

`RoutedSession.cancelCurrentTurn()`'s doc states the limitation outright today ("What that does *not* do is interrupt the fold already running"), and `plan.md` says the same, so this is a documented gap rather than a silent one.

## Why it was left out of ^0r7mxew

Two reasons, both worth knowing before starting:

1. **Scope.** That task's contract was cancelling the `Task` that owns `body(composedPrompt)`. A fold's summarizer call is not that.
2. **An event-loss hazard sits in the obvious implementation.** `runTurn` calls `try await performAutoCompaction(...)` *before* `runTurnAttempt`, and the turn's drained `pendingEvents` are re-queued only inside `runTurnAttempt` (via `finishTurnAndRequeueIfUnattached`). So making `performAutoCompaction`/`fold` throw `CancellationError` mid-fold would drop the events this turn drained from the outbox — silently destroying exactly the state ^0r7mxew was careful to preserve. `performAutoCompaction`'s doc currently promises it throws nothing from summarization, and `runTurn` relies on that.

## Work

- Have the fold path observe cancellation — at minimum before its model-assisted `Summarization` stage, which is the expensive part — for both `cancelCurrentTurn()` (`cancelRequestedTurnId`) and the caller's own `Task` (`Task.isCancelled`).
- Decide what a turn cancelled mid-fold leaves behind, and make it as well-defined as a turn cancelled mid-model-call already is:
  - the drained `pendingEvents` must be re-queued, not lost (see hazard 2 above — the requeue currently lives in the wrong place for this);
  - the transcript must not gain a half-applied fold (`fold` swaps `backend` and bumps `persistedEntryCount` only after appending the fold's new entries — check that an abandoned fold leaves the session exactly as it was);
  - whether the caller sees `CancellationError` from the fold, and whether a *proactive* fold's cancellation still records a turn trace at all (today a throw from `performAutoCompaction` escapes `runTurnAttempt`'s recording bracket entirely).
- Deterministic-stage-only folds are fast; do not add cancellation checks that make a cheap fold slower or its result nondeterministic.
- Update `RoutedSession.cancelCurrentTurn()`'s doc and `plan.md`'s turn-loop bullet, both of which currently state the opposite.

## Acceptance Criteria

- [x] A cancellation landing during a fold's model-assisted stage stops that fold instead of waiting it out, by both cancellation routes.
- [x] A turn cancelled mid-fold re-queues its drained outbox events rather than destroying them.
- [x] A turn cancelled mid-fold leaves the transcript and `backend` exactly as they were, never a half-applied fold.
- [x] What the caller observes is documented and matches the mid-model-call case.
- [x] Docs (`cancelCurrentTurn()`, `plan.md`) no longer state the old limitation.

## Tests

- [x] Cancelling a turn parked inside a proactive fold's summarizer call unwinds it, for `cancelCurrentTurn()` and for the caller's own `Task`.
- [x] That turn's drained outbox events are still pending afterwards.
- [x] The session's transcript/entry count is unchanged by the abandoned fold, and a later turn on the session succeeds.
- [x] A deterministic-only fold with no cancellation outstanding behaves exactly as it does today (regression).

## Outcome

The fold's summarizer is wrapped in a `CancellableCompactionSummarizer` that routes every `summarize(_:)` call through the existing `runCancellableModelCall(composedPrompt:_:)` — so a fold's model call gets the both-routes pre-check, `inFlightModelCall` registration, and `withTaskCancellationHandler` on exactly the same terms as a turn's own, with no new mechanism. A pre-check alone was rejected: the criterion is a stop landing *during* the model-assisted stage. `performAutoCompaction` now rethrows `CancellationError` per tier instead of degrading flash -> own-model -> deterministic-only, so a stop is never answered with more model calls. The event-loss hazard is fixed by bracketing `runTurn`'s proactive fold and calling a new shared `recordFailedTurn(...)` — extracted from `runTurnAttempt`'s catch — before rethrowing, so a fold-cancelled turn re-queues its drained events and leaves exactly the same lone bodyless close as any other cut-short turn. Deterministic stages are untouched: a `nil` summarizer stays `nil`, so a cheap fold gained no check.

## Review Findings (2026-07-25 21:29)

- [x] `Sources/FoundationModelsRouter/Session/RoutedSession.swift` — in `performAutoCompaction(prompt:budget:)`, the cancellation guard `if isTurnCancelled { noteAbandonedFold(discarding: error, tier: <tier>); throw CancellationError() }` is duplicated verbatim across the flash-tier and own-model-tier `catch` blocks, differing only in the tier name (`.flash` vs `.ownModel`). Extract it into one tier-parameterized helper — e.g. `private func abandonFoldIfCancelled(discarding error: Error, tier: FoldSummarizerTier) throws` — called from both `catch` blocks, so the two tiers cannot drift apart. Each site's fall-through differs (flash continues to the own-model tier; own-model continues to the deterministic `nil`-summarizer fold), so the helper must simply return when no cancellation is outstanding and leave the fall-through at the call site.