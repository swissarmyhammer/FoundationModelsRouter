---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01kx5h9dd1d84m9ntk7e5r88ta
  text: |-
    Implemented. Production diff is entirely in Sources/FoundationModelsRouter/Session/RoutedSession.swift (plus a one-line persistedEntryCount:0 in RoutedLLM.swift's root-session construction):

    - Added `persistedEntryCount: Int` actor state, threaded through the init (new required param, no default) and set to 0 for root sessions (RoutedLLM.swift) / captured as `backend.transcriptEntries().count` inside the existing serial-gate hold in `fork(workingDirectory:)`, immediately before `makeFork()`, so the read and the fork snapshot can't race.
    - Replaced the hand-built `.prompt`-open/`.response`-close events in `generate(grammar:_:)` with `recordTranscriptDelta(grammar:since:)`: reads `backend.transcriptEntries()`, clamps defensively via `entries[min(persistedEntryCount, entries.count)...]` (never a bare `entries[persistedEntryCount...]`), detects shrink (`entries.count < persistedEntryCount`) and on shrink logs a warning, records nothing, and resets the baseline. Otherwise maps every new entry through `TranscriptEntryMapper.event(from:)` and appends one event per entry (entry payload + flattened text; tokensIn/tokensOut stay nil), stamping `ms` only on the diff's last `.response`-kind event.
    - Streaming (`streamGenerating`) reuses the same `generate` chokepoint, so the diff runs once after the chunk loop completes — no per-chunk events.
    - Throw path: `recordTranscriptDelta` runs with `since: started` on both success and throw; the synthetic bodyless `.response` close is only appended when the diff did *not* already include a `.response`-kind entry, so a turn that fails after the SDK already durably appended a real `.response` (e.g. a post-generation validation/guardrail failure) never gets two `.response` events for one turn.

    Test migration: TranscriptNestingTests.swift gained an instructed-session test proving `.instructions` now appears in the entry-derived sequence (previously silently dropped by the hand-built bracket). SessionChokepointTests.swift's three chokepoint tests gained entry/text/ms assertions. New file TranscriptFidelityTests.swift covers: two-turn entry order+payload, fork-baseline delta (child file has only its own turns, no parent/child leakage either direction), streaming-matches-non-streaming, the shrink-clamp (a `VariableTranscriptBackend` whose `entries` a test drives directly, forcing a mid-session shrink — proven not to crash and to recover cleanly next turn), and the duplicate-`.response` regression the adversarial review caught (see below). Gated integration test added to LanguageModelSessionBackendTests.swift comparing recorded entry kinds to the real `session.transcript` kinds one-for-one against a live tiny model (built by constructing `RoutedSessionActor`/`LanguageModelProfile` directly via their internal/public initializers, bypassing `Router.resolve()`) — could not be executed in this environment (no network/GPU) but builds clean; gated behind `FM_ROUTER_INTEGRATION_TESTS`.

    Adversarial double-check (via really-done) caught one real bug on the first pass: the throwing path unconditionally appended a synthetic bodyless `.response` close even when the SDK's own diff already contained a real `.response` entry, producing two `.response` events for one failed turn. Fixed by having `recordTranscriptDelta` return whether it persisted a `.response`-kind entry this call, and only synthesizing the close when it didn't. Added a regression test (`throwingTurnWithRealResponseEntryRecordsExactlyOneResponseEvent`) that fails against the pre-fix code (would see 4 events instead of 3). Also fixed a stale class-level doc comment describing the old "exactly one open and one close" bracket invariant. Re-verified: build + build-tests + full test suite green (253/253 unit tests, gated suites correctly skipped).

    swift build: exit 0. swift build --build-tests: exit 0. swift test: 253 tests in 28 suites passed + 9 gated tests skipped (0 failures).
  timestamp: 2026-07-10T08:11:16.257925+00:00
- actor: claude-code
  id: 01kx5pqnxv2gtkykeq5tjxpka8
  text: |-
    Addressed all 13 review findings from the 2026-07-10 03:13 review pass and checked them off in the task description.

    1-2. Production-code duplication (RoutedLLM.swift:97 / RoutedSession.swift:441): extracted a shared top-level `makeRoutedSessionActor(profile:routerId:id:parentId:recordingDirectory:workingDirectory:backend:slot:model:recorder:instructions:grammar:serialGate:forkAdmissionGate:holdsAdmissionPermit:persistedEntryCount:)` factory in RoutedSession.swift (just above `actor RoutedSessionActor`), with a doc comment explaining why the constructor call is centralized. Both `makeSession(grammar:instructions:workingDirectory:)` (RoutedLLM.swift) and `fork(workingDirectory:)` (RoutedSession.swift) now call it instead of constructing `RoutedSessionActor(...)` directly.

    3-4. `@unchecked Sendable` docs (TranscriptFidelityTests.swift): added doc comments to `VariableTranscriptBackend` and `VariableLLMContainer` explaining the synchronization invariant (single-threaded `@MainActor` test access; any chokepoint read serializes through the session's serial gate), matching the established pattern from GuidedGenerationTests.swift's `MaxTokensRecordingBackend`.

    5-13. Nine streaming-path / guided-path coverage gaps, all fixed:
    - LanguageModelSessionBackendTests.swift (gated integration): extracted a `ChokepointHarness` + `makeChokepointHarness()`/`recordedEvents(from:)` helper pair, added `recordedEntryKindsMatchSessionTranscriptKindsStreaming()`.
    - SessionChokepointTests.swift: `streamResponseEmitsOpenAndClose()` now asserts provenance (routerId/sessionId/slot/model), promptEvent.entry/text/ms==nil, responseEvent.ms!=nil, and token nils — full parity with `respondEmitsOpenAndClose()`. Added `streamCloseEventEmittedOnThrow()` mirroring `closeEventEmittedOnThrow()`.
    - TranscriptFidelityTests.swift: added `guidedSessionStampsGrammarOnEntryEvents()` (new — exercises the guided/grammar path via `makeGuidedSession`, was previously untested in this file), `streamingShrinkingTranscriptClampsWithoutCrashing()`, `streamingThrowWithRealResponseEntryRecordsExactlyOneResponseEvent()`.
    - TranscriptNestingTests.swift: added `streamResponseFirstLineIsSessionMetaThenTurn()` and `streamResponseInstructedSessionRecordsLeadingInstructionsEntry()`.

    Verification: `swift build --build-tests` exit 0. `swift test`: 259 passed / 0 failed across 28 suites (up from 253 before this pass — 6 new unit tests), gated integration suite shows 10 skipped (up from 9 — 1 new gated test), correctly skipped since `FM_ROUTER_INTEGRATION_TESTS` is unset in this sandbox.

    Ran the `double-check` adversarial agent independently: it reran the build and full test suite itself (reproduced 259/0/10 exactly), verified the duplication extraction is parameter-for-parameter correct at both call sites, confirmed both Sendable doc comments, and confirmed all 9 new/extended tests assert meaningful, non-tautological invariants matching their non-streaming siblings. Verdict: PASS.

    Leaving task in `doing` for `/review` to pick back up.
  timestamp: 2026-07-10T09:46:26.619126+00:00
depends_on:
- 01KX0ZYTYAV7YM94ZXN39SD1XH
- 01KX0ZZQF8ZHY6R2867RQ92KSK
- 01KX101CBDCFV4EQSPQT0BNK8R
position_column: doing
position_ordinal: '80'
title: 'Snapshot-diff persistence: chokepoint records real Transcript entries'
---
## What\n\nReplace the synthesized prompt/response string events with persistence of the SDK's real transcript deltas — the core of the plan.md \"Transcript fidelity\" design. Depends on the gating task so `GatingRecorder` already strips/redacts structured payloads before the chokepoint starts emitting them (no content-leak window on main). Token metering is deliberately NOT in scope here — it is its own follow-up task. In `RoutedSessionActor` (Sources/FoundationModelsRouter/Session/RoutedSession.swift):\n\n- Add actor state `private var persistedEntryCount: Int`, initialized via a new init parameter: `0` for root sessions; for forks, the parent's `backend.transcriptEntries().count` captured in `fork(workingDirectory:)` inside the serial-gate window it already holds (so inherited history is never re-persisted).\n- In `generate(prompt:grammar:_:)`, after `body()` — on the success **and** throw paths — read `backend.transcriptEntries()` and take the new suffix **defensively**: `entries[min(persistedEntryCount, entries.count)...]`, never a bare `entries[persistedEntryCount...]`. Do not assume the SDK transcript is strictly append-only: the SDK ships a `TranscriptErrorHandlingPolicy` that could in principle condense or rewrite a transcript (the router does not opt into it today, but nothing guarantees that forever), and an out-of-bounds range here would trap *inside the recording path* — crashing generation and violating the recording-is-best-effort/never-fails-generation contract. A one-`min()` clamp is chosen over a doc-comment-only append-only assumption precisely because the failure mode of the assumption is a process crash in a path contractually forbidden from failing. If a shrink is detected (`entries.count < persistedEntryCount`), log a warning, record nothing for that turn's diff, and reset `persistedEntryCount = entries.count` so subsequent turns diff from reality. Map each new entry through `TranscriptEntryMapper.event(from:)`, append one event per new entry (envelope: routerId/sessionId/parentId/slot/model/grammar as today; `entry` payload + flattened `text` from the mapper; `tokensIn`/`tokensOut` stay `nil` for now), then advance `persistedEntryCount`. This happens inside the existing serial gate, where the bracket already runs.\n- Delete the hand-built `.prompt`-open and `.response`-close content events (`makePartialEvent(kind: .prompt, text: prompt)` / `kind: .response, text: response`). Keep: the lazy `session` meta event; the `ms` measurement (stamp it on the turn's final `.response`-kind entry event); and on the throw path a bodyless `response`-kind event with `ms` and no `entry` so every failed turn still leaves a trace alongside whatever entries the SDK durably appended.\n- Streaming (`streamGenerating`): same snapshot-diff after the chunk loop completes — no per-chunk events.\n- Rewrite affected expectations in Tests/FoundationModelsRouterTests/TranscriptNestingTests.swift (e.g. `[.session, .prompt, .response]` becomes the entry-derived sequence, including `.instructions` when the stub models an instructed session), SessionChokepointTests.swift, MultiTurnSessionTests.swift, and RecorderTests.swift/MergedAndRedactionTests.swift where their chokepoint-driven expectations shift. This test migration is an unavoidable companion of the behavior change and is acknowledged as the bulk of this task's size; the production diff itself is one file.\n\n## Acceptance Criteria\n- [ ] Recorded per-turn content events are derived exclusively from `backend.transcriptEntries()` deltas, not from prompt/response strings\n- [ ] A fork's transcript.jsonl contains only entries appended after the fork (baseline = parent count at fork time)\n- [ ] The snapshot-diff read is clamped: a transcript shorter than `persistedEntryCount` logs a warning, records no entry events for that turn, and resets the counter — it never traps\n- [ ] A throwing turn records whatever entries the SDK kept plus one bodyless `response` close with `ms`\n- [ ] Every entry event carries the `entry` payload and flattened `text` (pre-gating); `tokensIn`/`tokensOut` remain `nil`\n- [ ] `swift build` and `swift test` exit 0\n\n## Tests\n- [ ] Unit (stub-backed): two turns produce entry events in exact stub-transcript order with correct kinds and payloads\n- [ ] Unit: fork after turn 1, then a child turn — child's file has only the child's delta; parent's later turns never leak into the child file (and vice versa)\n- [ ] Unit: streaming turn records the same entry events as a non-streaming turn\n- [ ] Unit: throwing backend records SDK-retained entries + bodyless close\n- [ ] Unit: a stub whose transcript *shrinks* below `persistedEntryCount` between turns does not crash — the turn records no entry events, and a subsequent turn diffs correctly from the new (smaller) baseline\n- [ ] Gated integration (`FM_ROUTER_INTEGRATION_TESTS`): after one live turn, recorded entry kinds match `session.transcript` kinds one-for-one\n\n## Workflow\n- Use `/tdd` — write failing tests first, then implement to make them pass.\n\n## Review Findings (2026-07-10 03:13)\n\nScope: HEAD~1..HEAD (commit 889ab6a). Of the engine's 37 raw findings, 22 pure test-stub/fixture deduplication findings and 1 finding about an unrelated pre-existing test (KV cache reuse, untouched by this diff) were waived per the project's blanket test-refactor exception / out-of-scope filtering. The 13 findings below are genuine and in scope.\n\n- [x] `Sources/FoundationModelsRouter/RoutedLLM.swift:97` — The RoutedSessionActor initialization is near-verbatim duplicated across makeSession (here) and fork in RoutedSession.swift, differing only in parameter values. This creates maintenance burden: any change to the constructor signature or common initialization logic must be applied in two places, risking drift. Extract a private helper function `makeRoutedSessionActor(profile:routerId:id:parentId:recordingDirectory:workingDirectory:backend:slot:model:recorder:instructions:grammar:serialGate:forkAdmissionGate:holdsAdmissionPermit:persistedEntryCount:)` and call it from both sites, eliminating the duplicated constructor call and its 18-parameter argument list.\n- [x] `Sources/FoundationModelsRouter/Session/RoutedSession.swift:441` — The RoutedSessionActor initialization is near-verbatim duplicated here (in fork()) and in makeSession (RoutedLLM.swift:97), differing only in parameter values. Same maintenance risk: changes to constructor invocation must be applied in two places. Extract the duplicated constructor call into a shared helper function (see companion finding in RoutedLLM.swift) and call it from fork().\n- [x] `Tests/FoundationModelsRouterIntegrationTests/LanguageModelSessionBackendTests.swift:155` — The regression test recordedEntryKindsMatchSessionTranscriptKinds() only verifies kind matching for the non-streaming respond() path, not for streamResponse(). Both paths use the same snapshot-diff persistence through generate(), so the invariant 'recorded kinds exactly match live transcript kinds' should be tested for streaming to ensure the fix is complete across both generation paths. Add a parallel test that duplicates recordedEntryKindsMatchSessionTranscriptKinds() but calls streamResponse() instead of respond(), accumulating chunks and verifying the final recorded kinds still match the backend.session.transcript kinds one-for-one.\n- [x] `Tests/FoundationModelsRouterTests/SessionChokepointTests.swift:121` — The invariant 'ms lands only on the final .response-kind entry, not on .prompt' is verified in respondEmitsOpenAndClose() with assertions for both promptEvent.ms == nil and responseEvent.ms != nil, but streamResponseEmitsOpenAndClose() only checks responseEvent.ms != nil. Both paths share the generate() chokepoint and should verify the same invariants consistently. In streamResponseEmitsOpenAndClose(), extract promptEvent and add assertion: #expect(promptEvent.ms == nil) to match the completeness of the non-streaming test.\n- [x] `Tests/FoundationModelsRouterTests/SessionChokepointTests.swift:121` — In addition to the missing promptEvent.ms check already reported, streamResponseEmitsOpenAndClose() lacks complete verification of invariants that respondEmitsOpenAndClose() checks: provenance consistency (routerId, sessionId, slot, model across all events), promptEvent.entry presence, and token metering (should be nil). Both paths share generate() so should verify identical invariants completely. Add to streamResponseEmitsOpenAndClose(): all the provenance checks (routerId, sessionId, slot, model), promptEvent extraction and entry validation, and token validation (#expect(events.allSatisfy { $0.tokensIn == nil && $0.tokensOut == nil })) to achieve parity with respondEmitsOpenAndClose().\n- [x] `Tests/FoundationModelsRouterTests/SessionChokepointTests.swift:147` — The invariant 'exactly one close event (.response) on throw' is tested for non-streaming respond() at closeEventEmittedOnThrow() but not for streamResponse(). Both paths share the generate() chokepoint with identical throw-path handling, so both should be tested to prove the invariant holds everywhere. Add a test parallel to closeEventEmittedOnThrow() that calls streamResponse() on a backend configured to throw after emitting a .response entry, verifying the stream error path still records exactly one response event without duplication.\n- [x] `Tests/FoundationModelsRouterTests/TranscriptFidelityTests.swift:41` — @unchecked Sendable requires a documented synchronization invariant explaining why mutable shared state is safe. VariableTranscriptBackend has mutable properties (entries, responseText, shouldThrow) and crosses task/actor boundaries without documenting its invariant — e.g., \"safe because only test-controlled single-threaded access\" or \"protected by serial gate\". Without the comment, the Sendable claim appears unsupported and future readers may incorrectly assume thread-safety. Add a doc comment line before the class declaration explaining the synchronization invariant — e.g., \"@unchecked Sendable is safe here because tests are single-threaded and all accesses serialize through the session's serial gate.\".\n- [x] `Tests/FoundationModelsRouterTests/TranscriptFidelityTests.swift:118` — @unchecked Sendable requires a documented synchronization invariant explaining why this type is safe across task boundaries. VariableLLMContainer holds a mutable VariableTranscriptBackend and claims Sendable without documenting its invariant. Without explanation, the Sendable claim is unsupported. Add an explanation in the doc comment: e.g., \"@unchecked Sendable is safe here because the container is only used in single-threaded tests, and access to its backend serializes through the session's serial gate.\".\n- [x] `Tests/FoundationModelsRouterTests/TranscriptFidelityTests.swift:140` — The invariant 'grammar from the session is correctly stamped on each event's grammar field' is implemented in generate() and makePartialEvent() but never tested. All tests use non-guided sessions with grammar=nil, leaving the guided path (grammar != nil) unverified. The code path is identical for both, so both should be exercised to prove the invariant holds. Add a test that creates a guided session with a grammar constraint (or manually passes grammar to generate()), exercises generation, and verifies the recorded events carry the correct grammar.source value matching the session's grammar.\n- [x] `Tests/FoundationModelsRouterTests/TranscriptFidelityTests.swift:260` — The shrink-clamp defensive clamping test shrinkingTranscriptClampsWithoutCrashing() only exercises the non-streaming respond() path. The shrink-clamp logic in recordTranscriptDelta() applies equally to streaming; both paths should prove the same invariant (shrinkage doesn't crash, baseline resets, next turn recovers). Add a parallel test (or extend existing test) that drives the same transcript shrinkage scenario through streamResponse(), verifying the stream path also handles shrinkage defensively without crashing.\n- [x] `Tests/FoundationModelsRouterTests/TranscriptFidelityTests.swift:315` — The regression test throwingTurnWithRealResponseEntryRecordsExactlyOneResponseEvent() verifies the fix (exactly one .response event when SDK appended one before failing) only for non-streaming respond(). The streaming path uses the same generate() chokepoint with identical throw-path logic, so the same regression scenario should be tested for streamResponse() to prove the invariant holds for both paths. Add a test streamingThrowWithRealResponseEntryRecordsExactlyOneResponseEvent() that mirrors the non-streaming regression test but uses streamResponse() and verifies the stream error path records exactly one .response event without duplication.\n- [x] `Tests/FoundationModelsRouterTests/TranscriptNestingTests.swift:210` — The test firstLineIsSessionMetaThenTurn() verifies that session meta precedes the first turn's prompt/response for non-streaming only. The session meta event emission in recordSessionMetaIfNeeded() applies to both streaming and non-streaming paths; the invariant should be verified for streamResponse() to prove it holds for all generation entry points. Add a parallel assertion to firstLineIsSessionMetaThenTurn() (or a new test) that calls streamResponse() on a new session and verifies its transcript also opens with [.session, .prompt, .response] sequence.\n- [x] `Tests/FoundationModelsRouterTests/TranscriptNestingTests.swift:260` — The test instructedSessionRecordsLeadingInstructionsEntry() verifies that a session with instructions records a leading .instructions entry before .prompt/.response for non-streaming only. The snapshot-diff persistence and entry mapping in recordTranscriptDelta() apply equally to streaming; the invariant (SDK-generated .instructions entries are persisted in order) should be tested for streamResponse(). Add a parallel test (or extend existing test) that creates a session with instructions and calls streamResponse() instead of respond(), verifying the recorded event kinds are [.session, .instructions, .prompt, .response] for streaming too.