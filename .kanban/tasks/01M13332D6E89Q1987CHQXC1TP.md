---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m19ddrta6zvq0e2j3jj5fcc4
  text: |-
    ### finish — blocked, not started

    This card is held for a human decision. No file was changed.

    **The conflict.** The card demotes `SessionMailbox` to `internal`. `ToolMounting.makeWrapped` takes `mailbox: SessionMailbox`, so that demotion makes a public `makeWrapped` impossible.

    A downstream package, FoundationModelsMultitool, reports its `main` is red because `makeWrapped` is already internal:

        Sources/FoundationModelsMultitool/Invocation/RunBinding.swift:148:27:
          error: cannot find 'ToolMounting' in scope
        Sources/FoundationModelsMultitool/Invocation/RunBinding.swift:184:29:
          error: cannot find type 'OperationEventSink' in scope

    Measured in this tree:

    | symbol | at 760ae89 | at HEAD |
    |---|---|---|
    | `ToolMounting` | `public enum` | `enum` |
    | `makeWrapped`, both overloads | `public static func` | `static func` |
    | `SessionMailbox` | — | `public actor`, still public |

    So `makeWrapped` was demoted by an earlier card, and this card would remove the last way to restore it.

    **What the downstream package asks for.** One public entry to mount a tool on a session from outside this package, because `makeWrapped` gives seven behaviours together that cannot be rebuilt from public parts: mount arbitration where the tool's own `BackgroundTool.mount` beats the site configuration; decorator dispatch by output type; a completion token minted into the session mailbox; the `ToolContext.$current.withValue` binding; the event funnel; the invocation journal records; and cancellation with the mount clock. Either make `makeWrapped` public again and document it as the supported cross-package mount entry point, or publish a narrower named facade with the same seven behaviours.

    **The decision needed.** Does this card's demotion still stand, or does the package keep a public mount entry point? The card was written before this came up.
  timestamp: 2026-08-30T13:23:29.738636+00:00
depends_on:
- 01M1332A3W2HWNRZ24R8SDFY7A
- 01M1332MRYMJN0X1QM3DFDH8B4
- 01M1350P6MCZ1MWZQEDHHTC4V7
- 01M12MMJY0BHS8ABZGQKBNBP4A
- 01M19DN37FATFESBM12ZGZYHSJ
position_column: todo
position_ordinal: 8a80
title: Hoist the queue and elicitation types and demote the two actors
---
## What

`SessionOutbox` and `SessionMailbox` are public actors only because the `RoutedSession` protocol names five of their nested types (Sources/FoundationModelsRouter/Session/RoutedSession.swift:274, 278, 285, 292, 296, 308, 314). Hoist the five types to the top level, then demote the two actors.

- Move to top-level public types, with new names that stand alone: `SessionOutbox.ItemID` (suggest `PromptID`), `SessionOutbox.PromptQueueMutationResult`, `SessionOutbox.QueueDepth` (suggest `PromptQueueDepth`), `SessionMailbox.ElicitationAnswerDelivery`, and `SessionMailbox.ElicitationCompletionDelivery`.
- Update the `RoutedSession` protocol, `RoutedSessionActor`, and every other reference.
- Demote the `SessionOutbox` actor (Sources/FoundationModelsRouter/Session/SessionOutbox.swift:24) and the `SessionMailbox` actor (Sources/FoundationModelsRouter/Hosting/SessionMailbox.swift:15) to `internal`.
- Do not keep public deprecated aliases; this package is pre-1.0 and the break is accepted.
- Update `RoutedSession.md` where it names the old nested paths.

This is a mechanical rename across many files, so it runs last among the tasks that touch these files:
- the two demotion tasks, so the access levels are settled first;
- the DocC repair task, because both edit `RoutedSession.md`;
- the turn-span task, because it adds doc comments to the same `RoutedSession` protocol declarations this task rewrites.

## Acceptance Criteria
- [ ] The five types are top-level and public.
- [ ] `SessionOutbox` and `SessionMailbox` are `internal`.
- [ ] The `RoutedSession` protocol compiles with the new type names.
- [ ] The turn-span doc comments added earlier survive the rename.

## Tests
- [ ] Update existing tests for the new names; behavior assertions stay the same.
- [ ] Run `swift build` and `swift test` at the root. All pass.
- [ ] Run `swift build --package-path IntegrationTests`. It builds.

## Workflow
- Use `/tdd` — write failing tests first, then implement to make them pass. #router #api #cleanup