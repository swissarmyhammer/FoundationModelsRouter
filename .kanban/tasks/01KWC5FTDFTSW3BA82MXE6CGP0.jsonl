{"id":"01KWC5FTDF5P27E9J2A5CG48DK","timestamp":"2026-06-30T11:44:02.479736Z","op":"create","item_id":"01KWC5FTDFTSW3BA82MXE6CGP0","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,37 @@\n+---\n+depends_on:\n+- 01KWC5F41MNA2PA3K45Z86CRQ0\n+- 01KWC5ECCZYEAH49J635KC9QH5\n+position_column: todo\n+position_ordinal: 8a80\n+title: 'Residency lifecycle + Access API: sessions & embed (milestone 5)'\n+---\n+## What\n+Make a resolved profile usable and give it a residency lifetime. Plan \"Residency\", \"Access API\", \"Sessions & KV cache\" (basic), \"Sessions: working directory & isolation\", and the recording chokepoint scaffold (plan \"Transcripts & recording\"). Forking and the concurrency gates are milestone 9; full recording/nesting is milestone 10.\n+\n+- `Sources/FoundationModelsRouter/LanguageModelProfile.swift` (lifecycle):\n+  - `func release()` evicts all three models; `deinit` also runs it. Live sessions **retain their profile**, so eviction only happens once the handle and all sessions/forks are gone.\n+  - Enforce **one active profile at a time** on the `Router`: resolving while another profile is resident fails rather than over-committing RAM (release first).\n+- `Sources/FoundationModelsRouter/RoutedLLM.swift`:\n+  - `func makeSession(instructions: String? = nil, workingDirectory: URL? = nil) -> RoutedSession`.\n+- `Sources/FoundationModelsRouter/RoutedEmbedder.swift`:\n+  - `let dimension: Int`; `func embed(_ texts: [String]) async throws -> [[Float]]` over the resident `MLXEmbedders` model.\n+- `Sources/FoundationModelsRouter/Session/RoutedSession.swift`:\n+  - `protocol RoutedSession: Actor` with `profile` (retained), `routerID: ULID`, `id: ULID`, `parentID: ULID?`, `recordingDirectory: URL`, `workingDirectory: URL` (defaults to recordingDirectory), `respond(to:)`, `streamResponse(to:)` (unconstrained text only). `fork(workingDirectory:)` declared but its real KV-copy behavior is milestone 9.\n+  - Concrete actor backed by `MLXLMCommon` `ChatSession` / `ModelContainer`. **No public initializer** — only vended by `RoutedLLM.makeSession`; the recorder + `routerID` flow down from the Router.\n+  - **Single bracketed `generate` chokepoint:** every public method (`respond`, `streamResponse`) funnels through one private `generate` that runs the model inside a recorder bracket (open event → body → close/error in `defer`). Recorder is a non-optional `let` (default `.none`); milestone 10 enriches the events + nesting.\n+  - The raw `ChatSession` is never vended.\n+\n+## Acceptance Criteria\n+- [ ] `makeSession().respond(to:)` returns text (covered for real in the gated integration suite); with a stub model the chokepoint runs and emits exactly one open + one close event to an `InMemoryRecorder`, and a close event even when the body throws.\n+- [ ] A session retains its profile: dropping the profile handle while a session is alive does NOT evict; eviction happens after the last session is released (assert via an eviction spy/counter).\n+- [ ] `release()` evicts all three; calling `resolve` a second time while resident throws (one-active-profile), and succeeds after `release()`.\n+- [ ] `workingDirectory` defaults to `recordingDirectory` and is overridable via `makeSession(workingDirectory:)` without moving the recording directory.\n+- [ ] There is no public `RoutedSession` initializer (compile-time: construction only via `makeSession`).\n+\n+## Tests\n+- [ ] `Tests/FoundationModelsRouterTests/SessionLifecycleTests.swift` (Swift Testing) with a stub `ModelContainer`/loader + `InMemoryRecorder`: chokepoint emits open/close (incl. on throw); profile-retention via eviction spy; one-active-profile enforcement; workingDirectory default + override.\n+- [ ] Run `swift test --filter SessionLifecycleTests` — all pass.\n+\n+## Workflow\n+- Use `/tdd` — write failing chokepoint + retention + one-active-profile tests with stubs first.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,37 +0,0 @@\n----\n-depends_on:\n-- 01KWC5F41MNA2PA3K45Z86CRQ0\n-- 01KWC5ECCZYEAH49J635KC9QH5\n-position_column: todo\n-position_ordinal: 8a80\n-title: 'Residency lifecycle + Access API: sessions & embed (milestone 5)'\n----\n-## What\n-Make a resolved profile usable and give it a residency lifetime. Plan \"Residency\", \"Access API\", \"Sessions & KV cache\" (basic), \"Sessions: working directory & isolation\", and the recording chokepoint scaffold (plan \"Transcripts & recording\"). Forking and the concurrency gates are milestone 9; full recording/nesting is milestone 10.\n-\n-- `Sources/FoundationModelsRouter/LanguageModelProfile.swift` (lifecycle):\n-  - `func release()` evicts all three models; `deinit` also runs it. Live sessions **retain their profile**, so eviction only happens once the handle and all sessions/forks are gone.\n-  - Enforce **one active profile at a time** on the `Router`: resolving while another profile is resident fails rather than over-committing RAM (release first).\n-- `Sources/FoundationModelsRouter/RoutedLLM.swift`:\n-  - `func makeSession(instructions: String? = nil, workingDirectory: URL? = nil) -> RoutedSession`.\n-- `Sources/FoundationModelsRouter/RoutedEmbedder.swift`:\n-  - `let dimension: Int`; `func embed(_ texts: [String]) async throws -> [[Float]]` over the resident `MLXEmbedders` model.\n-- `Sources/FoundationModelsRouter/Session/RoutedSession.swift`:\n-  - `protocol RoutedSession: Actor` with `profile` (retained), `routerID: ULID`, `id: ULID`, `parentID: ULID?`, `recordingDirectory: URL`, `workingDirectory: URL` (defaults to recordingDirectory), `respond(to:)`, `streamResponse(to:)` (unconstrained text only). `fork(workingDirectory:)` declared but its real KV-copy behavior is milestone 9.\n-  - Concrete actor backed by `MLXLMCommon` `ChatSession` / `ModelContainer`. **No public initializer** — only vended by `RoutedLLM.makeSession`; the recorder + `routerID` flow down from the Router.\n-  - **Single bracketed `generate` chokepoint:** every public method (`respond`, `streamResponse`) funnels through one private `generate` that runs the model inside a recorder bracket (open event → body → close/error in `defer`). Recorder is a non-optional `let` (default `.none`); milestone 10 enriches the events + nesting.\n-  - The raw `ChatSession` is never vended.\n-\n-## Acceptance Criteria\n-- [ ] `makeSession().respond(to:)` returns text (covered for real in the gated integration suite); with a stub model the chokepoint runs and emits exactly one open + one close event to an `InMemoryRecorder`, and a close event even when the body throws.\n-- [ ] A session retains its profile: dropping the profile handle while a session is alive does NOT evict; eviction happens after the last session is released (assert via an eviction spy/counter).\n-- [ ] `release()` evicts all three; calling `resolve` a second time while resident throws (one-active-profile), and succeeds after `release()`.\n-- [ ] `workingDirectory` defaults to `recordingDirectory` and is overridable via `makeSession(workingDirectory:)` without moving the recording directory.\n-- [ ] There is no public `RoutedSession` initializer (compile-time: construction only via `makeSession`).\n-\n-## Tests\n-- [ ] `Tests/FoundationModelsRouterTests/SessionLifecycleTests.swift` (Swift Testing) with a stub `ModelContainer`/loader + `InMemoryRecorder`: chokepoint emits open/close (incl. on throw); profile-retention via eviction spy; one-active-profile enforcement; workingDirectory default + override.\n-- [ ] Run `swift test --filter SessionLifecycleTests` — all pass.\n-\n-## Workflow\n-- Use `/tdd` — write failing chokepoint + retention + one-active-profile tests with stubs first.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KWC5XWAJRQSFD0SNGSWFAN5X","timestamp":"2026-06-30T11:51:43.186072Z","op":"update","item_id":"01KWC5FTDFTSW3BA82MXE6CGP0","forward_patch":"--- original\n+++ modified\n@@ -4,34 +4,27 @@\n - 01KWC5ECCZYEAH49J635KC9QH5\n position_column: todo\n position_ordinal: 8a80\n-title: 'Residency lifecycle + Access API: sessions & embed (milestone 5)'\n+title: Profile residency lifecycle + recorded embedding access (milestone 5a)\n ---\n ## What\n-Make a resolved profile usable and give it a residency lifetime. Plan \"Residency\", \"Access API\", \"Sessions & KV cache\" (basic), \"Sessions: working directory & isolation\", and the recording chokepoint scaffold (plan \"Transcripts & recording\"). Forking and the concurrency gates are milestone 9; full recording/nesting is milestone 10.\n+Give a resolved profile a residency lifetime and a recorded embedding surface. Plan \"Residency\", \"Access API\" (embed), \"Transcripts & recording\" (`embedding` event kind). The session/generation surface is split into milestone 5b; forking is milestone 9; full nesting/manifest is milestone 10.\n\n - `Sources/FoundationModelsRouter/LanguageModelProfile.swift` (lifecycle):\n-  - `func release()` evicts all three models; `deinit` also runs it. Live sessions **retain their profile**, so eviction only happens once the handle and all sessions/forks are gone.\n+  - `func release()` evicts all three models; `deinit` also runs it.\n   - Enforce **one active profile at a time** on the `Router`: resolving while another profile is resident fails rather than over-committing RAM (release first).\n-- `Sources/FoundationModelsRouter/RoutedLLM.swift`:\n-  - `func makeSession(instructions: String? = nil, workingDirectory: URL? = nil) -> RoutedSession`.\n - `Sources/FoundationModelsRouter/RoutedEmbedder.swift`:\n   - `let dimension: Int`; `func embed(_ texts: [String]) async throws -> [[Float]]` over the resident `MLXEmbedders` model.\n-- `Sources/FoundationModelsRouter/Session/RoutedSession.swift`:\n-  - `protocol RoutedSession: Actor` with `profile` (retained), `routerID: ULID`, `id: ULID`, `parentID: ULID?`, `recordingDirectory: URL`, `workingDirectory: URL` (defaults to recordingDirectory), `respond(to:)`, `streamResponse(to:)` (unconstrained text only). `fork(workingDirectory:)` declared but its real KV-copy behavior is milestone 9.\n-  - Concrete actor backed by `MLXLMCommon` `ChatSession` / `ModelContainer`. **No public initializer** — only vended by `RoutedLLM.makeSession`; the recorder + `routerID` flow down from the Router.\n-  - **Single bracketed `generate` chokepoint:** every public method (`respond`, `streamResponse`) funnels through one private `generate` that runs the model inside a recorder bracket (open event → body → close/error in `defer`). Recorder is a non-optional `let` (default `.none`); milestone 10 enriches the events + nesting.\n-  - The raw `ChatSession` is never vended.\n+  - **Recorded:** `RoutedEmbedder` carries `routerID: ULID` + a non-optional `TranscriptRecorder` (populated by the Router at resolve — see milestone 4b). `embed` emits one `embedding` `TranscriptEvent` (provenance `{routerId, slot: .embedding, model, seq, ts, tokensIn?, ms}`) into a directory under the router recordings root (e.g. `recordings/<routerID>/embeddings/transcript.jsonl`). Best-effort: a sink failure logs, never fails `embed`.\n\n ## Acceptance Criteria\n-- [ ] `makeSession().respond(to:)` returns text (covered for real in the gated integration suite); with a stub model the chokepoint runs and emits exactly one open + one close event to an `InMemoryRecorder`, and a close event even when the body throws.\n-- [ ] A session retains its profile: dropping the profile handle while a session is alive does NOT evict; eviction happens after the last session is released (assert via an eviction spy/counter).\n-- [ ] `release()` evicts all three; calling `resolve` a second time while resident throws (one-active-profile), and succeeds after `release()`.\n-- [ ] `workingDirectory` defaults to `recordingDirectory` and is overridable via `makeSession(workingDirectory:)` without moving the recording directory.\n-- [ ] There is no public `RoutedSession` initializer (compile-time: construction only via `makeSession`).\n+- [ ] `release()` evicts all three (assert via an eviction spy/counter); after release, residency is clear.\n+- [ ] Calling `resolve` a second time while a profile is resident throws (one-active-profile); it succeeds after `release()`.\n+- [ ] `embed` returns vectors of length `dimension` (real vectors asserted in the gated integration suite; unit test uses a stub embedder).\n+- [ ] `embed` emits exactly one `embedding` event with correct provenance to an `InMemoryRecorder`; a forced sink failure is swallowed and `embed` still returns.\n\n ## Tests\n-- [ ] `Tests/FoundationModelsRouterTests/SessionLifecycleTests.swift` (Swift Testing) with a stub `ModelContainer`/loader + `InMemoryRecorder`: chokepoint emits open/close (incl. on throw); profile-retention via eviction spy; one-active-profile enforcement; workingDirectory default + override.\n-- [ ] Run `swift test --filter SessionLifecycleTests` — all pass.\n+- [ ] `Tests/FoundationModelsRouterTests/ProfileLifecycleTests.swift` (Swift Testing) with a stub embedder/loader + `InMemoryRecorder`: evict-all on release; one-active-profile enforcement (second resolve throws, then succeeds after release); embed records one `embedding` event + swallowed sink error.\n+- [ ] Run `swift test --filter ProfileLifecycleTests` — all pass.\n\n ## Workflow\n-- Use `/tdd` — write failing chokepoint + retention + one-active-profile tests with stubs first.\n\\ No newline at end of file\n+- Use `/tdd` — write failing evict / one-active-profile / embed-recording tests with stubs first.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -4,27 +4,34 @@\n - 01KWC5ECCZYEAH49J635KC9QH5\n position_column: todo\n position_ordinal: 8a80\n-title: Profile residency lifecycle + recorded embedding access (milestone 5a)\n+title: 'Residency lifecycle + Access API: sessions & embed (milestone 5)'\n ---\n ## What\n-Give a resolved profile a residency lifetime and a recorded embedding surface. Plan \"Residency\", \"Access API\" (embed), \"Transcripts & recording\" (`embedding` event kind). The session/generation surface is split into milestone 5b; forking is milestone 9; full nesting/manifest is milestone 10.\n+Make a resolved profile usable and give it a residency lifetime. Plan \"Residency\", \"Access API\", \"Sessions & KV cache\" (basic), \"Sessions: working directory & isolation\", and the recording chokepoint scaffold (plan \"Transcripts & recording\"). Forking and the concurrency gates are milestone 9; full recording/nesting is milestone 10.\n\n - `Sources/FoundationModelsRouter/LanguageModelProfile.swift` (lifecycle):\n-  - `func release()` evicts all three models; `deinit` also runs it.\n+  - `func release()` evicts all three models; `deinit` also runs it. Live sessions **retain their profile**, so eviction only happens once the handle and all sessions/forks are gone.\n   - Enforce **one active profile at a time** on the `Router`: resolving while another profile is resident fails rather than over-committing RAM (release first).\n+- `Sources/FoundationModelsRouter/RoutedLLM.swift`:\n+  - `func makeSession(instructions: String? = nil, workingDirectory: URL? = nil) -> RoutedSession`.\n - `Sources/FoundationModelsRouter/RoutedEmbedder.swift`:\n   - `let dimension: Int`; `func embed(_ texts: [String]) async throws -> [[Float]]` over the resident `MLXEmbedders` model.\n-  - **Recorded:** `RoutedEmbedder` carries `routerID: ULID` + a non-optional `TranscriptRecorder` (populated by the Router at resolve — see milestone 4b). `embed` emits one `embedding` `TranscriptEvent` (provenance `{routerId, slot: .embedding, model, seq, ts, tokensIn?, ms}`) into a directory under the router recordings root (e.g. `recordings/<routerID>/embeddings/transcript.jsonl`). Best-effort: a sink failure logs, never fails `embed`.\n+- `Sources/FoundationModelsRouter/Session/RoutedSession.swift`:\n+  - `protocol RoutedSession: Actor` with `profile` (retained), `routerID: ULID`, `id: ULID`, `parentID: ULID?`, `recordingDirectory: URL`, `workingDirectory: URL` (defaults to recordingDirectory), `respond(to:)`, `streamResponse(to:)` (unconstrained text only). `fork(workingDirectory:)` declared but its real KV-copy behavior is milestone 9.\n+  - Concrete actor backed by `MLXLMCommon` `ChatSession` / `ModelContainer`. **No public initializer** — only vended by `RoutedLLM.makeSession`; the recorder + `routerID` flow down from the Router.\n+  - **Single bracketed `generate` chokepoint:** every public method (`respond`, `streamResponse`) funnels through one private `generate` that runs the model inside a recorder bracket (open event → body → close/error in `defer`). Recorder is a non-optional `let` (default `.none`); milestone 10 enriches the events + nesting.\n+  - The raw `ChatSession` is never vended.\n\n ## Acceptance Criteria\n-- [ ] `release()` evicts all three (assert via an eviction spy/counter); after release, residency is clear.\n-- [ ] Calling `resolve` a second time while a profile is resident throws (one-active-profile); it succeeds after `release()`.\n-- [ ] `embed` returns vectors of length `dimension` (real vectors asserted in the gated integration suite; unit test uses a stub embedder).\n-- [ ] `embed` emits exactly one `embedding` event with correct provenance to an `InMemoryRecorder`; a forced sink failure is swallowed and `embed` still returns.\n+- [ ] `makeSession().respond(to:)` returns text (covered for real in the gated integration suite); with a stub model the chokepoint runs and emits exactly one open + one close event to an `InMemoryRecorder`, and a close event even when the body throws.\n+- [ ] A session retains its profile: dropping the profile handle while a session is alive does NOT evict; eviction happens after the last session is released (assert via an eviction spy/counter).\n+- [ ] `release()` evicts all three; calling `resolve` a second time while resident throws (one-active-profile), and succeeds after `release()`.\n+- [ ] `workingDirectory` defaults to `recordingDirectory` and is overridable via `makeSession(workingDirectory:)` without moving the recording directory.\n+- [ ] There is no public `RoutedSession` initializer (compile-time: construction only via `makeSession`).\n\n ## Tests\n-- [ ] `Tests/FoundationModelsRouterTests/ProfileLifecycleTests.swift` (Swift Testing) with a stub embedder/loader + `InMemoryRecorder`: evict-all on release; one-active-profile enforcement (second resolve throws, then succeeds after release); embed records one `embedding` event + swallowed sink error.\n-- [ ] Run `swift test --filter ProfileLifecycleTests` — all pass.\n+- [ ] `Tests/FoundationModelsRouterTests/SessionLifecycleTests.swift` (Swift Testing) with a stub `ModelContainer`/loader + `InMemoryRecorder`: chokepoint emits open/close (incl. on throw); profile-retention via eviction spy; one-active-profile enforcement; workingDirectory default + override.\n+- [ ] Run `swift test --filter SessionLifecycleTests` — all pass.\n\n ## Workflow\n-- Use `/tdd` — write failing evict / one-active-profile / embed-recording tests with stubs first.\n\\ No newline at end of file\n+- Use `/tdd` — write failing chokepoint + retention + one-active-profile tests with stubs first.\n\\ No newline at end of file\n","transaction_id":null}
