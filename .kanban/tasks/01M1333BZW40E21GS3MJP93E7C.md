---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m16y6brxmq744yak1eqgted8
  text: |
    Research done. Findings:

    - `respond(to:matching:maxTokens:)` sits in `extension RoutedModel where Container == any LoadedLLMContainer` in Sources/FoundationModelsRouter/Guided/GuidedGeneration.swift. Its sibling `makeGuidedSession(...)` in the same file is already `public`; the raw `respond(to:following:maxTokens:)` and the typed `respond(to:generating:maxTokens:)` stay internal, and the card does not ask for them.
    - `GuidedRequestError` is internal today. Every protocol the test needs is already public: `LoadedLLMContainer`, `LanguageModelSessionBackend`, `ModelLoader`, `MachineProbe`, `MetadataSource`, `TranscriptRecorder`.
    - Precedent for a plain-import test: Tests/FoundationModelsRouterTests/RoutedSessionPublicSurfaceTests.swift. It imports the module with no `@testable` and still reads the test target's own helpers, because `@testable` is per file.
    - Reusable helpers, so the new file copies no scaffolding: `RouterTestFixtures.makeRouter(cacheDir:loader:)`, `RouterTestFixtures.profile()`, `RouterTestFixtures.makeTempDir(prefix:)`, `StubModelLoader(container:dimension:)`, `PlainTranscriptStubContainer`, `StubSessionBackend(responseText:)`. `StubSessionBackend`'s guided entry point runs the real (GPU-free) xgrammar validation, so an over-spec schema raises the real error.
    - The DocC catalog is excluded from the build target in Package.swift, so a new .md page adds no build input.
  timestamp: 2026-08-29T14:18:49.501178+00:00
- actor: claude-code
  id: 01m16yppxw398jjq7hewzdhm7z
  text: |
    TDD record, and two notes for the next agent.

    RED first. The new test file was written before any source change. `swift build --build-tests` then reported exactly the two defects the card names: `'respond' is inaccessible due to 'internal' protection level` at the two call sites, and `cannot find type 'GuidedRequestError' in scope`. GREEN followed: `public` on the enum at GuidedGeneration.swift line 7 and on `respond(to:matching:maxTokens:)` at line 209. Nothing else in that file changed, and the doc comments stayed as they were, as the card orders.

    What did NOT work, so the next agent does not repeat it:

    1. A container declared inside the plain-import test file cannot conform to `LoadedLLMContainer`. The `extension LoadedLLMContainer` at ModelLoader.swift line 53 carries no access modifier, thus its three defaults — `languageModel`, `makeSession(instructions:tools:)` and `makeSession(transcript:tools:)` — are internal and never reach outside the module. This is a real public-surface defect, but it is outside this card. Recorded as new task ^9j7pyd5. The test now uses `ConfiguredLLMContainer` (Helpers/AutoCompactionFixtures.swift), which the test target already conformed, so no new stub was written and no scaffolding was copied.

    2. Returning `profile.standard` alone from the fixture traps: `makeSession requires a live owning LanguageModelProfile; the handle holds it weakly and the profile was released before this call` (RoutedLLM.swift line 30). The fixture returns the whole `LanguageModelProfile`, and each test body keeps it alive for the call.

    The DocC page is a new article, `# Guided generation`. It links `RoutedModel/respond(to:matching:maxTokens:)`, `JSONValue` and `GuidedRequestError` (each case as well), plus `Grammar` and `makeGuidedSession`. The catalog is excluded from the build target in Package.swift, so the page is documentation input only and adds no build warning.
  timestamp: 2026-08-29T14:27:45.212765+00:00
- actor: claude-code
  id: 01m16ypybphs9ca6fn9f2hymhv
  text: |
    ### implement — changed
    - evidence: 3 files — Sources/FoundationModelsRouter/Guided/GuidedGeneration.swift (modified), Sources/FoundationModelsRouter/FoundationModelsRouter.docc/GuidedGeneration.md (new), Tests/FoundationModelsRouterTests/GuidedPublicSurfaceTests.swift (new). `swift build`: Build complete, exit 0, 0 errors and 0 project warnings. `swift test`: exit 0 — 1105 tests in 117 suites passed with 2 known issues (both pre-existing `withKnownIssue` checks in RealModelHarness.swift and BoundedWait.swift), plus 83 tests in 10 suites passed; 0 failures. The 2 new tests both pass. Every Acceptance Criteria and Tests item on the card is checked.
    - next: `/review`.
  timestamp: 2026-08-29T14:27:52.822620+00:00
- actor: claude-code
  id: 01m16yx95sy8y87ebc9m7yjsrw
  text: |-
    ### test — green
    - evidence: swift test — 1188 tests passed (1105 + 83), 0 failed, 0 skipped; swift build/test emitted 0 warnings in Sources/ or Tests/ (1 unrelated warning from the mlx-swift vendored dependency bundle, out of scope)
    - note: ran swiftformat --lint (pattern-let inline, short-optionals always, single-line-for-each convert, guard-like-if-statements convert) as an extra check since no .swiftformat/.swift-format config or Airbnb plugin exists in this repo. It found 217/268 files needing reformatting repo-wide, none of them the 3 files this task touched (GuidedGeneration.swift, GuidedGeneration.md, GuidedPublicSurfaceTests.swift). That debt is pre-existing and out of this task's scope, so it was left untouched per "do not change the scope of the work."
    - next: none, build is clean
  timestamp: 2026-08-29T14:31:20.505101+00:00
position_column: doing
position_ordinal: '80'
title: Publish the guided respond(to:matching:) surface
---
## What

`JSONValue` is public, but the only method that returns it — the guided `respond(to:matching:)` in Sources/FoundationModelsRouter/Guided/GuidedGeneration.swift:210 — is internal. The audit reads this as an unfinished feature: the overload was meant to be public. Publish it.

- Make `respond(to:matching:)` public. Its doc comment at lines 200-210 is already complete, with `- Parameters:`, `- Returns:`, and `- Throws:`; leave it as it is.
- Publish `GuidedRequestError` (Sources/FoundationModelsRouter/Guided/GuidedGeneration.swift:7), which is internal today. It is the error the method documents throwing. Without it a plain-import consumer cannot name or catch the failure, and the ``GuidedRequestError`` doc link becomes the same unresolvable-internal-symbol defect the DocC repair task is fixing.
- `JSONValue`'s members are already sufficient: the six cases plus `Codable` and `Equatable` are public (Core/JSONValue.swift:5, GuidedGeneration.swift:187). Add nothing to it.
- Create a new DocC page `Sources/FoundationModelsRouter/FoundationModelsRouter.docc/GuidedGeneration.md` for this surface. The catalog holds only `RoutedSession.md` today, so this is a new file and does not collide with the other tasks that edit the catalog.

## Acceptance Criteria
- [x] A consumer with a plain `import FoundationModelsRouter` can call `respond(to:matching:)`, read the returned `JSONValue`, and catch `GuidedRequestError` by type.
- [x] `GuidedGeneration.md` exists and links `respond(to:matching:)`, `JSONValue`, and `GuidedRequestError`.

## Tests
- [x] Add `Tests/FoundationModelsRouterTests/GuidedPublicSurfaceTests.swift` with a plain `import FoundationModelsRouter` (no `@testable`), so the compiler itself proves the surface is public.
- [x] In that file, call `respond(to:matching:)` against the scripted fixtures and assert on the returned `JSONValue`.
- [x] Add a case that forces a grammar failure and catches `GuidedRequestError` by type, which proves the error is nameable from outside the module.
- [x] Run `swift test`. All tests pass.

## Workflow
- Use `/tdd` — write failing tests first, then implement to make them pass. #router #api