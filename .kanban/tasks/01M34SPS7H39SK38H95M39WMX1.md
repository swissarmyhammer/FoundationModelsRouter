---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m34ywfyd5nz8v5e7w80ekc18
  text: 'Note from ^xjk0pp8 (count tokens with the model''s tokenizer): the session now owns `RoutedSessionActor.tokenCounter`, backed by the loaded container''s tokenizer (`TokenCounter`: `count(_ text:)`, `count(_ transcript:)`, `prefix(of:tokens:)`). The overflow retry computation was not changed by ^xjk0pp8. This card can count the transcript with `tokenCounter`, or it can use the engine''s `usage.input` after a call. Both give a count, not an estimate.'
  timestamp: 2026-09-22T16:23:49.453133+00:00
- actor: claude-code
  id: 01m356kb48rvaphfh2x91exggp
  text: |-
    Research (implement, iteration 1):
    - `recoverFailedAttempt` in `Session/RoutedSessionActorTurnExecution.swift` builds the retry budget from `loweredRetryTarget(from:)` (target / 2). The retry prompt carries no pending events, so its composed prompt is `ownPrompt`. The session counts it with `tokenCounter.count(_:)`.
    - `responseTokenCeiling(requested:contextTokens:)` gives the whole window (`contextTokens`) when the caller names no `maxTokens`. With the card's formula, a turn that names no `maxTokens` has no room: window − prompt − window < 0. Such a turn does not retry after an overflow; it throws the overflow error. The retry runs only when the caller names a ceiling smaller than the window. This follows the card word for word. Tests that drove the retry with no `maxTokens` must name one.
    - The ceiling is `nil` only when the caller names none and the window is unknown (`contextTokens <= 0`). No room can be computed then, so the turn does not retry.
    - No log line exists for the overflow retry today. This card adds one.
  timestamp: 2026-09-22T18:38:38.216012+00:00
- actor: claude-code
  id: 01m358jxsgknd5gtxph30ddybq
  text: |-
    ### implement — changed
    - evidence: 7 files — Sources/FoundationModelsRouter/Session/OverflowRetryTarget.swift (new), Sources/FoundationModelsRouter/Session/RoutedSessionActorTurnExecution.swift, Sources/FoundationModelsRouter/Compaction/Compactor.swift, Tests/FoundationModelsRouterTests/AutoCompactionTests.swift, Tests/FoundationModelsRouterTests/TurnCancellationTests.swift, Tests/FoundationModelsRouterTests/Helpers/AutoCompactionFixtures.swift
    - `retryTargetHalvingDivisor` and `loweredRetryTarget(from:)` are deleted. `OverflowRetryTarget` computes `contextTokens − promptTokens − responseTokenCeiling`, capped at `budget.targetTokens`. The prompt is counted with the session's `tokenCounter`. A result that is not positive throws the original overflow. The `.compaction` event carries the target in `CompactionResult.overflowRetryTarget`, and a log line in category `OverflowRetry` states the window, prompt, ceiling, room, configured target and outcome.
    - Discovery: a turn that names no `maxTokens` gets the whole window as its ceiling, so its overflow has no room and does not retry. Four AutoCompactionTests and two TurnCancellationTests drove the retry with no ceiling. They now name `AutoCompactionFixtures.retryableResponseCeiling` (1 token, a test value only). Before that change, `cancellingTheReactiveCompactionStopsTheRetry` waited for a summarizer call that never came and the suite did not end.
    - next: test
  timestamp: 2026-09-22T19:13:21.712385+00:00
- actor: claude-code
  id: 01m358k3yhekk8w9187dvyd4dv
  text: |-
    ### test — green
    - evidence: `swift build --build-tests` 0 warnings; `swift test` — 1356 tests in 145 suites passed (2 known issues are `withKnownIssue` marks that were there before this card), plus 1 and 83 tests in the other runs, 0 failed, 0 warnings; `swift build --package-path IntegrationTests --build-tests` — Build complete; `rg 'retryTargetHalvingDivisor|loweredRetryTarget'` finds nothing.
    - next: commit
  timestamp: 2026-09-22T19:13:28.017254+00:00
position_column: doing
position_ordinal: '80'
title: Compute the overflow retry's compaction target from the room the turn needs
---
## Decision (from the owner, 2026-09-22)

`retryTargetHalvingDivisor` (2) and `loweredRetryTarget(from:)` (`Session/RoutedSessionActorTurnExecution.swift:655-666`) are an invented amount and must go. After a context overflow, the retry compacts to the room the turn needs, computed from the turn, not to `target / 2`.

## Why

- The halving came with the first auto-compaction commit, with no reason for 2.
- A lower target is needed: after the proactive compaction the transcript is at or under `target`, so a retry at the same target changes nothing. But the amount is a computation, not a fraction: the overflow means transcript + prompt + generation do not fit the window, and all three sizes are known.

## Do this

1. Delete the constant and `loweredRetryTarget(from:)`.
2. In `recoverFailedAttempt` (`:447-460`), compute the retry's target in tokens: `contextTokens − promptTokens − responseTokenCeiling`, where `promptTokens` is the composed prompt's size (measure it with the session's tokenizer, or with the same estimate the compactor uses, and say which in the code) and `responseTokenCeiling` is the ceiling this turn gave the backend. Cap the result at `budget.targetTokens`, so the retry never compacts to less than the configured target asks for a normal compaction.
3. When the result is not positive (the prompt alone, plus the response room, does not fit the window), no compaction helps. Do not retry. Throw the original overflow error, as the failed retry does today.
4. Build the `TokenBudget` for the retry from that token count (`TokenBudget` takes fractions; derive the fraction from `limit`, or add a token-valued initializer). Keep `limit` and `trigger` as they are.
5. Record the computed target on the `.compaction` event and in the log line, so a reader sees what the retry aimed for and why.
6. Update the tests in `AutoCompactionTests` that assert the halved target. Add: an overflow with a 1,000-token prompt on a 10,000-token window with a 2,000-token ceiling compacts to 7,000 tokens (capped at target when target is lower); an overflow whose prompt is larger than the window retries nothing and throws.

## Not a number

The retry still happens one time. That is a count of attempts, not a size, and the owner has not asked to change it.

## Acceptance

- `rg 'retryTargetHalvingDivisor|loweredRetryTarget'` finds nothing.
- The two tests above pass. All tests pass. #compaction #limits