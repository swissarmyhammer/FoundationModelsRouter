---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m0xxwsmarasjmwhpnq2gegy8
  text: 'Picked up. Research: the engine reads `configuration.waitSeconds` only in the `.detaching` branch of `DetachingTool.call`. When the stored `waitSeconds` is deleted, that branch has no window to race against. Decision: `.background` goes straight to `detach(...)` — the same path a `waitSeconds` of zero took. This is the contract (a declared tool always hands back the handle) and it keeps the test fixtures that used `waitSeconds: 0` fast. The wait race helpers (`raceSettlement`, `WaitRaceOutcome`) then have no caller and are deleted as dead code; the settle-versus-timeout race stays. The sub-instant case where a body settles before `markDetached` runs still returns in band — that is task ^bzekmfd''s "always a handle" work. Sibling package `FoundationModelsMultitool` has conformers (`Execute`, `MultiTool+Detachment`) that will need the same rename; that package is outside this task''s tree.'
  timestamp: 2026-08-26T02:20:26.122317+00:00
- actor: claude-code
  id: 01m0xykb8s9xphxgjgqcxf3y7w
  text: |-
    Implementation landed. TDD: the declaration tests (`undeclaredToolMountsRunToCompletion`, `declaredToolMountsBackground`, the rewritten `stockTimeoutStaysNonOptional`) were written first and failed to compile (`Mode` had no `background`; `configuration` was private); then the engine change made them pass.

    What changed:
    - `Hosting/DetachingTool.swift`: `waitSeconds`, `defaultWaitSeconds`, the `waitSeconds:` init parameter, `clockRelationFailure`, and `DetachingToolError.invalidClocks` are deleted. `Mode.detaching` is `Mode.background`. `nativeSessionMount` is `DetachConfiguration(mode: .runToCompletion, timeout: defaultTimeoutSeconds)`. `detachmentClocks(from:)` is `detachmentTimeout(from:) -> TimeInterval?` with a blanket `nil` default. `call` resolves the timeout only; `.background` goes straight to `detach(...)`. The wait race (`raceSettlement`, `WaitRaceOutcome`) had no caller and is deleted. `configuration` is internal (like `wrapped`) so a composition-site test can read the resolved mount.
    - `Session/ToolOutputCapping.swift`: `sessionMounted` doc comment rewritten for the new mount policy and the examples (shell and agent tools declare background; file edit, skill, and discovery tools do not).
    - Doc-only fixes for stale references: `RoutedSessionActorTurnExecution.swift` (named the deleted `raceSettlement`), `GenerationReentry.swift`, `SessionMailbox.swift` (`boundedNanoseconds` doc), and the `RunEventFunnel` doc.
    - Tests: fixtures now declare `DetachConfiguration(mode: .background, timeout: nil)` through `detachmentMount` (`DeclaredBackgroundTool`, `CollectSentenceTool`, `GatedBackgroundTool` in two suites); `detachAtOnceMount` is `backgroundMount`; the three `NestedGenerationReentryTests` tools drop the conformance; in-band tests mount `.runToCompletion`; the clock-relation tests, the per-call `waitSeconds` test, and the `waitSeconds`-inert test are deleted with the feature.

    Not done here, on purpose: a body that settles before `markDetached` runs still returns in band (task ^bzekmfd, "always a handle"). The sibling package `FoundationModelsMultitool` conforms to `detachmentClocks(from:)` in `Execute.swift` and `MultiTool+Detachment.swift`; it is outside this repository and needs the same rename before it builds against this change.
  timestamp: 2026-08-26T02:32:45.081830+00:00
- actor: claude-code
  id: 01m0xykg3gqzkk1mzy8jcm7zst
  text: |-
    ### implement — changed
    - evidence: 9 files — Sources/FoundationModelsRouter/Hosting/DetachingTool.swift, Sources/FoundationModelsRouter/Hosting/SessionMailbox.swift, Sources/FoundationModelsRouter/Session/ToolOutputCapping.swift, Sources/FoundationModelsRouter/Session/RoutedSessionActorTurnExecution.swift, Sources/FoundationModelsRouter/Session/GenerationReentry.swift, Tests/FoundationModelsRouterTests/DetachingToolTests.swift, NestedGenerationReentryTests.swift, SessionOutboxToolWiringTests.swift, RespondRunPlaneDrainTests.swift, DeclaredRunKindTests.swift, RegisteredJournalOpTests.swift. `swift build --build-tests`: 0 errors, 0 warnings. `swift test`: 1042 tests in 101 suites passed, 83 tests in 10 suites passed (2 pre-existing known issues). `rg 'waitSeconds' Sources`: only RaceGate.swift:16, ToolContext.swift:45, SessionMailbox.swift:309 (doc lines owned by ^bzekmfd/^1pch3ee). `rg 'invalidClocks|clockRelationFailure|defaultWaitSeconds|detachmentClocks' Sources Tests`: no match.
    - next: /review
  timestamp: 2026-08-26T02:32:50.032283+00:00
depends_on:
- 01M0XGPYWHJZSCGFTACTMGCG7M
position_column: doing
position_ordinal: '80'
title: One explicit background declaration replaces DetachConfiguration
---
## What
No timer and no race decide behavior. The default for every tool is run-to-completion; a tool that is known ahead of time to run long declares background about itself. One knob remains: `timeout`. Work in `Sources/FoundationModelsRouter/Hosting/DetachingTool.swift` plus the one real composition site.

- [x] Delete `DetachConfiguration.waitSeconds`, `defaultWaitSeconds`, the `waitSeconds:` init parameter, `clockRelationFailure(tool:mode:waitSeconds:timeout:)`, and `DetachingToolError.invalidClocks(tool:waitSeconds:timeout:)`.
- [x] Rename `Mode.detaching` to `Mode.background`. Keep `Mode.runToCompletion`.
- [x] The declaration API is exact: a long-running tool declares
      `var detachmentMount: DetachConfiguration? { DetachConfiguration(mode: .background, timeout: nil) }`
      (or a long timeout). No new member for the mode. A tool that declares nothing is synchronous.
- [x] Replace `DetachmentParameterProviding.detachmentClocks(from:) -> (waitSeconds: TimeInterval?, timeout: TimeInterval?)` (public, 6 conformers) with a timeout-only member: `detachmentTimeout(from:) -> TimeInterval?`. Keep the blanket default. The per-call `timeout` path MUST stay alive — `../FoundationModelsMultitool/Sources/FoundationModelsMultitool/MultiTool+Detachment.swift:98` bounds every `runCode` call with it.
- [x] The one composition site is `ToolDetachment.sessionMounted` in `Sources/FoundationModelsRouter/Session/ToolOutputCapping.swift:189-204` (all of `RoutedModel.makeSession`, `RoutedSessionActor.fork`, and `restoreSessionTree` go through it). Flip it: `nativeSessionMount` becomes exactly `DetachConfiguration(mode: .runToCompletion, timeout: defaultTimeoutSeconds)` (120 s — a synchronous tool that hangs reports `timedOut`); a tool's own `.background` declaration wins. Rewrite that site's doc comment (lines 145-177) — it narrates the old mount policy.
- [x] Doc comments state the examples: shell and agent tools declare background; file edit, skill, and discovery tools do not.

## Acceptance Criteria
- [x] `rg 'waitSeconds' Sources` matches only `Hosting/DetachingTool.swift` (the race body, deleted by ^bzekmfd) and doc-only lines in `Concurrency/RaceGate.swift:16`, `Hosting/ToolContext.swift:45`, `Hosting/SessionMailbox.swift:309` (owned by ^bzekmfd/^1pch3ee). No other file matches.
- [x] `rg 'invalidClocks|clockRelationFailure|defaultWaitSeconds|detachmentClocks' Sources Tests` returns no match.
- [x] A test proves: an undeclared tool mounts run-to-completion with the 120 s timeout; a declared tool mounts background.
- [x] `swift build --build-tests` is green.

## Tests
- [x] Update conformers and fixtures in: `Tests/FoundationModelsRouterTests/DetachingToolTests.swift`, `NestedGenerationReentryTests.swift` (8 sites, 3 conformances — its raised-waitSeconds premise is handled by task ^6858xas), `SessionOutboxToolWiringTests.swift` (4 sites), `RespondRunPlaneDrainTests.swift` (2 sites), `DeclaredRunKindTests.swift`, `RegisteredJournalOpTests.swift`.
- [x] Run `swift test` — green.

## Workflow
- Use `/tdd` — write failing tests first, then implement to make them pass.