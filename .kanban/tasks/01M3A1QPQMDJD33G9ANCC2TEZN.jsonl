{"id":"01M3A1QPQN58MKJYXBYPJ524A8","timestamp":"2026-09-24T15:49:50.453094Z","op":"create","item_id":"01M3A1QPQMDJD33G9ANCC2TEZN","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,39 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01M39ZNAJWMVZ291SCH8CSJ2HW\n+position_column: todo\n+position_ordinal: '8980'\n+title: 'R2: key the prompt cache by the Router session id, and release it when the session closes'\n+---\n+## Why\n+\n+Now the fork keys the cache by `(modelID, id of the first transcript entry)` (fork `MLXLanguageModel.swift:695-700` at HEAD 9a44d4f). Results:\n+- A fork has the first entry of its parent, so the two share one key and take the one entry from each other.\n+- A compaction or repetition rebuild that drops the first entry orphans the old key.\n+- The store never learns that a session ended.\n+\n+The plan from the FoundationModelsAgents session (R2) keeps the fork's key and makes the Router count the open holders of each key. **Challenge (proposed here, not yet accepted by the other sessions):** the Router knows the session. The queued executor wrapper of ^8csj2hw can be one instance for each session, as `RecordingLanguageModel` is one for each handle (`Recording/RecordingLanguageModel.swift:72-84`: its `Executor.Configuration` compares by the state identity). The wrapper then binds a task-local key, on the same task, just before it calls the inner executor. Result:\n+- Each session, and each fork, has its own key. No holder count is necessary.\n+- A compaction keeps the key. The old prefix does not match, the planner reuses what it can, and the new entry replaces the old one. No key is orphaned.\n+- `close()` releases exactly its own key.\n+- \"Keep nothing\" (R3) is the same task-local with no key.\n+\n+## External dependencies (mlx-swift-lm board)\n+\n+- F5: `releasePromptCache(sessionID:)`.\n+- New, proposed to the mlx session: a public task-local that sets the cache key of a pass (`.session(String)` or `.none`). `sessionCacheKey` falls back to the first entry id when the task-local is not set. If the mlx session refuses it, go back to the holder count of the original plan and write why on this task.\n+\n+## What to do\n+\n+1. In the queued wrapper (^8csj2hw), make one model instance for each `RoutedSessionActor` and bind the task-local to the session ULID around the inner executor call.\n+2. `RoutedSessionActor.close()` (`Session/RoutedSessionActorForking.swift:333-349`) calls `releasePromptCache(sessionID:)` for its ULID. Put the call BEFORE the `guard !terminalEvents.isEmpty else { return }` at `:340`, or the release is skipped for most sessions.\n+3. A session that is dropped without `close()` is still limited: the byte LRU and the disk budget of the fork remove it later. Write this in the doc comment of `close()`.\n+\n+## Acceptance Criteria\n+\n+- [ ] A fork and its parent use two different keys (test with a model that records the key of each pass).\n+- [ ] A closed session releases its key. The close of a fork does not release the key of its open parent.\n+- [ ] A compaction keeps the key of the session (no new key in the store).\n+- [ ] A session closed with no mailbox events still releases its key. #generation-queue #prompt-cache\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,39 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01M39ZNAJWMVZ291SCH8CSJ2HW\n-position_column: todo\n-position_ordinal: '8980'\n-title: 'R2: key the prompt cache by the Router session id, and release it when the session closes'\n----\n-## Why\n-\n-Now the fork keys the cache by `(modelID, id of the first transcript entry)` (fork `MLXLanguageModel.swift:695-700` at HEAD 9a44d4f). Results:\n-- A fork has the first entry of its parent, so the two share one key and take the one entry from each other.\n-- A compaction or repetition rebuild that drops the first entry orphans the old key.\n-- The store never learns that a session ended.\n-\n-The plan from the FoundationModelsAgents session (R2) keeps the fork's key and makes the Router count the open holders of each key. **Challenge (proposed here, not yet accepted by the other sessions):** the Router knows the session. The queued executor wrapper of ^8csj2hw can be one instance for each session, as `RecordingLanguageModel` is one for each handle (`Recording/RecordingLanguageModel.swift:72-84`: its `Executor.Configuration` compares by the state identity). The wrapper then binds a task-local key, on the same task, just before it calls the inner executor. Result:\n-- Each session, and each fork, has its own key. No holder count is necessary.\n-- A compaction keeps the key. The old prefix does not match, the planner reuses what it can, and the new entry replaces the old one. No key is orphaned.\n-- `close()` releases exactly its own key.\n-- \"Keep nothing\" (R3) is the same task-local with no key.\n-\n-## External dependencies (mlx-swift-lm board)\n-\n-- F5: `releasePromptCache(sessionID:)`.\n-- New, proposed to the mlx session: a public task-local that sets the cache key of a pass (`.session(String)` or `.none`). `sessionCacheKey` falls back to the first entry id when the task-local is not set. If the mlx session refuses it, go back to the holder count of the original plan and write why on this task.\n-\n-## What to do\n-\n-1. In the queued wrapper (^8csj2hw), make one model instance for each `RoutedSessionActor` and bind the task-local to the session ULID around the inner executor call.\n-2. `RoutedSessionActor.close()` (`Session/RoutedSessionActorForking.swift:333-349`) calls `releasePromptCache(sessionID:)` for its ULID. Put the call BEFORE the `guard !terminalEvents.isEmpty else { return }` at `:340`, or the release is skipped for most sessions.\n-3. A session that is dropped without `close()` is still limited: the byte LRU and the disk budget of the fork remove it later. Write this in the doc comment of `close()`.\n-\n-## Acceptance Criteria\n-\n-- [ ] A fork and its parent use two different keys (test with a model that records the key of each pass).\n-- [ ] A closed session releases its key. The close of a fork does not release the key of its open parent.\n-- [ ] A compaction keeps the key of the session (no new key in the store).\n-- [ ] A session closed with no mailbox events still releases its key. #generation-queue #prompt-cache\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M3A1S05GQ93041W0G01VM3ZW","timestamp":"2026-09-24T15:50:32.880851Z","op":"update","item_id":"01M3A1QPQMDJD33G9ANCC2TEZN","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,11 @@\n ---\n assignees:\n - claude-code\n+comments:\n+- actor: claude-code\n+  id: 01m3a1s05gdctdh8wkbkkv6kp2\n+  text: '2026-09-24: the FoundationModelsAgents session accepted the task-local key, and the plan file changed. Fork F6 is now \"a public task-local cache key that the host sets\" (`.session(String)`, `.none`, fallback to the first entry id). The external dependencies of this task are thus F5 (release) and F6 (task-local), and no separate proposal task exists. The holder count is only the fallback if the mlx session refuses F6.'\n+  timestamp: 2026-09-24T15:50:32.880445+00:00\n depends_on:\n - 01M39ZNAJWMVZ291SCH8CSJ2HW\n position_column: todo\n","reverse_patch":"--- original\n+++ modified\n@@ -1,11 +1,6 @@\n ---\n assignees:\n - claude-code\n-comments:\n-- actor: claude-code\n-  id: 01m3a1s05gdctdh8wkbkkv6kp2\n-  text: '2026-09-24: the FoundationModelsAgents session accepted the task-local key, and the plan file changed. Fork F6 is now \"a public task-local cache key that the host sets\" (`.session(String)`, `.none`, fallback to the first entry id). The external dependencies of this task are thus F5 (release) and F6 (task-local), and no separate proposal task exists. The holder count is only the fallback if the mlx session refuses F6.'\n-  timestamp: 2026-09-24T15:50:32.880445+00:00\n depends_on:\n - 01M39ZNAJWMVZ291SCH8CSJ2HW\n position_column: todo\n","transaction_id":null}
{"id":"01M3A1YCJS6GPJTT7C938BB6B4","timestamp":"2026-09-24T15:53:29.433027Z","op":"update","item_id":"01M3A1QPQMDJD33G9ANCC2TEZN","forward_patch":"--- original\n+++ modified\n@@ -6,6 +6,10 @@\n   id: 01m3a1s05gdctdh8wkbkkv6kp2\n   text: '2026-09-24: the FoundationModelsAgents session accepted the task-local key, and the plan file changed. Fork F6 is now \"a public task-local cache key that the host sets\" (`.session(String)`, `.none`, fallback to the first entry id). The external dependencies of this task are thus F5 (release) and F6 (task-local), and no separate proposal task exists. The holder count is only the fallback if the mlx session refuses F6.'\n   timestamp: 2026-09-24T15:50:32.880445+00:00\n+- actor: claude-code\n+  id: 01m3a1ycjqeyeb0224x4pgbbbb\n+  text: 'Fork dependencies (mlx-swift-lm board, 2026-09-24): ^2mk47nr (task-local, no dependency, can land first) and ^zcys2qw (release). The mlx session verified that the task-local reaches sessionCacheKey: Executor.respond -> withTaskCancellationHandler -> $drainCoordinator.withValue -> runRespond -> sessionCacheKey, all on one task. LIMIT: the binding does NOT reach the executor through LanguageModelSession, because the SDK can run the executor on another task. The wrapper must bind it inside its own executor respond and call the inner executor directly (as RecordingLanguageModelState.makePassthrough does). API: `public enum PromptCacheScope { case session(String); case none }`, `@TaskLocal public static var promptCacheScope: PromptCacheScope?` (nil = first-entry-id rule), `public func releasePromptCache(sessionID: String) async` (memory + spilling + disk for this model id; no-op if unknown). The release id is the same string the wrapper binds. The holder count is not necessary.'\n+  timestamp: 2026-09-24T15:53:29.431805+00:00\n depends_on:\n - 01M39ZNAJWMVZ291SCH8CSJ2HW\n position_column: todo\n","reverse_patch":"--- original\n+++ modified\n@@ -6,10 +6,6 @@\n   id: 01m3a1s05gdctdh8wkbkkv6kp2\n   text: '2026-09-24: the FoundationModelsAgents session accepted the task-local key, and the plan file changed. Fork F6 is now \"a public task-local cache key that the host sets\" (`.session(String)`, `.none`, fallback to the first entry id). The external dependencies of this task are thus F5 (release) and F6 (task-local), and no separate proposal task exists. The holder count is only the fallback if the mlx session refuses F6.'\n   timestamp: 2026-09-24T15:50:32.880445+00:00\n-- actor: claude-code\n-  id: 01m3a1ycjqeyeb0224x4pgbbbb\n-  text: 'Fork dependencies (mlx-swift-lm board, 2026-09-24): ^2mk47nr (task-local, no dependency, can land first) and ^zcys2qw (release). The mlx session verified that the task-local reaches sessionCacheKey: Executor.respond -> withTaskCancellationHandler -> $drainCoordinator.withValue -> runRespond -> sessionCacheKey, all on one task. LIMIT: the binding does NOT reach the executor through LanguageModelSession, because the SDK can run the executor on another task. The wrapper must bind it inside its own executor respond and call the inner executor directly (as RecordingLanguageModelState.makePassthrough does). API: `public enum PromptCacheScope { case session(String); case none }`, `@TaskLocal public static var promptCacheScope: PromptCacheScope?` (nil = first-entry-id rule), `public func releasePromptCache(sessionID: String) async` (memory + spilling + disk for this model id; no-op if unknown). The release id is the same string the wrapper binds. The holder count is not necessary.'\n-  timestamp: 2026-09-24T15:53:29.431805+00:00\n depends_on:\n - 01M39ZNAJWMVZ291SCH8CSJ2HW\n position_column: todo\n","transaction_id":null}
{"id":"01M3A5K3C11DXJQ56TZHHEGQF8","timestamp":"2026-09-24T16:57:13.857577Z","op":"update","item_id":"01M3A1QPQMDJD33G9ANCC2TEZN","forward_patch":"--- original\n+++ modified\n@@ -18,27 +18,33 @@\n ---\n ## Why\n\n-Now the fork keys the cache by `(modelID, id of the first transcript entry)` (fork `MLXLanguageModel.swift:695-700` at HEAD 9a44d4f). Results:\n+Now the fork keys the cache by `(modelID, id of the first transcript entry)` (`sessionCacheKey(for:modelID:)` in the fork's `MLXLanguageModel.swift`). Results:\n+\n - A fork has the first entry of its parent, so the two share one key and take the one entry from each other.\n - A compaction or repetition rebuild that drops the first entry orphans the old key.\n - The store never learns that a session ended.\n\n-The plan from the FoundationModelsAgents session (R2) keeps the fork's key and makes the Router count the open holders of each key. **Challenge (proposed here, not yet accepted by the other sessions):** the Router knows the session. The queued executor wrapper of ^8csj2hw can be one instance for each session, as `RecordingLanguageModel` is one for each handle (`Recording/RecordingLanguageModel.swift:72-84`: its `Executor.Configuration` compares by the state identity). The wrapper then binds a task-local key, on the same task, just before it calls the inner executor. Result:\n-- Each session, and each fork, has its own key. No holder count is necessary.\n-- A compaction keeps the key. The old prefix does not match, the planner reuses what it can, and the new entry replaces the old one. No key is orphaned.\n-- `close()` releases exactly its own key.\n-- \"Keep nothing\" (R3) is the same task-local with no key.\n+Decision (accepted by the FoundationModelsAgents and mlx-swift-lm sessions, 2026-09-24): the Router gives each session its own key through the fork's task-local, and releases it on close. No count of holders is necessary. Design: `generation-queue.md`, sections 2 and 3.\n+\n+## External dependencies (tag `needs-fork`)\n\n-## External dependencies (mlx-swift-lm board)\n+Fork tasks on the mlx-swift-lm board: ^2mk47nr (the task-local key) and ^zcys2qw (the release). Do not start until both are merged on the fork's `stable` branch. Then remove the `needs-fork` tag.\n\n-- F5: `releasePromptCache(sessionID:)`.\n-- New, proposed to the mlx session: a public task-local that sets the cache key of a pass (`.session(String)` or `.none`). `sessionCacheKey` falls back to the first entry id when the task-local is not set. If the mlx session refuses it, go back to the holder count of the original plan and write why on this task.\n+## Fork API this task uses\n\n+```swift\n+public enum PromptCacheScope: Sendable, Hashable { case session(String); case none }\n+@TaskLocal public static var promptCacheScope: PromptCacheScope?   // nil = first-entry-id rule\n+public func releasePromptCache(sessionID: String) async            // memory + spilling + disk; no-op if unknown\n+```\n+\n ## What to do\n\n-1. In the queued wrapper (^8csj2hw), make one model instance for each `RoutedSessionActor` and bind the task-local to the session ULID around the inner executor call.\n-2. `RoutedSessionActor.close()` (`Session/RoutedSessionActorForking.swift:333-349`) calls `releasePromptCache(sessionID:)` for its ULID. Put the call BEFORE the `guard !terminalEvents.isEmpty else { return }` at `:340`, or the release is skipped for most sessions.\n-3. A session that is dropped without `close()` is still limited: the byte LRU and the disk budget of the fork remove it later. Write this in the doc comment of `close()`.\n+1. Bump the fork pin in the root package and in `IntegrationTests` (`swift package update mlx-swift-lm` in both; write the resolved revision in a comment). Skip this step if R1 ^tv2yt7s already moved the pin to a revision that has both fork tasks.\n+2. In the executor `respond` of the per-session queued wrapper (^8csj2hw), bind `MLXLanguageModel.$promptCacheScope` to `.session(<the session ULID>)` around the call of the inner executor, on the same task. LIMIT (verified by the mlx session): the binding reaches the fork's executor only when the host calls `Executor.respond` directly on the same task. A binding made on the session actor, above `LanguageModelSession`, does not reach it, because the SDK can run the executor on another task.\n+3. The per-session wrapper state of ^8csj2hw thus carries the session ULID. A fork gets the ULID of the fork, not of its parent.\n+4. `RoutedSessionActor.close()` (`Session/RoutedSessionActorForking.swift`) calls `releasePromptCache(sessionID:)` with the same ULID string. Put the call BEFORE the `guard !terminalEvents.isEmpty else { return }` early return, or the release is skipped for most sessions.\n+5. A session that is dropped without `close()` is still limited: the fork's byte LRU and disk budget remove it later. Write this in the doc comment of `close()`.\n\n ## Acceptance Criteria\n\n@@ -45,4 +51,5 @@\n - [ ] A fork and its parent use two different keys (test with a model that records the key of each pass).\n - [ ] A closed session releases its key. The close of a fork does not release the key of its open parent.\n - [ ] A compaction keeps the key of the session (no new key in the store).\n-- [ ] A session closed with no mailbox events still releases its key. #generation-queue #prompt-cache\n\\ No newline at end of file\n+- [ ] A session closed with no mailbox events still releases its key.\n+- [ ] `secondTurnReusesFirstTurnsKVCache` (`IntegrationTests/.../LanguageModelSessionBackendTests.swift`) stays green. #generation-queue #prompt-cache #needs-fork\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -18,33 +18,27 @@\n ---\n ## Why\n\n-Now the fork keys the cache by `(modelID, id of the first transcript entry)` (`sessionCacheKey(for:modelID:)` in the fork's `MLXLanguageModel.swift`). Results:\n-\n+Now the fork keys the cache by `(modelID, id of the first transcript entry)` (fork `MLXLanguageModel.swift:695-700` at HEAD 9a44d4f). Results:\n - A fork has the first entry of its parent, so the two share one key and take the one entry from each other.\n - A compaction or repetition rebuild that drops the first entry orphans the old key.\n - The store never learns that a session ended.\n\n-Decision (accepted by the FoundationModelsAgents and mlx-swift-lm sessions, 2026-09-24): the Router gives each session its own key through the fork's task-local, and releases it on close. No count of holders is necessary. Design: `generation-queue.md`, sections 2 and 3.\n-\n-## External dependencies (tag `needs-fork`)\n-\n-Fork tasks on the mlx-swift-lm board: ^2mk47nr (the task-local key) and ^zcys2qw (the release). Do not start until both are merged on the fork's `stable` branch. Then remove the `needs-fork` tag.\n+The plan from the FoundationModelsAgents session (R2) keeps the fork's key and makes the Router count the open holders of each key. **Challenge (proposed here, not yet accepted by the other sessions):** the Router knows the session. The queued executor wrapper of ^8csj2hw can be one instance for each session, as `RecordingLanguageModel` is one for each handle (`Recording/RecordingLanguageModel.swift:72-84`: its `Executor.Configuration` compares by the state identity). The wrapper then binds a task-local key, on the same task, just before it calls the inner executor. Result:\n+- Each session, and each fork, has its own key. No holder count is necessary.\n+- A compaction keeps the key. The old prefix does not match, the planner reuses what it can, and the new entry replaces the old one. No key is orphaned.\n+- `close()` releases exactly its own key.\n+- \"Keep nothing\" (R3) is the same task-local with no key.\n\n-## Fork API this task uses\n+## External dependencies (mlx-swift-lm board)\n\n-```swift\n-public enum PromptCacheScope: Sendable, Hashable { case session(String); case none }\n-@TaskLocal public static var promptCacheScope: PromptCacheScope?   // nil = first-entry-id rule\n-public func releasePromptCache(sessionID: String) async            // memory + spilling + disk; no-op if unknown\n-```\n+- F5: `releasePromptCache(sessionID:)`.\n+- New, proposed to the mlx session: a public task-local that sets the cache key of a pass (`.session(String)` or `.none`). `sessionCacheKey` falls back to the first entry id when the task-local is not set. If the mlx session refuses it, go back to the holder count of the original plan and write why on this task.\n\n ## What to do\n\n-1. Bump the fork pin in the root package and in `IntegrationTests` (`swift package update mlx-swift-lm` in both; write the resolved revision in a comment). Skip this step if R1 ^tv2yt7s already moved the pin to a revision that has both fork tasks.\n-2. In the executor `respond` of the per-session queued wrapper (^8csj2hw), bind `MLXLanguageModel.$promptCacheScope` to `.session(<the session ULID>)` around the call of the inner executor, on the same task. LIMIT (verified by the mlx session): the binding reaches the fork's executor only when the host calls `Executor.respond` directly on the same task. A binding made on the session actor, above `LanguageModelSession`, does not reach it, because the SDK can run the executor on another task.\n-3. The per-session wrapper state of ^8csj2hw thus carries the session ULID. A fork gets the ULID of the fork, not of its parent.\n-4. `RoutedSessionActor.close()` (`Session/RoutedSessionActorForking.swift`) calls `releasePromptCache(sessionID:)` with the same ULID string. Put the call BEFORE the `guard !terminalEvents.isEmpty else { return }` early return, or the release is skipped for most sessions.\n-5. A session that is dropped without `close()` is still limited: the fork's byte LRU and disk budget remove it later. Write this in the doc comment of `close()`.\n+1. In the queued wrapper (^8csj2hw), make one model instance for each `RoutedSessionActor` and bind the task-local to the session ULID around the inner executor call.\n+2. `RoutedSessionActor.close()` (`Session/RoutedSessionActorForking.swift:333-349`) calls `releasePromptCache(sessionID:)` for its ULID. Put the call BEFORE the `guard !terminalEvents.isEmpty else { return }` at `:340`, or the release is skipped for most sessions.\n+3. A session that is dropped without `close()` is still limited: the byte LRU and the disk budget of the fork remove it later. Write this in the doc comment of `close()`.\n\n ## Acceptance Criteria\n\n@@ -51,5 +45,4 @@\n - [ ] A fork and its parent use two different keys (test with a model that records the key of each pass).\n - [ ] A closed session releases its key. The close of a fork does not release the key of its open parent.\n - [ ] A compaction keeps the key of the session (no new key in the store).\n-- [ ] A session closed with no mailbox events still releases its key.\n-- [ ] `secondTurnReusesFirstTurnsKVCache` (`IntegrationTests/.../LanguageModelSessionBackendTests.swift`) stays green. #generation-queue #prompt-cache #needs-fork\n\\ No newline at end of file\n+- [ ] A session closed with no mailbox events still releases its key. #generation-queue #prompt-cache\n\\ No newline at end of file\n","transaction_id":null}
