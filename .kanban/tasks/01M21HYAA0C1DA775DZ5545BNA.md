---
comments:
- actor: claude-code
  id: 01m21jqcgdseh4d6fcan0snc7z
  text: |-
    ### implement — research

    - `ModelSlot` (`Core/ModelSlot.swift`) and `ModelRef` (`Core/ModelRef.swift`) are public. No change is necessary for them.
    - `ResolutionProgress.swift` holds the only internal members: `SlotProgress`, `SlotProgress.State`, the four stored properties, `progressFraction`, `ResolutionProgress.fraction` and `ResolutionProgress.slots`.
    - The writers of `slots` and `fraction` are all in this module: `Router.swift` (`reporter`, `markSizing`, `markChosen`, `setSlotState`, `complete`, `recordFailure`, `recordLoadError`) and `ResolutionProgress.refreshFraction()`. `public internal(set)` keeps each of them valid.
    - `SlotProgress.init` stays internal: a consumer reads the type and does not write it.
    - Every type a plain-import test needs to drive `Router.resolve` is public: `Router.init`, `MachineProbe`, `MetadataSource`, `RawRepoMetadata`, `ModelLoader`, `LoadedLLMContainer`, `LoadedEmbeddingContainer`, `DownloadProgress`, `ProfileDefinition`.
    - The compile proof goes in a new test target, `FoundationModelsRouterPublicSurfaceTests`, that depends on the library alone. The Router test target imports the library `@testable` in its other files, so a plain import there is not a proof.
    - CI calls the shared `swift-ci` workflow, which runs `swift test` at the root. A new root test target runs in CI with no workflow change.
    - The rules that apply: `swift/access-control` (explicit modifiers, `internal(set)`), `missing-docs-swift` (a doc comment on each public item), `magic-numbers-swift` (named constants in the test), `optionals` (no `guard` in a test), `test-partitioning` (the new target is a root unit target), `duplication`.
  timestamp: 2026-09-08T22:37:54.061925+00:00
- actor: claude-code
  id: 01m21k0nr20jze9mrcg6heehy1
  text: |-
    ### implement — changed

    TDD record:
    - RED: the new target `FoundationModelsRouterPublicSurfaceTests` did not compile. `swift build --build-tests` stopped with `'slots' is inaccessible due to 'internal' protection level` (two sites) and `'fraction' is inaccessible due to 'internal' protection level`. Two more diagnostics cascaded from the failed `#require`.
    - GREEN: `SlotProgress`, `SlotProgress.State` and `progressFraction` are `public`; `state`, `chosen`, `bytesDownloaded`, `bytesTotal`, `ResolutionProgress.fraction` and `ResolutionProgress.slots` are `public internal(set)`. `SlotProgress.init` stays internal. The `@Observable` macro kept the `internal(set)` accessors: the build has no compiler warning, and every internal writer in `Router.swift` still compiles.

    The test drives one `Router.resolve` over stub types written against the public surface alone (`MachineProbe`, `MetadataSource`, `ModelLoader`, `LoadedLLMContainer`, `LoadedEmbeddingContainer`). The loader reports named byte counts, and the test reads the same counts back through `progress.slots` for each of the three slots, with the slot state, the chosen model, `progressFraction` and the overall `fraction`.

    Discoveries:
    - `swift build` prints one warning on this machine: `missing creator for mutated node: (...mlx-swift_Cmlx.bundle/Contents/MacOS)`. It is present on the unchanged tree too (checked with the edits stashed), so it is not from this change.
    - `swift test` runs each test target as its own process, so the summary line at the end of the output is the last target alone. Read every `Test run with` line.
    - The two "known issues" in the Router test target are pre-existing `withKnownIssue` blocks in `RealModelHarnessTests` and `BoundedWaitTests`.
    - The nested `IntegrationTests` package still builds (`swift build --package-path IntegrationTests --build-tests`), so its `@testable` users are not affected.

    - evidence: 3 files — `Package.swift` (new test target), `Sources/FoundationModelsRouter/Resolution/ResolutionProgress.swift`, `Tests/FoundationModelsRouterPublicSurfaceTests/ResolutionProgressPublicSurfaceTests.swift` (new). `swift build --build-tests`: exit 0, no compiler warning. `swift test --filter ResolutionProgressPublicSurfaceTests`: 1 test in 1 suite passed. `swift test`: 1255 tests in 135 suites passed (2 known issues, pre-existing), 1 test in 1 suite passed, 83 tests in 10 suites passed; 0 failures.
    - next: `/review`, then `/commit` to land the change on `main` (the one open acceptance box).
  timestamp: 2026-09-08T22:42:58.434710+00:00
- actor: claude-code
  id: 01m21k4v0ff321zj0mygayykth
  text: |-
    ### test — green

    I did these steps.

    1. I touched the changed files. This step makes sure the build does not hide warnings.
       - Package.swift
       - Sources/FoundationModelsRouter/Resolution/ResolutionProgress.swift
       - Tests/FoundationModelsRouterPublicSurfaceTests/ResolutionProgressPublicSurfaceTests.swift
    2. I ran `swift build --build-tests`. The build was clean. The build showed only one warning: "missing creator for mutated node" on the MLX bundle. This warning is present on the tree before this change. It is not a new warning.
    3. I ran `swift test` at the root. I read the full output, not a filtered run.

    Result: all tests pass. Zero failures. Zero skipped tests.

    - Main test bundle: 1255 tests in 135 suites passed, with 2 known issues. Both known issues are pre-existing and planned (`RealModelHarness.swift` and `BoundedWait.swift`). They are not new. They are not failures.
    - New public-surface test bundle: 1 test in 1 suite passed. This is the new test for this task. It proves the plain `import FoundationModelsRouter` reads `slots`, each slot's `state`, `chosen`, `bytesDownloaded`, `bytesTotal`, `progressFraction`, and the overall `fraction`.
    - Evals test bundle: 83 tests in 10 suites passed.

    I did not run the `IntegrationTests` package. This change only widens access to existing members. It does not change behavior. The `IntegrationTests` package reads `ResolutionProgress.phase`, which this change does not touch. CI runs the integration suite.

    - evidence: `swift build --build-tests` clean (only the pre-existing MLX bundle warning); `swift test` — 1255 passed / 0 failed (135 suites, 2 pre-existing known issues) + 1 passed / 0 failed (1 suite, new public-surface test) + 83 passed / 0 failed (10 suites) = 1339 passed, 0 failed, 0 skipped
    - next: ready for review
  timestamp: 2026-09-08T22:45:14.895621+00:00
- actor: claude-code
  id: 01m21k66qd3vqch0ya5acn00sa
  text: |-
    ### commit — changed
    - evidence: e153630 feat(resolution): make per-slot progress public (^5545bna), 5 files changed (356 insertions, 22 deletions)
    - next: none
  timestamp: 2026-09-08T22:45:59.661391+00:00
- actor: claude-code
  id: 01m21kc8mm9jhbz328d8xr0krw
  text: |-
    ### review — clean
    - evidence: `review sha HEAD~1..HEAD` (commit e153630). The engine made 7 attempts and reported 0 findings, 0 confirmed, 0 refuted. It reviewed 3 files: `Package.swift`, `Sources/FoundationModelsRouter/Resolution/ResolutionProgress.swift`, `Tests/FoundationModelsRouterPublicSurfaceTests/ResolutionProgressPublicSurfaceTests.swift`. It did not review 2 files in `.kanban/` because `.reviewignore` excludes them.
    - next: none. The task moved from `review` to `done`. The commit is on `main`.
  timestamp: 2026-09-08T22:49:18.228757+00:00
- actor: claude-code
  id: 01m21kesb07p3y5zyzxrfwbwc1
  text: |-
    ### finish iteration 1 — clean
    - implement: changed — 3 files (Package.swift, Sources/FoundationModelsRouter/Resolution/ResolutionProgress.swift, Tests/FoundationModelsRouterPublicSurfaceTests/ResolutionProgressPublicSurfaceTests.swift)
    - test: green — swift test, 1339 passed, 0 failed, 0 skipped
    - commit: e153630
    - review: clean — 0 findings, task moved to done
    - note: the acceptance box "The change is on main" is true. Commit e153630 is HEAD on main. The box is not checked because no step is permitted to change the description after the review.
  timestamp: 2026-09-08T22:50:40.864312+00:00
position_column: done
position_ordinal: ffffd180
title: Make the per-slot ResolutionProgress surface public
---
## What

Make the per-slot `ResolutionProgress` surface public, so a consumer
outside this package can draw a download bar with byte counts.

Asked for by `FoundationModelsACPAgent`, card `^sf22smx`, which blocks
its CLI download progress (its `cli-plan.md` §5.7).

**The overall fraction is already reachable.** `ResolutionProgress`
publishes `public var phases: AsyncStream<PhaseTransition>` with
`public typealias PhaseTransition = (phase: Phase, fraction: Double)`.
Only the **per-slot** surface is missing, and that is what a bar with
byte counts needs.

In `Sources/FoundationModelsRouter/Resolution/ResolutionProgress.swift`:

- `var slots: [ModelSlot: SlotProgress]` becomes public.
- `SlotProgress` becomes public, with its `state`, `chosen`,
  `bytesDownloaded`, `bytesTotal` and `progressFraction`.
- `SlotProgress.State` becomes public.
- `ModelSlot` and `ModelRef` are **already public**
  (`Core/ModelSlot.swift:5`, and `ModelRef` appears in
  `public static let defaultStandard: [ModelRef]`), so they need no
  change. Confirm this before you start.
- Make `fraction` public too, for a consumer that wants it without a
  subscription to `phases`.

Keep every setter internal where a setter is not necessary: a consumer
reads this type and never writes it.

**Two constraints to plan for:**

1. The type is `@MainActor @Observable public final class`. Every
   consumer read is a main-actor hop.
2. A change to the access level of an `@Observable` stored property is
   not a keyword edit — the macro makes the observation plumbing, so
   check that the expanded accessors keep the intended access and that
   the build stays free of warnings.

- [x] `slots` public
- [x] `SlotProgress`, its members, and `State` public
- [x] `fraction` public
- [x] Confirm `ModelSlot` and `ModelRef` are already public

## Acceptance Criteria

- [x] A module that does a plain `import FoundationModelsRouter` — not
      `@testable` — reads `progress.slots`, and each slot's `state`,
      `chosen`, `bytesDownloaded` and `bytesTotal`, and compiles.
- [x] `swift build` and `swift test` pass with no warning.
- [ ] The change is on `main`, because consumers track the `main`
      branch and not a version.

## Tests

- [x] The compile proof goes in a **separate module** that uses a plain
      `import FoundationModelsRouter`. A test inside the Router test
      target sees internal members through `@testable` and would prove
      nothing.
- [x] That test is `@MainActor`, because the type is.
- [x] It reads every member the consumer needs, and asserts the values
      it wrote back. This is a normal red-to-green test, not a
      "fails to compile" claim.
- [x] `swift test` passes.

## Workflow
- Use `/tdd` — write failing tests first, then implement to make them pass.