{"id":"01M34S2Q8K37ZG21D6JD5BGT1P","timestamp":"2026-09-22T14:42:22.099343Z","op":"create","item_id":"01M34S2Q8K6N94P3E23H4ZH9XV","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,40 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: 8c80\n+title: Delete the 24-hour deadline clamp on run-plane waits\n+---\n+## Decision (from the owner, 2026-09-22)\n+\n+`ToolContext.deadlineSecondsCeiling` (86,400 s, `Hosting/ToolContext.swift:18`) is an invented bound and must go. A caller's deadline is honored as given. No deadline means the wait ends at settlement or at cancellation.\n+\n+## Why\n+\n+- Born as `waitSecondsCeiling` in 8e9566f (2026-08-04), renamed in 0c32e22. No reason for one day.\n+- Today a background run longer than a day ends the turn-end drain early with `deadlineElapsed`, and a caller that asks to wait longer gets less, with no report.\n+- The one real reason for a guard is arithmetic: `Task.sleep(nanoseconds:)` takes `UInt64`, and `UInt64(Double.infinity * 1e9)` traps. That guard stays; it is a fact about the type.\n+\n+## Sites\n+\n+- `Hosting/ToolContext.swift:16-18`: the constant. `:229-234`: docs.\n+- `Hosting/SessionMailbox.swift:414-420` `boundedNanoseconds(clamping:)`: the clamp. `:186-211` `wait(completionToken:seconds:)`: takes a `Double` deadline.\n+- `Session/RoutedSessionActorGeneration.swift:28, 90, 124`: the drain passes the ceiling as its wait.\n+- `Hosting/ToolRun.swift:174`: the timeout window. `Hosting/BackgroundToolRunner.swift:181`: the grace sleep.\n+- Tests that assert the clamp (search `deadlineSecondsCeiling` and `boundedNanoseconds` in `Tests/`).\n+\n+## Do this\n+\n+1. Delete the constant.\n+2. `boundedNanoseconds` keeps only the arithmetic guard: NaN and negative give 0; a value at or above `Double(UInt64.max) / nanosecondsPerSecond` gives `UInt64.max`; everything else converts as is.\n+3. `wait(completionToken:seconds:)` takes `seconds: Double?`. `nil` registers no expiry task; the waiter resumes at settlement, or when the caller's task is cancelled. Keep the `Double` path for a caller that names a deadline.\n+4. The turn-end drain (`awaitSettlement`) passes `nil`. Its exits are settlement and cancellation, which it already handles. Remove the `deadlineElapsed` branch from `settleBackgroundRuns` if nothing can produce it there. Update the docs at `:28` and `:90`.\n+5. `ToolRun` and `BackgroundToolRunner` keep their own values (a per-call timeout is a host-stated number) and pass them through the guard as is.\n+6. Rewrite the docs that mention the ceiling.\n+\n+## Acceptance\n+\n+- `rg 'deadlineSecondsCeiling'` finds nothing.\n+- A `wait` with `seconds: nil` on a run that settles after 2 s returns `.settled`. A `wait` with `seconds: 1` on the same run returns `.deadlineElapsed`.\n+- `boundedNanoseconds(clamping: .infinity) == UInt64.max`; `boundedNanoseconds(clamping: 90_000) == 90_000 * 1e9`.\n+- All tests pass. #compaction #limits\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,40 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: 8c80\n-title: Delete the 24-hour deadline clamp on run-plane waits\n----\n-## Decision (from the owner, 2026-09-22)\n-\n-`ToolContext.deadlineSecondsCeiling` (86,400 s, `Hosting/ToolContext.swift:18`) is an invented bound and must go. A caller's deadline is honored as given. No deadline means the wait ends at settlement or at cancellation.\n-\n-## Why\n-\n-- Born as `waitSecondsCeiling` in 8e9566f (2026-08-04), renamed in 0c32e22. No reason for one day.\n-- Today a background run longer than a day ends the turn-end drain early with `deadlineElapsed`, and a caller that asks to wait longer gets less, with no report.\n-- The one real reason for a guard is arithmetic: `Task.sleep(nanoseconds:)` takes `UInt64`, and `UInt64(Double.infinity * 1e9)` traps. That guard stays; it is a fact about the type.\n-\n-## Sites\n-\n-- `Hosting/ToolContext.swift:16-18`: the constant. `:229-234`: docs.\n-- `Hosting/SessionMailbox.swift:414-420` `boundedNanoseconds(clamping:)`: the clamp. `:186-211` `wait(completionToken:seconds:)`: takes a `Double` deadline.\n-- `Session/RoutedSessionActorGeneration.swift:28, 90, 124`: the drain passes the ceiling as its wait.\n-- `Hosting/ToolRun.swift:174`: the timeout window. `Hosting/BackgroundToolRunner.swift:181`: the grace sleep.\n-- Tests that assert the clamp (search `deadlineSecondsCeiling` and `boundedNanoseconds` in `Tests/`).\n-\n-## Do this\n-\n-1. Delete the constant.\n-2. `boundedNanoseconds` keeps only the arithmetic guard: NaN and negative give 0; a value at or above `Double(UInt64.max) / nanosecondsPerSecond` gives `UInt64.max`; everything else converts as is.\n-3. `wait(completionToken:seconds:)` takes `seconds: Double?`. `nil` registers no expiry task; the waiter resumes at settlement, or when the caller's task is cancelled. Keep the `Double` path for a caller that names a deadline.\n-4. The turn-end drain (`awaitSettlement`) passes `nil`. Its exits are settlement and cancellation, which it already handles. Remove the `deadlineElapsed` branch from `settleBackgroundRuns` if nothing can produce it there. Update the docs at `:28` and `:90`.\n-5. `ToolRun` and `BackgroundToolRunner` keep their own values (a per-call timeout is a host-stated number) and pass them through the guard as is.\n-6. Rewrite the docs that mention the ceiling.\n-\n-## Acceptance\n-\n-- `rg 'deadlineSecondsCeiling'` finds nothing.\n-- A `wait` with `seconds: nil` on a run that settles after 2 s returns `.settled`. A `wait` with `seconds: 1` on the same run returns `.deadlineElapsed`.\n-- `boundedNanoseconds(clamping: .infinity) == UInt64.max`; `boundedNanoseconds(clamping: 90_000) == 90_000 * 1e9`.\n-- All tests pass. #compaction #limits\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M37EBD0WKAEJCQ0ESK72TY3D","timestamp":"2026-09-23T15:32:35.484142Z","op":"update","item_id":"01M34S2Q8K6N94P3E23H4ZH9XV","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: 8c80\n+position_column: doing\n+position_ordinal: '80'\n title: Delete the 24-hour deadline clamp on run-plane waits\n ---\n ## Decision (from the owner, 2026-09-22)\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: 8c80\n title: Delete the 24-hour deadline clamp on run-plane waits\n ---\n ## Decision (from the owner, 2026-09-22)\n","transaction_id":null}
{"id":"01M37EHGRPBKT0D47MRY1A2PW2","timestamp":"2026-09-23T15:35:55.926548Z","op":"update","item_id":"01M34S2Q8K6N94P3E23H4ZH9XV","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,23 @@\n ---\n assignees:\n - claude-code\n+comments:\n+- actor: claude-code\n+  id: 01m37ehgrp46fjf608t4mn8ddq\n+  text: |-\n+    ### implement — changed\n+    Sites found in current code: ToolContext.swift:16-18 (constant) and :224-231 (docs); SessionMailbox.swift:181-206 (wait), :401-407 (boundedNanoseconds), :409-415 (expireWaiter); RoutedSessionActorGeneration.swift:24-27, :87-90, :100, :123, :387-395; ToolRun.swift:174; BackgroundToolRunner.swift:181; SessionMailboxTests.swift:85-116.\n+\n+    Design choices:\n+    - `ToolContext.deadlineSecondsCeiling` is deleted. `rg deadlineSecondsCeiling` finds nothing.\n+    - `boundedNanoseconds(clamping:)` keeps only the type guard: NaN and negative give 0; a nanosecond count at or above `Double(UInt64.max)` gives `UInt64.max`; other values convert as is. No new constant.\n+    - `SessionMailbox.wait` and the public `ToolContext.wait` take `seconds: Double?`. `nil` starts no expiry task. Existing callers that pass a `Double` compile with no change.\n+    - A `nil` wait must end at cancellation, so `wait` now has a task cancellation handler. It ends the waiter with a new public case `WaitOutcome.cancelled`. A named deadline also ends early with `.cancelled` when the calling task is cancelled.\n+    - The turn-end drain passes `nil`. `RunPlaneDrainWaitOutcome` is now `.settled` / `.cancelled`; the `deadlineElapsed` branch is gone from `settleBackgroundRuns`. `awaitSettlement` cancels its inner wait task when it returns, so no waiter stays in the mailbox after a cancellation.\n+    - `ToolRun.watchForTimeout` and `BackgroundToolRunner.settledEnvelope` keep their own values and pass them through the guard as is (no code change there).\n+    - Tests: new SessionMailboxTests for `.infinity` and `greatestFiniteMagnitude` give `UInt64.max`, 90_000 s converts as is, NaN and negatives give 0, `wait(seconds: nil)` on a run that settles after 2 s gives `.settled` while `wait(seconds: 1)` gives `.deadlineElapsed`, and a cancelled `nil` wait gives `.cancelled`. The numbers live in the test only.\n+    - evidence: `swift test`: Test run with 1323 tests in 149 suites passed (2 known issues); Test run with 1 test; Test run with 19 tests. `swift build --build-tests --package-path IntegrationTests`: Build complete.\n+  timestamp: 2026-09-23T15:35:55.926269+00:00\n position_column: doing\n position_ordinal: '80'\n title: Delete the 24-hour deadline clamp on run-plane waits\n","reverse_patch":"--- original\n+++ modified\n@@ -1,23 +1,6 @@\n ---\n assignees:\n - claude-code\n-comments:\n-- actor: claude-code\n-  id: 01m37ehgrp46fjf608t4mn8ddq\n-  text: |-\n-    ### implement — changed\n-    Sites found in current code: ToolContext.swift:16-18 (constant) and :224-231 (docs); SessionMailbox.swift:181-206 (wait), :401-407 (boundedNanoseconds), :409-415 (expireWaiter); RoutedSessionActorGeneration.swift:24-27, :87-90, :100, :123, :387-395; ToolRun.swift:174; BackgroundToolRunner.swift:181; SessionMailboxTests.swift:85-116.\n-\n-    Design choices:\n-    - `ToolContext.deadlineSecondsCeiling` is deleted. `rg deadlineSecondsCeiling` finds nothing.\n-    - `boundedNanoseconds(clamping:)` keeps only the type guard: NaN and negative give 0; a nanosecond count at or above `Double(UInt64.max)` gives `UInt64.max`; other values convert as is. No new constant.\n-    - `SessionMailbox.wait` and the public `ToolContext.wait` take `seconds: Double?`. `nil` starts no expiry task. Existing callers that pass a `Double` compile with no change.\n-    - A `nil` wait must end at cancellation, so `wait` now has a task cancellation handler. It ends the waiter with a new public case `WaitOutcome.cancelled`. A named deadline also ends early with `.cancelled` when the calling task is cancelled.\n-    - The turn-end drain passes `nil`. `RunPlaneDrainWaitOutcome` is now `.settled` / `.cancelled`; the `deadlineElapsed` branch is gone from `settleBackgroundRuns`. `awaitSettlement` cancels its inner wait task when it returns, so no waiter stays in the mailbox after a cancellation.\n-    - `ToolRun.watchForTimeout` and `BackgroundToolRunner.settledEnvelope` keep their own values and pass them through the guard as is (no code change there).\n-    - Tests: new SessionMailboxTests for `.infinity` and `greatestFiniteMagnitude` give `UInt64.max`, 90_000 s converts as is, NaN and negatives give 0, `wait(seconds: nil)` on a run that settles after 2 s gives `.settled` while `wait(seconds: 1)` gives `.deadlineElapsed`, and a cancelled `nil` wait gives `.cancelled`. The numbers live in the test only.\n-    - evidence: `swift test`: Test run with 1323 tests in 149 suites passed (2 known issues); Test run with 1 test; Test run with 19 tests. `swift build --build-tests --package-path IntegrationTests`: Build complete.\n-  timestamp: 2026-09-23T15:35:55.926269+00:00\n position_column: doing\n position_ordinal: '80'\n title: Delete the 24-hour deadline clamp on run-plane waits\n","transaction_id":null}
